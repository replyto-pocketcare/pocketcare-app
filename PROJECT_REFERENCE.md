# PROJECT_REFERENCE.md — PocketCare

> **Living index / audit of the whole repo. Read this first in every session to minimize context.**
> Keep it updated whenever structure, decisions, or conventions change. Detailed rationale lives in `ARCHITECTURE.md`.

**Last updated:** 2026-07-31 · **Status:** Web full-featured (see below); **mobile revival in progress, rev 3 (pure native, two independent apps)** — **Phase 1 domain porting is DONE**, **P2.1 (schema parity) is DONE**, **iOS `xcodebuild test` is human-confirmed GREEN** through 3 rounds of real compiler fixes to P2.2/P2.3/P2.4 (connector, quarantine, auth; Android found+fixed a parallel real bug via source review but remains completely unverified by any real compiler this whole arc — no `./gradlew build` has ever run against this code), and **P2.5 (repositories) has begun** — ledger (accounts/transactions/balance+net-worth derivation) is done on both platforms, 6 domains left (finance/budget/splits/receipts/upi + P2.6 repair logic). See "Native mobile" section / `docs/mobile/TODO.md`. Header test count below is historical/stale — trust the live `pnpm test:core` count (290 passing as of this update) and the change log. **DB fully normalized** (enums→lookup tables, M2M via junctions).

## ⚠️ Pivot (2026-07-02): web-only
Mobile (`apps/mobile`) was DELETED. Sole client is `apps/web` (Next.js), installable as a **PWA** via Chrome. Shared `packages/*` (money, finance, ledger, budget, entitlements, i18n, types, ui-tokens, db, data) remain the engine. Animations use **three.js / @react-three/fiber** (card wallet), **framer-motion** (progress bars), **recharts** (charts).

---

## What this is
Offline-first personal expense & wealth manager. Cross-device (iOS/Android + **web**), home-screen widgets, wearable companions. **Multi-language + multi-currency.** Earthy, minimal theme with elegant animations. Freemium.

## Stack (locked)
- **Monorepo:** Turborepo + pnpm. `apps/mobile` (Expo) + `apps/web` (Next.js) share `packages/*` core (~90% logic reuse).
- **Mobile:** React Native + Expo (dev build / CNG — **never Expo Go**), TypeScript strict.
- **Web:** Next.js App Router + PowerSync **Web SDK** (WASM SQLite, **no SSR** for synced DB; enable `asyncWebAssembly`+`topLevelAwait`).
- **Local DB:** SQLite via **PowerSync** (native on mobile, WASM in browser). **Sync:** PowerSync ↔ **Supabase** Postgres (server-authoritative).
- **Backend:** Supabase (Auth, Postgres, RLS, Storage, Edge Functions).
- **UI:** Reanimated 3 + Moti + Gesture Handler; **Skia**/Victory Native for charts; Zustand for UI state only.
- **i18n/l10n:** i18next + react-i18next (shared bundles in `packages/core/i18n`), expo-localization, **Intl** for number/date/currency formatting, RTL support.
- **Payments:** RevenueCat. **Notifications:** expo-notifications + Edge Functions.
- **Native:** WidgetKit (iOS) + App Widget (Android); watchOS + Wear OS companions.

## Golden rules (financial integrity — do not violate)
1. Money = **integer minor units**, never floats. Use the `Money` type in `src/lib/money`.
2. Balances are **derived from an append-only ledger**, never mutated in place.
3. Initial-balance changes = new `opening_balance`/`adjustment` ledger entries.
4. Transaction + its breakdown items written in **one atomic SQLite tx**; items **must sum** to total (client + Postgres check).
5. Sync is **server-authoritative**; synced ledger entries are immutable — corrections are compensating entries.
6. Reads always come from local SQLite (fully usable offline).
7. **Multi-currency:** amounts stored in their account's native currency + ISO code; **never** converted in place. Convert only at display/aggregation time via `exchange_rates` (as-of date for history). Cross-currency transfers capture `fx_rate` + `to_amount`.
8. **One user id from first launch** (Supabase anonymous auth). Guest→registered = upgrade same UID in place (no data movement). All tables FK `user_id` `ON DELETE CASCADE` → deletion is one cascade. See ARCHITECTURE.md §9.

## Data model (see ARCHITECTURE.md §4 for columns) — NORMALIZED
Entity tables: `profiles · accounts · transactions · transaction_items · categories · labels · budgets · credit_card_details · goals · goal_allocations · loans · recurring_commitments · subscriptions · planned_cashflow · holdings · price_snapshots · exchange_rates · entitlements · guest_sessions · statements`

**Lookup tables** (enums as tables; `id = code`, `label`, `sort`; FK-referenced): `account_types · transaction_types · category_kinds · periods · commitment_kinds · tiers · rate_modes · payment_methods`. Seeded in `0001_init.sql`; synced globally read-only (`reference_data` stream).

**Junction (M2M) tables:** `transaction_labels` (transaction↔labels) · `budget_categories` (budget↔categories) · `budget_labels` (budget↔labels) · `account_type_payment_methods` (which payment methods apply to each account type; global read-only).

Key FKs: `accounts.type→account_types` · `transactions.type→transaction_types` · `transactions.payment_method→payment_methods` · `categories.kind→category_kinds` · `budgets.period / subscriptions.billing_cycle / recurring_commitments.frequency→periods` · `recurring_commitments.kind→commitment_kinds` · `entitlements.tier→tiers` · `profiles.rate_mode→rate_modes`.

`profiles` holds `base_currency` (default **INR**), `locale`, `rate_mode`. Translations are static JSON in `packages/core/i18n` (not a DB table).

Codes — Account types: `savings | current | credit_card | cash | mutual_funds | stocks`. Transaction types: `income | expense | transfer | opening_balance | adjustment`. Payment methods: `upi | debit_card | net_banking | credit_card | cash` (savings/current→upi,debit_card,net_banking; cash→cash; credit_card→credit_card).

**Reading labels:** transactions no longer have a `label` text column. Compute display strings via `GROUP_CONCAT` subquery over `transaction_labels JOIN labels`. Repo `transactions.create/update` takes `labels: string[]` (find-or-create by name → junction). Budget scope comes from `budget_categories`/`budget_labels` (no more `scope`/`scope_ref`/`category_ids`/`label_names`). Payment method pickers read `account_type_payment_methods JOIN payment_methods` and store the **code** (FK).

## Structure (Phase 0 built ✅ except where noted)
```
apps/mobile (Expo router: app/_layout, app/index)      ✅ skeleton
apps/web    (Next.js App Router: app/layout, app/page)  ✅ skeleton
packages/types            ✅   packages/ui-tokens (earthy theme) ✅
packages/core/money  ✅ tested (15)   packages/core/finance ✅ tested (13)
packages/core/entitlements ✅ tested (3)   packages/core/ledger ✅ tested (8)
packages/core/budget ✅ tested (10: period bounds, progress, threshold, CC cycle)
packages/core/sync-policy ✅ tested (21: transient vs permanent classification, quarantine policy, user-facing copy)
packages/core/diagnostics ✅ tested (20: redaction — amounts/emails/UPI/tokens stripped, error codes + UUIDs preserved)
packages/core/upi ✅ tested (29: intent URL, VPA validation, amount formatting, injection safety)
packages/core/receipts ✅ tested (88: allocation, reconciliation, money/date parsing, 10 receipt fixtures)
packages/core/i18n ✅ (en/hi/nl; wired into nav + page titles + settings)
packages/db (schema + connector + auth; incl. credit_card_details) ✅   packages/data (interfaces + PowerSync repos) ✅
  db sync config: **sync-streams.yaml** (Sync Streams, edition 3) ✅ — the legacy sync-rules.yaml has been removed; deploy this file under Sync Streams in the PowerSync dashboard, NOT under Sync Rules
supabase/migrations: 0001_init ✅ · 0002_seed_and_credit_cards ✅ (validated) · …latest: `0015_templates_recurring`, `0016_template_sort`, `0017_category_rules`, `0018_holdings_exchange`, `0019_account_allow_negative`, `0020_market_data`, `0021_security`, `0022_feedback_coupons`, `0023_report_kind`, `0024_promos_segments`, …`0040_receipts_and_expense_items`, `0041_payment_handles`, `0042_drop_expense_items_sum_trigger`, `0043_bug_report_diagnostics`, `0044_client_errors` (all pglast-valid; apply in order + redeploy sync-streams.yaml)   supabase/functions: split-invite, split-invite-accept, market-sync, redeem-coupon, receipt-scan, payment-handle   scripts: support-admin.ts, gen-support-key.ts
apps/mobile: index, accounts, account/new, transaction/new, budgets, src/{powersync,notifications} ✅
apps/web: providers + accounts, accounts/new, transactions/new, budgets, src/powersync ✅
```
Total tests: **49 passing** (`pnpm test:core`). Repos: accounts, transactions, balances, budgets, creditCards.
Reuse boundary: everything in `packages/*` is shared; only presentation differs (RN primitives vs DOM). Charts share a data contract, platform-specific renderers.
Package names: `@pocketcare/{types,money,finance,i18n,entitlements,db,data,ui-tokens,mobile,web}`.

## Feature index (21 + extras) → all mapped in ARCHITECTURE.md §5
Accounts, balances, transactions+breakdown, budgets, credit cards, goals/emergency fund, blocked-balance views, subscriptions+simulator, investments toggles, dashboard, loans/recurring, compounding projections, sub-item calculator, search+statements, onboarding+3-day guest, freemium gating, widgets, wearables.

## Freemium split (proposed)
Free: accounts, transactions, basic budget, search. Premium: advanced analytics, goals+projections, subscription simulator, investment auto-fetch, statements, widgets/wearables, comparisons. Gate via `useEntitlement(feature)` (works offline).

## Roadmap phases
0 Foundations (monorepo, mobile+web wired to Supabase/PowerSync) · 1 Core ledger (MVP) · 2 Budgets+credit cards · 3 Goals+EF · 4 Recurring/loans/subs · 5 Investments+dashboard · 6 Statements/onboarding/guest/freemium · 7 Widgets+wearables+web PWA.
Each feature ships to mobile **and** web in the same phase (shared core); web uses a PWA in place of OS home-screen widgets.

## Open decisions
Market-data API for holdings · FX rate provider for `exchange_rates` · launch languages (which + RTL) · RevenueCat vs native billing.

## Gotchas / platform notes
- **Single React (build break if duplicated):** `react-native@0.74.5` (transitive via `@react-three/fiber` + `react-i18next` optional peers) pins `react: 18.2.0` exactly, which pulls a **second React** into the tree. With two Reacts, `next build` fails prerendering the error pages (`/_error: /404,/500`) with `useContext` null / invalid hook call. Fixed by a root **`pnpm.overrides`** forcing `react`/`react-dom` to `18.3.1` — after editing overrides run `pnpm install` to dedupe. Don't remove this override.
- **PowerSync web must NOT be instantiated during SSR** (crashes: `SSRDBAdapter … tx.execute is not a function`). `apps/web/src/powersync.ts` creates the DB lazily, browser-only (`getDb()`, `getRepositories()`); `app/providers.tsx` gates the tree until `initSystem()` resolves on the client. Never construct `PowerSyncDatabase` at web module top-level.
- i18n JSON is imported plainly (no `with { type: "json" }` import attributes) for SWC/webpack compatibility.
- **Client env must be referenced statically**: `process.env.NEXT_PUBLIC_*` (web) / `process.env.EXPO_PUBLIC_*` (mobile). Dynamic `process.env[key]` is NOT inlined into the client bundle → value is undefined at runtime. Both `src/powersync.ts` files use static consts.
- **One root `.env` for the monorepo**: `apps/web/next.config.js` loads `../../.env` (dependency-free parser). Expo (mobile) still needs the vars in `apps/mobile/` — symlink or copy the root `.env` there.
- **Mobile monorepo setup (required to compile):** `apps/mobile/babel.config.js` (babel-preset-expo + reanimated plugin), `apps/mobile/metro.config.js` (watchFolders = repo root, nodeModulesPaths, disableHierarchicalLookup). Root `.npmrc` sets `node-linker=hoisted` because Metro can't resolve pnpm's symlinked store — **reinstall (`pnpm install`) after adding it**.
- Transaction save: both apps' add-transaction screens pick an account + type and call `repositories.transactions.create` (atomic txn+items). Breakdown items only sent when >1.
- **Auth / identity model:** boot does NOT auto-create a guest. `initSystem` connects only if `getSession()` finds a session; otherwise the user is routed to `/onboarding` (create account / sign in / try-as-guest). Register = `signUp()` for fresh users, in-place `updateUser` only when upgrading an existing anonymous guest. `src/powersync.ts` re-keys the local DB on `onAuthStateChange` when the user id changes (clears + reconnects with the new JWT) so data syncs correctly across devices; same-UID guest→register keeps local writes. `useAuthStatus()` gates the app shell.
- **Mobile no-horizontal-scroll invariants:** body has `overflow-x: clip`; grid children need `min-width:0` (see `.shell-main`); fixed-width inputs are capped by `.input{max-width:100%}`; responsive card grids use `minmax(min(Npx,100%),1fr)`; wide tables get an `overflow-x:auto` wrapper; two-column inline grids should carry `className="dash-cols"` to collapse ≤860px.

## Conventions
- **Toolchain:** Node 22+ (`.nvmrc`), pnpm 9 workspaces, Turborepo. TS strict + `noUncheckedIndexedAccess` + `verbatimModuleSyntax`.
- **Erasable TS only** in shared packages (no `enum`/namespaces) — const-object unions instead — so Node type-stripping runs tests with no build step.
- **Tests:** required for all money/finance/entitlements logic. Run `pnpm test:core` (Node built-in `node:test`, `--experimental-strip-types`). Currently 31 passing.
- **Secrets:** `.env` from `.env.example`; web vars need `NEXT_PUBLIC_`, mobile vars need `EXPO_PUBLIC_`. Never commit `.env`.
- **Money:** always use `@pocketcare/money` (integer minor units); never do raw arithmetic on amounts.
- _TODO: lint/format config (eslint/prettier), commit style — add when apps get built out._

## Web app pages (apps/web/app)
`/` dashboard (net worth ±blocked, accounts, recent, spending pie) · `/accounts` (+color, net-worth toggle) · `/accounts/new` · `/transactions` (search+filter) · `/transactions/new` (income/expense/transfer + cross-currency + breakdown) · `/cards` (three.js wallet, scroll-driven, settle-up) · `/budgets` (animated bar) · `/goals` (EF-first, animated bar) · `/cashflow` (**BETA** Planned Cashflow hub: incomes + payments[subs+loans+household] + savings + AI projections; `/subscriptions` & `/loans` now redirect here) · `/investments` (holdings + auto-fetch toggle) · `/settings` (currency, language, dark mode, categories+sub-categories, colored labels, plan) · `/onboarding` · `/login` (guest→register in place) · `/receipts/new` (scan a bill: on-device OCR + opt-in AI fallback) · `/receipts/review` (edit the parsed draft; must reconcile before saving) · `/receipts/split` (per-item assignment across a group).
Shell: `AppShell.tsx` (sidebar nav, dark-mode toggle, install button, SW register). Data hooks: `src/hooks.ts` (net worth via ledger+FX, respects `include_in_net_worth`). Writes: `src/write.ts`. 3D: `src/cards/WalletScene.tsx`. PWA: `public/manifest.webmanifest`, `public/sw.js`, icons.
Also: `/insights` (recharts: cashflow, net trend, by category/label, month comparison — Premium) · `/statements` (period picker, printable/PDF — Premium) · `/accounts/[id]/edit` (name/type/color/net-worth/archive).
New deps to install: `@react-three/fiber @react-three/drei three framer-motion recharts` (+types).
Migrations: single **`0001_init.sql`** (normalized; 0002–0004 folded in). **Fresh Supabase project + redeploy sync-streams** (adds `reference_data` global stream + `transaction_labels`/`budget_categories`/`budget_labels` in user_data).
Premium gating via `useTier()` (reads entitlements). Settings has a Free/Premium toggle (updates entitlements.tier). Budgets support name + scope(category/label) + recurring period OR custom date range (repo handles both).

## Native mobile

**Status (2026-07-31, rev 3 — current direction): PURE NATIVE, planning only.** `apps/mobile` (the rev-2 React Native/Expo attempt) never made it into a commit — a full repo reset to `origin/main` removed it from disk along with a session's worth of build-breakage debugging (Xcode/Clang vs. the `fmt` pod's `consteval` usage, `expo-localization` switch exhaustiveness, three rounds of `lru-cache` export-shape mismatches, a stale `expo` pin — see the change log below for the entries that survived, since those were committed). Owner decision after that: two fully independent native apps — **Android (Kotlin 2.x + Jetpack Compose)**, **iOS (Swift 6 + SwiftUI)** — no cross-platform layer at all, first-party OS frameworks by default.

**Work queue: `docs/mobile/TODO.md`** (claim 1–3 finishable tasks, capability tags [S]/[M]/[H], mandatory ≤15-line Handover block rewritten **and committed** every session — a plan that isn't committed doesn't survive, learned the hard way twice now). **Full plan: `docs/plans/native-mobile-apps.md`** (rev 3). Executors: read this section + TODO.md + only the plan sections for your current task.

**ADR (rev 3):** two independent native apps, no shared runtime code between them. What IS shared: backend, `sync-streams.yaml`, **golden vectors** (`tools/golden-vectors/` — exported from the TS core, the law for both ports, not yet created), generated design tokens + i18n catalogs, test catalog. The 290-test TS suite remains the single spec; behavior changes start in `packages/core`, then vectors re-export, then both apps fix. Rejected: RN/Expo (attempted, rev 2 — third-party breakage risk owned by others), KMP (a cross-platform toolchain is the same class of dependency, especially on iOS), Capacitor.

**Sandbox constraint (new as of rev 3):** the agent sandbox has node/npm only — no JDK compiler, no Gradle, no Xcode. `apps/android`/`apps/ios` skeletons (P0.2/P0.3) can be written here but their Done-when (`./gradlew build test` / `xcodebuild test`) can only be verified in CI or on a human's real machine.

**P0.1 done (2026-07-31):** `tools/golden-vectors/export.ts` + `vectors/*.json` — 250 vectors across 16 domain files, ≥1 per public function, two runs verified byte-identical. This is the fixture set P0.4's kotlin.test/XCTest runners load and P1.x's ports must reproduce exactly.

### Repo layout (rev 3 target)
```
apps/android/          # :app, :domain (pure Kotlin, vector-tested), :data, :widgets (Glance), :baselineprofile
apps/ios/               # App, Domain (pure SwiftPM pkg, vector-tested), Data, Widgets+LiveActivity targets; XcodeGen
tools/golden-vectors/  # export.ts + vectors/*.json — single fixture source for BOTH apps
docs/mobile/            # TODO.md (queue+handover)
```

### Feature parity (Android / iOS) — tracks `docs/plans/native-mobile-apps.md` (rev 3) §7 slices
| Slice | Feature | Web | Android | iOS |
|---|---|---|---|---|
| S1 | Onboarding/walkthrough, auth (guest/OTP/Google) | ✅ | ⬜ not started | ⬜ not started |
| S1 | Accounts, transactions, dashboard-lite, settings-lite | ✅ | ⬜ | ⬜ |
| S2 | Budgets, goals/EF, cashflow hub, credit cards | ✅ | ⬜ | ⬜ |
| S3 | Splits & groups, UPI settle-up | ✅ | ⬜ | ⬜ |
| S4 | Receipts (camera OCR), statement import | ✅ | ⬜ | ⬜ |
| S5 | Investments, insight cards, statements | ✅ | ⬜ | ⬜ |
| S6 | Assistant (voice input) | ✅ | ⬜ | ⬜ |
| P4 | Widgets, Live Activities (iOS), native push, biometric lock | — | ⬜ | ⬜ |

## Change log
- 2026-07-31 (latest of all, absolute newest of all — P2.5 begins) — **Mobile: P2.5 (repositories) started — ledger (accounts, transactions, balance/net-worth derivation) done on both platforms, the dependency root every other domain's repository will read off.** New files: `WriteHelpers.kt`/`.swift` (Kotlin/Swift mirror of `apps/web/src/write.ts`'s `insertRow`/`updateRow`/`softDelete` — the generic synced-row helpers every repository builds its writes on, per CLAUDE.md's stated web convention) and `LedgerRepository.kt`/`.swift` (accounts/transactions reads and writes; `watchAccountBalances`/`watchNetWorth` call the already-ported `deriveBalance`/`aggregateNetWorth` domain functions — never recomputing balance math locally, which is the actual point of P2.5). Schema confirmed column-by-column against `PocketCareSchema.kt`/`.swift` (P2.1) rather than assumed; query shapes copied from `apps/web/src/hooks.ts`'s real `useAccountBalances`/`useNetWorth`/`useBlockedByAccount`/`useRates` (IFNULL defaults for archived/include-in-net-worth/kind, emergency-fund exclusion from blocked amounts, latest-FX-rate-wins ordering) so mobile matches the web app's actual behavior, not a plausible-looking reimplementation. Two real API findings from source verification, not assumption: (1) Kotlin's `Queries.watch` returns `Flow<List<T>>`, which has a real `combine()` operator — used directly for the derived multi-table views. Swift's `Queries.watch` returns `AsyncThrowingStream<[T], Error>`, which has no built-in combine/combineLatest; hand-rolling one blind (no compiler available) was judged disproportionate risk for Phase 2's actual Done-when (TP L3 sync correctness, not UI reactivity) — so iOS's derived views (`accountBalances`/`netWorth`) are one-shot async snapshot functions while single-table reads (`watchAccounts`/`watchTransactions`) remain real reactive streams. This is a documented, deliberate platform asymmetry (flagged inline in `LedgerRepository.swift`'s header), not an oversight, left for Phase 3+ UI wiring to resolve once the actual UI framework choice is made. (2) `RateLookup` on both platforms is a plain function-type alias, not a `fun interface`/functional-interface protocol — building one via a SAM-style `RateLookup { ... }` constructor call (which was tried first) does not compile for a type alias; fixed to a lambda assigned to an explicitly-typed `val`/`let` instead. Also: Kotlin boolean columns are written as explicit `Long` 0/1 rather than `Boolean`, since the SQLite bind layer's `Boolean` parameter support could not be independently confirmed from this sandbox and `Long` is unambiguously safe. Balance-checked and fully re-read all 4 new files. NOT yet build-verified on either platform — this is materially more complex, uncompiled surface (SQL string building, cursor mapping, generic dictionary-based writes) than the P2.2-P2.4 fixes verified so far, so getting a real compiler run on this specific code is the priority next step before continuing to the remaining 6 P2.5/P2.6 domains. `docs/mobile/TODO.md`'s P2.5 queue row and Handover updated accordingly.
- 2026-07-31 (latest of all, absolute newest of all — post-round-3) — **Mobile: iOS `xcodebuild test` confirmed GREEN by the user after round 3's fixes; found + fixed the same real bug on Android (source-verified), and completed P2.3's missing piece on both platforms (diagnostics wiring).** (1) Android's `SupabaseConnector.kt` had the exact same class of bug just fixed on iOS: `CrudEntry.opData`'s real type (powersync-kotlin 1.13.0, `CrudEntry.kt`/`SqliteRow.kt` at the pinned tag) is `SqliteRow?`, and `SqliteRow` itself implements `Map<String, String?>` — stringified, with the real typed values behind a separate `.typed: Map<String, Any?>` accessor. The existing code did `opData?.toMap()` into a `Map<String, Any?>`-typed val; Kotlin's `Map<out V>` covariance means this assignment produces ZERO warning or error (unlike Swift's invariant Dictionary, which at least sometimes catches this shape of mistake) — so this would have silently uploaded every field, including money minor-unit amounts, as a JSON string, with no compiler ever able to catch it. Fixed all 3 call sites (`toOpSummary`, the `.put` and `.patch` upload branches) to use `.typed`. Also fixed `clientId` (confirmed non-nullable `Int` in the real SDK via the same source, not `Int?` as the `?.toLong()` call implied) and documented a real SDK constraint discovered in the process: `SqliteRow.typed`'s numeric parser (`jsonNumberOrBoolean()`) always produces 32-bit `Int`, never `Long`, so any future synced column needing bigint range would silently overflow — not fixable from our side, flagged for future schema review. (2) P2.3 ("Quarantine / dead-letter queue... diagnostics `formatLog`/`makeEntry`, already ported, log them") was listed TODO, but investigation showed the dead-letter persistence and retry-loop wiring were already done as part of P2.2 — the actual missing piece was that the connector's failure path only did ad-hoc `Log.e`/`print` calls and never touched the already-ported `diagnostics` domain (P1.6) at all, so there was no way to produce the redacted, human-readable report `formatLog` exists to generate for a future "share diagnostics" support flow. Implemented `DiagnosticsLog.kt`/`DiagnosticsLog.swift` on both platforms: a capped 200-entry in-memory ring buffer, `logDiagnostic()` feeding every failure through `makeEntry` (which redacts money/secrets/PII on the way in — safe to pass raw failure messages straight through), `diagnosticsReport()` wrapping `formatLog`. Wired into both `SupabaseConnector`s' failure branch, with severity mapped from the sync-policy domain's classification (`FAILURE_CLASS_PERMANENT` → ERROR, transient → WARN). Both new files required care around per-file import rules discovered/reconfirmed in the process: `SupabaseConnector.swift` needed an explicit `import Domain` added (its pre-existing `Classification`/`FailureDetails` usages worked without one, via inferred chained member access on same-target types — but bare enum-case shorthand like `.obj(...)` and top-level constants like `FAILURE_CLASS_PERMANENT` need the declaring module directly imported in that specific file); `SupabaseConnector.kt` needed 4 new imports for the same reason (Kotlin has an identical per-file, not per-package, import rule). Balance-checked and fully re-read both connector files and both new files; NOT yet re-verified by an actual `xcodebuild`/`./gradlew build` run. `docs/mobile/TODO.md`'s Phase 2 queue table updated: P2.2/P2.3/P2.4 status now distinguishes "code complete, iOS xcodebuild-clean" from "code complete, Android never build-verified" rather than a blanket BLOCKED, though TP L3 (real sync round-trip) remains the actual Done-when gate for all three regardless of compile status.
- 2026-07-31 (latest of all, absolute newest of all — round 3) — **Mobile: fixed the 3rd real `xcodebuild` error batch, this time in `SupabaseConnector.swift`, plus one data-correctness bug found while chasing it down.** Errors: (1) `entry.clientId.map { Int64($0) }` — `CrudEntry.clientId` is a non-optional `Int64` in the real SDK (confirmed via both DocC and the actual `CrudEntry.swift` source at the pinned commit), so `.map` doesn't exist; fixed to pass `entry.clientId` directly. (2) `AnyJSON.number`/`.boolean` don't exist — fetched supabase-swift's real `AnyJSON.swift` (its path, `Sources/Helpers/AnyJSON/AnyJSON.swift`, wasn't guessable — 3 plausible `raw.githubusercontent.com` paths all returned silently empty; found via jsdelivr's flat directory-listing API instead); the real cases are `.integer(Int)`, `.double(Double)`, `.bool(Bool)`, `.string`, `.null`, `.object`, `.array` — fixed both call sites. (3) Beyond the reported errors: fetching `CrudEntry.swift` for fix #1 revealed that `CrudEntry.opData` (used everywhere in the file) is a *deprecated, stringified* accessor — `[String: String?]?` — that turns every column value into a String for backwards compatibility; the typed accessor is `opDataTyped: JsonParam?` (`JsonParam = [String: JsonValue]`). The old code compiled fine (Swift silently widens `String?` into `Any?`/`AnyJSON.string`) but would have uploaded every field — including money minor-unit integers — to Postgres as a JSON string, a silent violation of golden rule #1 that no compiler would ever catch, only a real round-trip against the database. Switched `uploadData`'s row-building (both `.put` and `.patch`) and `toOpSummary`'s dead-letter payload to `opDataTyped`, adding two small recursive converters (`anyJSON(from: JsonValue) -> AnyJSON`, `anyValue(from: JsonValue) -> Any?`) since `JsonValue`'s own `toValue()` method exists in the SDK but isn't `public` — it's module-internal and can't be called from `Data`. Balance-checked (45/45 braces, 129/129 parens, 19/19 brackets) and fully re-read for coherence; NOT yet re-verified by an actual `xcodebuild` run. `docs/mobile/TODO.md` Handover rewritten with the specific lesson: a file compiling clean is not the same as a file being correct, especially for accessors an SDK marks deprecated — and flagged an open question of whether Android's `SupabaseConnector.kt` (which reads `entry.opData` directly into `Map<String, Any?>`) has the same trap on the Kotlin side, unverified this session.
- 2026-07-31 (latest of all, absolute newest of all) — **Mobile: fixed 3 real `xcodebuild` errors in `Quarantine.swift`**, the first real compiler feedback this arc has had on the Swift side (the prior session's Swift review was source-inspection-only, since this sandbox still has no Swift toolchain). Errors, all in `apps/ios/Data/Sources/Data/Quarantine.swift`: (1) `cursor.getLong(index: 0)` — PowerSync Swift's `SqlCursor` protocol has no `getLong` at all (a Kotlin-ism); fixed to `try cursor.getInt64(index: 0)`, confirmed against the SDK's real DocC JSON index (`getInt64(index:)`/`getInt64Optional(index:)` are the actual Int64 accessors). (2) five `type 'Any' does not conform to the 'Sendable' protocol` errors from `code as Any`/`failure.code as Any`/etc. inside `parameters:` arrays — `Queries.execute`/`getOptional`'s real signature takes `parameters: [Sendable?]?`, not `[Any]`, and `Any` doesn't conform to `Sendable`; fixed by removing the casts entirely (the already-`Sendable`-conforming values coerce automatically via the array literal's contextual type) except for one non-literal `.map` call, which needed an explicit `$0 as Sendable?`. (3) `shouldQuarantine(classification, attempts: attempts)` — "extraneous argument label" — our own ported `SyncPolicy.swift` declares `shouldQuarantine(_ c: Classification, _ attempts: Int)` with an unlabeled second parameter; this one had actually already been caught and fixed correctly during the prior session's source-inspection-only review (by reading our own already-ported Domain source, which is just as checkable as a third-party SDK), before any real compiler output existed — confirms that source inspection *can* catch real bugs, just not all of them (it didn't catch the Any/Sendable or getLong issues, which needed the compiler). `Auth.swift` and `SupabaseConnector.swift` compiled clean with no changes needed, matching the prior session's high-confidence read of those files. Android (`Auth.kt`/`SupabaseConnector.kt`/`Quarantine.kt`) remains untested by any real compiler — the Kotlin bug fixes from the prior session (schema-qualification, OTP type) are still source-inspection-verified only, not `./gradlew build`-verified. `docs/mobile/TODO.md` Handover rewritten with the specific lesson: source inspection against real pinned SDK code is a good stand-in for a missing compiler and catches real bugs, but isn't equivalent to actually compiling — Swift-specific rules like existential `Any`-vs-`Sendable` coercion only surface once `xcodebuild` runs for real.
- 2026-07-31 (latest of all, absolute newest of all) — **Mobile: verified another agent's P2.2/P2.4 (PowerSync connector + Auth, commit 4633c6f plus uncommitted working-tree fixes) against real pinned SDK source, since this sandbox has neither a JDK (JRE-only, no javac) nor a Swift toolchain and can't compile either platform.** Instead of guessing, fetched the actual dependency source at the exact pinned versions — `apps/ios/Data/Package.resolved` proved `swift package resolve` had genuinely run on a real machine (powersync-swift 1.15.1, supabase-swift 2.54.1), so fetched `SupabaseClient.swift`/`Types.swift`/`AuthClient.swift` from supabase-swift and cross-checked every API call (`session.user` non-optional with a direct `isAnonymous` property, `client.schema(_:)` real per-call schema-scoping method, `UserAttributes`/`OpenIDConnectCredentials`/`EmailOTPType.email`/`signInWithIdToken` all confirmed present with matching signatures) — no bugs found on iOS, high confidence. For Android (`libs.versions.toml` pins powersync-kotlin 1.13.0, supabase-kt 3.6.0 via BOM), fetched supabase-kt's actual `Postgrest.kt` and PowerSync's own official Kotlin Supabase connector reference implementation, and found two real bugs that compile cleanly but are functionally wrong: (1) `SupabaseConnector.kt`'s `client.postgrest[op.table]` uses the 1-arg subscript, which resolves against `Postgrest.Config.defaultSchema` ("public" unless configured — nothing in this codebase configures it yet) — silently dropping the schema-qualification CLAUDE.md's golden rule #3 requires, which would 404 or target the wrong schema on every real sync upload; fixed to the real 2-arg `client.postgrest[schema, op.table]` overload (confirmed to exist in supabase-kt's actual source, not assumed). (2) `Auth.kt`'s `verifyOtp` used `OtpType.Email.MAGIC_LINK`, a different and more specific OTP type than the generic email-OTP flow `sendOtp` actually starts; fixed to `OtpType.Email.EMAIL` (confirmed via Supabase's own Kotlin reference docs, and now consistent with iOS's `.email` case). Also normalized `batch.complete("")` to `batch.complete(null)`, matching PowerSync's own official connector's `transaction.complete(null)` rather than passing an arbitrary empty-string write-checkpoint value. Two items flagged as needing a real compiler rather than fixed blindly, since evidence was suggestive but not conclusive: whether `getCrudBatch()` (vs. the newer `getNextCrudTransaction()`) is still current API, and whether `Auth.kt`'s `appMetadata`-based `isGuest` workaround is even necessary (evidence found suggests `UserInfo.isAnonymous` may still exist directly in supabase-kt 3.x, contradicting the working-tree comment that motivated the workaround, but the exact 3.6.0 behavior couldn't be pinned down with certainty). `docs/mobile/TODO.md`'s Handover rewritten with the full list for whoever gets a real `./gradlew build`/`xcodebuild` next.
- 2026-07-31 (latest of all, absolute newest of all) — **Mobile P2.2 and P2.4 implemented (PowerSync connector, Auth):** Wrote the `SupabaseConnector` and `Auth` modules for both Android (Kotlin) and iOS (Swift) natively. The Android implementation uses `supabase-kt` 3.x with Ktor OkHttp and `kotlinx.serialization` for payload JSON construction. The iOS implementation uses `supabase-swift` v2 and the PowerSync Swift SDK. Both implement op-coalescing (batching multiple `PUT`/`DELETE` operations into single array upserts and `.in()` queries) while respecting quarantine checks and `MAX_PERMANENT_ATTEMPTS`. Added basic unit tests for pure-logic components (`opKey`, `encodePayload`, `AuthState`). The code is written and integrated into the `data` (Android) and `Data` (iOS) modules, but is currently marked as **BLOCKED** in `TODO.md` because verification requires L3 integration testing against a reachable Supabase instance, which is unavailable in the sandbox.
- 2026-07-31 (latest of all, absolute newest of all) — **Mobile P2.1 DONE (schema parity, 700cd24): `AppSchema`'s 63 PowerSync tables mirrored onto Android/iOS as a generated schema descriptor, not hand-typed.** New tooling: `tools/golden-vectors/export-mobile-schema.mjs` imports a stripped copy of `packages/db/src/index.ts` (everything up to but not including the trailing `export { SupabaseConnector, ... } from "./connector.ts"` block — that block pulls in TS parameter-property syntax `node --experimental-strip-types` can't parse, and it's irrelevant to the schema anyway) and dumps `AppSchema.tables` to `tools/golden-vectors/vectors/mobile-schema.json` via each `Table`'s own `.toJSON()`; `tools/golden-vectors/gen-mobile-schema.mjs` renders that JSON into `apps/android/domain/.../db/PocketCareSchema.kt` and `apps/ios/Domain/Sources/Domain/PocketCareSchema.swift`. Chose introspection-and-codegen over hand-transcription deliberately: 63 tables × ~10 columns × 2 platforms is exactly the volume where P1.6's transcription bugs (hidden control-byte literals, a wrong FNV constant) would recur, and the runtime dump immediately surfaced a genuine per-table inconsistency in the TS source that would've been easy to miss reading by eye — every PowerSync table gets an implicit `id: TEXT` primary key, but only some table definitions (`profiles`) redeclare it explicitly in their column map while most (`notifications`, etc.) rely on the implicit one; captured correctly for all 63 by asking the real `Table` objects, not by pattern-matching source text. Scoped deliberately as a schema DESCRIPTOR (table/column names, SQLite storage type TEXT/INTEGER/REAL, indexes, local-only flags) rather than per-table row model classes with real nullability/FK typing — that's P2.5 (repositories) territory once a local SQLite driver is chosen per platform, and generating it now would mean guessing at a design decision not yet made. This pair of scripts IS the plan's requested 3-way parity check: re-running both after `packages/db/src/index.ts` changes and diffing the two generated files shows exactly what drifted. Verified two ways before committing: (1) a from-scratch Python structural diff (bracket-aware parsing, not naive regex — an earlier draft of this check produced false-positive "index column mismatch" reports on every multi-column index because a non-greedy regex was breaking on nested parens; rewritten with proper matching-paren tracking) found **zero mismatches** across all 63 tables' columns/indexes/local-only flags, on both Kotlin and Swift, against the canonical JSON; (2) re-running both scripts from a clean `AppSchema` produced sha256-identical output to what's committed, confirming the generation is deterministic — the property the ongoing parity check depends on. `docs/mobile/TODO.md`'s P2.1 rows moved TODO → DONE, Handover rewritten, "Next up" pointed at P2.2 (connector) or P2.4 (auth), both now unblocked.
- 2026-07-31 (latest of all, absolute newest of all) — **Mobile: Phase 1 domain porting is DONE — all 16 domains / 250 golden vectors human-confirmed green on both platforms** (`./gradlew test` Android, `swift test --package-path Domain` iOS), zero fixes needed for P1.5 (receipts) through P1.7 (entitlements) — a notably cleaner result than P1.4's iOS pass, which needed two real Swift 6 fixes after first compile (27a91cb, 6f320b9). Plausibly (not provably) the extra Python cross-check verification pass against the full TS `*.test.ts` surfaces (previous change log entry) caught what a Kotlin/Swift compiler alone couldn't — the two sessions' outcomes are consistent with that but it's one data point, not a controlled comparison. Phase 1's gate (plan §5: "all vectors green on both apps") is cleared. `docs/mobile/TODO.md`'s Phase 2 section (data layer, plan §6 — "not expanded yet, expand once Phase 1 is ~80% done") is now expanded into 6 tasks × 2 platforms: schema parity (mirror `AppSchema` as Kotlin/Swift models + a 3-way parity check script), PowerSync connector port (op-coalescing, using the now-ported `sync-policy` domain's retry classification), quarantine/dead-letter queue (wires `shouldQuarantine`/`backoffMs` into the real retry loop), auth (guest/OTP/Google, in-place upgrade, offline marker), repositories (read/write facades calling the already-ported pure domain functions for derived values), and repair logic (resolving the drift `reconcile`'s checksums surface). Order matters: schema parity blocks everything else. Phase 2's Done-when is a materially different bar than Phase 1's — these tasks touch real I/O (SQLite, network), so pure-function vector tests won't be sufficient; the gate is `docs/plans/full-test-plan.md`'s L3 (sync/data integration against a local Supabase project with fault-injection presets), which — like Gradle/Xcode before it — can't be run from this sandbox and needs a human or CI with a reachable test Supabase project.
- 2026-07-31 (latest of all, absolute newest) — **Mobile: additional cross-check verification pass on P1.6/P1.7's regex-heavy domains (guardrail, diagnostics, upi), per plan §5's "also port the TS tests not expressible as vectors."** No code changed — this was pure verification, done because the golden vectors for these three domains (10, 11, 29 vectors respectively) are a small fraction of each domain's real `*.test.ts` assertion surface, and a compiler can't catch a semantic regex-transcription error the way it catches a syntax error. Re-read all six P1.6/P1.7 `packages/core/*/src/*.test.ts` files (including `guardrail/src/fixtures.ts`, previously unread — 54 adversarial prompts across 5 categories + 15 benign prompts, used by a test requiring 100% correct block/allow classification) and reimplemented the EXACT regex patterns and logic written into `Guardrail.kt`/`Diagnostics.kt`/`Upi.kt` in Python, run side-by-side against ground truth from the real TS module executed via `node --experimental-strip-types`: guardrail's 13 rules against all 69 fixture prompts (0 mismatches on allow/category), diagnostics' `redactSecrets`/`redactText`/`redactDetail` against all 20 assertions in `diagnostics.test.ts` (currency-notation coverage, UUID preservation, bearer/JWT/API-key stripping, sensitive-vs-diagnostic key redaction, array capping, bounded recursion — 0 failures), and upi's `isValidVpa`/`maskVpa`/`formatAmount`/`buildIntentUrl`/`parseUpiTarget` against all ~45 assertions in `upi.test.ts` including the injection-attempt/duplicate-parameter/malformed-QR adversarial cases (0 failures). This doesn't substitute for a real Kotlin/Swift compile+test run — it can't catch a type error or a build-system issue — but it substantially de-risks exactly the failure mode a compiler is blind to. Also caught, while byte-scanning every new `.test.ts`/`.ts` file for the control-byte class of bug (not just `reconcile/index.ts`, already known): `diagnostics.test.ts` has one more hidden control byte — `assert.equal(out.includes("\x00"), false)` (checking `formatLog` never leaks a raw NUL into its plain-text output) renders in every tool as `out.includes(" ")`, a check against an ordinary space. No fix was needed (neither port's `formatLog` has any code path that could emit a NUL), but it's flagged here since it's easy to misread as an odd but harmless assertion rather than what it actually is.
- 2026-07-31 (latest of all, newest) — **Mobile Phase 1 domain porting is now CODE-COMPLETE: receipts (P1.5, commit 56a3200) + reconcile/upi/sync-policy/diagnostics/guardrail (P1.6) + entitlements (P1.7) all ported to Kotlin AND Swift, both platforms — all 250 vectors across all 16 domain files now have a registered implementation. NOT yet build-verified on either platform** (P1.5-P1.7 code has never been compiled; see "Native mobile" section / `docs/mobile/TODO.md` Blocked line for the next required step). Four notable findings this session, in order of how much damage a wrong guess would have done: (1) `packages/core/reconcile/src/index.ts`'s FNV-1a canonical-row serializer joins fields with the literal control character **U+0001 (SOH)** and uses **U+0000 (NUL)** as its null placeholder — both the Read tool and `grep` silently misrender these as `""` and `" "`, so a transcription from the rendered text produces a plausible-looking but completely wrong hash function. Caught by running the real TS module against the real golden vectors via `node --experimental-strip-types`, diffing hex checksums, and confirming the actual bytes via direct Python `open(path,'rb')` inspection — a hand-transcription using `""`/`" "` had produced `c781b6b5f092e470` against an expected `4586515961447bde`. A full scan of every other `packages/core/*.ts` file confirmed this is the ONLY file in the whole porting set with embedded control bytes. (2) `packages/core/upi/src/index.ts`'s `newPaymentRef` uses a mulberry32-style PRNG (via `export.ts`'s `seeded()` helper) whose FNV-ish offset constant, `1469598103934665603`, is NOT the textbook FNV-1a-64 offset basis (`14695981039346656037` — note the different digit count/magnitude); verified which constant is actually correct by testing both candidates in Python against the real `newPaymentRef` vectors (seed=42 → `"PCVQ4XFSJW5R"`, seed=1 → `"PCWAS98JVZP9"`) before writing a line of Kotlin/Swift — the textbook value produces a completely different (wrong) result. Ported the whole PRNG in unsigned 32-bit space (Kotlin `UInt`, Swift `UInt32`) since the algorithm only ever uses JS's `>>>` (never signed `>>`), sidestepping all sign/bitPattern conversion a naive Int32 port would need. (3) `packages/core/diagnostics/src/index.ts` (support-log redaction) is the highest cross-engine-risk domain after receipts: an order-sensitive multi-pass pipeline (strip secrets → stash UUIDs → protect SQLSTATE codes → scrub money-shaped numbers → restore stashes) where getting the PASS ORDER wrong silently redacts the wrong thing (e.g. a `23514` error code getting eaten by the long-integer-amount regex if the code-protection pass didn't run first). Every regex transcribed verbatim from the TS source and every one of the 11 vectors traced by hand character-by-character before writing any Kotlin/Swift, including the interaction between `redactSecrets`' email-vs-VPA regex ordering ("emails before VPAs — an email is also `x@y` shaped", per the source's own comment) and `redactText`'s code-protection placeholder scheme (`" CODE0 "`-style tokens with surrounding spaces, later restored by a second regex pass). (4) `sync-policy`'s `explainForUser` and `guardrail`'s `REFUSAL_MESSAGE` carry literal **U+2014 (em dash)** and, in one case, **U+2192 (rightwards arrow)** Unicode characters in user-facing strings — verified byte-exact via Python `ord(ch)` against the raw vector JSON before typing them into Kotlin/Swift source, rather than trusting that a copy-paste of a smart-looking dash preserved the exact codepoint. Also caught (not for the first time, but freshly relevant): JS's single-string `.replace(str, str)` replaces only the FIRST occurrence, while Kotlin's `String.replace(String, String)` replaces ALL by default — every replace in this session's new code either uses a `Regex`-based replace (which already matches JS's `/g`-flag semantics) or an explicit range/callback-based replace, never the naive single-string form, specifically to avoid re-triggering the exact bug P1.5's `extractQty` caught. `entitlements` was ported with one deliberate non-fix: `packages/types`' `Tier` type has four values (`free`/`lite`/`pro`/`premium`) but `canUse()` only special-cases the literal string `"premium"`, so `"lite"`/`"pro"` silently fall through to the free-feature check — documented as a likely-latent source-side gap rather than "corrected" during the port, since only `"free"`/`"premium"` have golden-vector coverage and porting tasks reproduce the source's actual behavior, not a guess at its intent. "Gate map" (also mentioned under P1.7 in the plan) was deliberately NOT ported: it has no golden vectors, is a UI-side feature-gating table rather than pure domain logic, and is deferred to Phase 3+ UI work. `docs/mobile/TODO.md`'s Handover block, Queue table (P1.5/P1.6/P1.7 rows), and Traps/notes section (new control-byte and replace-first-vs-all lessons) all updated in the same change set.
- 2026-07-31 (latest of all) — **Mobile P1.4: iOS build fully green (commit 6f320b9), Android already confirmed green.** Human confirmed Android via `./gradlew test` → BUILD SUCCESSFUL. iOS needed two more real Swift 6 build errors fixed post-port: (1) `27a91cb` — `SplitsInsights.swift`'s `averageSettleDays` originally shared one global `ISO8601DateFormatter`, which Swift 6 strict concurrency flagged as non-Sendable shared mutable state; fixed by allocating a fresh formatter per call inside `parseIsoMillis` instead of `nonisolated(unsafe)`, since this is production domain code with real concurrent-Task exposure and `ISO8601DateFormatter`'s thread-safety for concurrent reads isn't an Apple-documented guarantee — asserting `nonisolated(unsafe)` would have asserted an unverified safety claim, not made one true. Weighed against a fully hand-rolled, allocation-free epoch-day parser (Howard Hinnant's `days_from_civil`, in the same spirit as `Budget.swift`'s hand-rolled `Ymd` arithmetic) when asked whether the fix was "the most optimized way to handle that" — concluded the Foundation-based fix is the right tradeoff here specifically because this call site only *parses* an explicit-UTC ISO string into an absolute instant (no add/subtract, no calendar-carry logic), which is exactly the lower-risk category `Budget.swift`'s own header comment carves out from its Calendar-avoidance rule; a hand-rolled parser would trade a well-tested stdlib type for hand-written substring/integer arithmetic to save one small non-hot-path allocation per call, which is the wrong trade for a domain function, not a UI hot loop. (2) `6f320b9` — the newly-added `SplitsInsightsVectors.swift` test fixtures (`fixtureEdges`/`fixtureSettlements`, global `let`s of `[FriendEdge]`/`[FriendSettlement]`) hit a second, previously-unconfirmed Swift behavior: automatic `Sendable` synthesis for structs only applies within the SAME module, so public structs from `Domain` consumed from `DomainTests` (a different module, even via `@testable import`) are non-Sendable unless `Domain` itself declares conformance. Fixed proactively across the whole package rather than patching just the two flagged structs — added explicit `Sendable` to all 18 plain-data public structs in `Sources/Domain` (`Money`/`CurrencyMismatchError`/`MoneyError`, `LedgerEntry`/`AccountBalance`, `FinanceError`/`RecurringLike`/`SubscriptionImpact`/`CashflowInputs`/`YearProjection`/`AmortRow`, `Ymd`/`DateWindow`/`BudgetProgress`/`BillingCycle`, `Party`/`SplitEdge`, `DebtLike`/`PaymentLike`/`AverageSettleResult`/`FriendEdge`/`FriendSettlement`/`Contribution`/`FriendInsight`) since this exact error will otherwise recur for every future domain port (P1.5+) the first time its test file puts a Domain-package type in a global fixture. Deliberately left `FriendStats` (a genuinely mutable `final class`) non-Sendable — asserting Sendable there would be an incorrect safety claim, not a defensive one. Neither iOS fix is build-verified yet; next step is a human `swift test --package-path Domain` run.
- 2026-07-31 (later) — **Mobile P1.4 built: splits-insights+splits-math ported to Kotlin AND Swift (commit 17291cb).** Ported `packages/core/splits-insights/src/index.ts` (`averageSettleDays`, `computeFriendStats`, `pickFriendInsights`) and `apps/web/src/splits/math.ts` (`pairwiseEdges`), 10 new vectors. First domain whose exported vectors are plain JSON numbers throughout — no `amt()`/`mny()` money-string wrapping — confirmed by reading the generated `splits-insights.json`/`splits-math.json` directly rather than assumed from the earlier domains' convention. `averageSettleDays`' FIFO debt-aging needs real ISO date arithmetic across two input shapes ("2026-01-01" date-only and "2026-01-01T00:00:00Z" full-instant, both of which JS's `new Date(string)` treats as UTC): Kotlin routes date-only strings through `LocalDate.atStartOfDay(UTC)` and full ones through `Instant.parse()`; Swift uses `ISO8601DateFormatter`, which is safe here (unlike `Budget.swift`'s deliberate avoidance of `Calendar.date(byAdding:)`) because this is pure parsing of an explicit-UTC string into an absolute `Date` with no add/subtract arithmetic involved — a genuinely different risk profile, not a contradiction of the earlier caution. A second wrinkle: `pickFriendInsights`' first vector has a placeholder *input* (`{"stats": "computeFriendStats(edges,settlements) above"}` — a literal descriptive string, not data) because `export.ts` reuses the fixture from the vector immediately above it rather than re-serializing it; both adapters detect this (the `stats` field is a JSON string, not an array) and recompute `computeFriendStats()` from the identical hardcoded fixture, read directly off `export.ts`, not guessed. Also applied the "one initializer, not two overlapping ones" lesson from the Swift `Money.sum()` ambiguity bug proactively: Swift's `FriendStats` class has exactly one initializer (all fields defaulted except `friendId`) rather than a `friendId`-only convenience one alongside a full one, which would have been ambiguous at every `friendId`-only call site. Hand-verified `computeFriendStats`, `pickFriendInsights`, all three `averageSettleDays` vectors (including the January-has-31-days edge case and the "prepayment before any debt exists" case), and the multi-payer `pairwiseEdges` vector by tracing the ported algorithm against the exported expected values — all matched. Not yet build-verified.
- 2026-07-31 (earlier) — **First fully green native-mobile build on both platforms** — human ran `./gradlew test` (Android) and `swift test --package-path Domain` (iOS) and confirmed money/ledger/finance/budget all pass with `failed=0` on both. Getting there took 3 real fixes past "it compiled": (1) the human's first `xcodebuild test -scheme PocketCare` run only executed 1 test (the App-level placeholder) — the `Domain` SwiftPM package's own `DomainTests` (where `VectorRunnerTests` actually lives) isn't wired into that Xcode scheme at all, by design (`Domain/Package.swift`'s own comment: "fast to test, plain `swift test`, no simulator"); `README.md` already documented the right command, the confusion was just about which of the two to run. (2) Swift 6's strict concurrency checking (on by default at `swift-tools-version: 6.0`) rejected `FunctionRegistry`'s mutable `static var impls` as unsafe shared global state — a real error from the FIRST actual `swift test` execution of this pre-existing P0.4b file (it had only ever been compiled as part of a build before, never run). Fixed with `nonisolated(unsafe)` rather than `@MainActor` (commit 29a9d01) — the latter would have forced `async`/`await` onto every register/lookup call site across all four `*Vectors.swift` adapters for a cross-actor hop nothing here actually needs (every access is single-threaded and sequential within one XCTest method). (3) A real, surprising one: `finance[2] periodicRateFromAnnual` failed with textually IDENTICAL expected/actual values (`0.006666666666666667` on both sides) — `NSNumber.isEqual` returned `false` anyway, because the two NSNumbers came from different construction paths (`JSONSerialization`'s parse vs. the adapter's `NSNumber(value:)`) and isEqual isn't as reliably value-based as assumed (P1.3's own commit message had asserted "Swift's NSNumber-based isEqual already does this correctly by default" — that assumption is now known to be wrong, or at least not universal). Fixed (commit 77607ac) by replacing the `AnyObject.isEqual` bridge in `VectorRunnerTests.swift` with a hand-written recursive `jsonValueEqual` that compares `NSNumber`s by `.doubleValue` (with an explicit `CFGetTypeID`/`CFBooleanGetTypeID` check first, so a JSON `true` can never accidentally match a numeric `1`) — the same fix `Vectors.kt`'s `jsonElementsEqual` already needed on the Kotlin side during P1.3, this time proven necessary on Swift too rather than assumed unnecessary. Kotlin needed zero fixes past "compiled" this round. `docs/mobile/TODO.md`'s P0.4a/P0.4b/P1.1/P1.2/P1.3 rows all moved DOING → DONE.
- 2026-07-31 (earlier) — **Mobile P1.3 built: finance+budget ported to Kotlin AND Swift (commit e8ef8b8), continuing systematically per the human's earlier "Systematic" sequencing choice (no new prompt needed this time — "continue with other phases").** The largest port yet: `packages/core/finance/src/index.ts` (16 fns — futureValue, periodicRateFromAnnual, periodsToGoal, monthlyEquivalent, recurringMonthlyTotal, percentOfIncome, subscriptionImpact, projectCashflow, yearlyEquivalent, emiFromPrincipal, amortizationSchedule, timeframeTotal, emiDueDate, isDuePassed, effectivePaidEmis) and `packages/core/budget/src/index.ts` (4 fns — periodBounds, budgetProgress, crossedThreshold, billingCycle), 43 new vectors. Two things surfaced for the first time: (1) rounding — the TS source uses `Math.round` (ties toward +Infinity), a DIFFERENT rule from Money's round-half-away-from-zero; Kotlin's `Double.roundToLong()` matches by its own documented contract, Swift needed a verified `jsMathRound` helper (`floor(x+0.5)`, confirmed against MDN's own tie-breaking example via search) since no stdlib rounding mode implements this rule directly. (2) Infinity-valued results (`periodsToGoal`, `percentOfIncome`, `budgetProgress.pct` on a zero limit/income) — JS's `JSON.stringify(Infinity) === "null"`; Swift's adapters map `.infinite` to `NSNull` directly. Fixing this on the Kotlin side exposed and fixed a real latent harness bug: `VectorRunnerTest.kt` compared `JsonElement`s with raw `!=`, which is TEXTUAL equality for kotlinx.serialization, not value equality — harmless for the whole-number fields ported so far but wrong in general for genuinely fractional doubles like `periodicRateFromAnnual`'s `0.006666666666666667`, which would only match by coincidence if Kotlin's Double formatting happened to match V8's digit-for-digit. Added `Vectors.kt`'s `jsonElementsEqual()` (numeric primitives compared by parsed Double value; money's decimal-string amounts still compared as plain text) and switched the runner to use it — Swift's `NSNumber`-based `isEqual` already did this correctly, no Swift-side fix needed, a real (not assumed) platform asymmetry. The date math (`periodBounds`, `billingCycle`, `emiDueDate`) is a direct transliteration of the TS source's own `(year, month0, day)` integer arithmetic on both platforms rather than leaning on `java.time`'s `plusMonths()` or Foundation's `Calendar` — search surfaced real reports of Swift's `Calendar.date(byAdding:)` silently reverting to the device's local time zone unless every `Calendar`/`DateComponents` value involved is airtight about UTC, so `Budget.swift` hand-rolls a small `Ymd` type using Sakamoto's day-of-week algorithm and the standard Gregorian leap-year rule, both checked against multiple independent published references before use (same "don't trust the platform's automatic behavior" instinct as Money's `NumberFormatter` decision). `java.time.LocalDate` is still used on the Kotlin side, but only as a days-in-month oracle, never for month-add operations. With no compiler available in this sandbox, hand-traced the trickiest vectors (`periodsToGoal`, `effectivePaidEmis`'s `autoMark` case, `amortizationSchedule`'s first row, both `billingCycle` vectors, all four `periodBounds` vectors) against their exported expected values by hand — all matched. Also caught and fixed a real would-be Kotlin compile error before it happened: `futureValue(0, monthly, r, months)` inside `subscriptionImpact` needed `0L`, not `0` — Kotlin does not implicitly widen an Int literal to a Long parameter (confirmed via search, not assumed), the same class of mistake as the earlier Swift "Ambiguous use of 'money'" build error. None of P1.1/P1.2/P1.3 is build-verified yet.
- 2026-07-31 (later) — **Mobile P1.2 built: ledger ported to Kotlin AND Swift (commit 5a705de).** Chosen over fast-tracking a UI slice: asked the human "Systematic vs. fast-track a visual slice on mock data" after they asked when they'd see UI, and they picked Systematic — continue porting domains in the plan's dependency order (money done, ledger next since accounts/transactions need it). Ported from `packages/core/ledger/src/index.ts`, field names read directly off `tools/golden-vectors/export.ts`'s ledger section (wire uses `account_id`/`to_account_id`/`to_amount`, Kotlin/Swift properties use `accountId`/`toAccountId`/`toAmount`): `Ledger.kt` / `Ledger.swift`, all 4 functions (`signedEffectFor`, `deriveBalance`, `availableBalance`, `aggregateNetWorth`), registered via `LedgerVectors.kt` / new `LedgerVectors.swift` into each platform's `FunctionRegistry`, wired into `VectorRunnerTest.ledger()` / `VectorRunnerTests.testLedger()`. Balances are always derived by summing signed per-entry effects, never stored/mutated (golden rule #2) — `deriveBalance` folds `signedEffectFor` over every entry rather than keeping a running total anywhere. The Swift port proactively applied the lesson from the same session's earlier "Ambiguous use of 'money'" fix: explicit `Int64(0)` for the running total in `aggregateNetWorth`, and an explicit `if`/`else` instead of a `try`-inside-ternary for the blocked/unblocked balance choice, since neither could be compiler-checked in this sandbox. `ledger.json` has 10 vectors across the 4 functions, zero `throws` cases, so the exception-type-identity path isn't exercised by this domain. Grepped all 4 new files for the nested-block-comment trap before committing (none found). Not yet build-verified.
- 2026-07-31 (later) — **Real Xcode build error fixed: "Ambiguous use of 'money'" in `Money.swift`'s `sum()` (commit 6be76b9).** The human pasted a real `xcodebuild test` failure — `sum()`'s empty-list branch called `money(0, currency)`, where the bare integer literal `0` is ambiguous between the `money(Int64, String)` and `money(Double, String) throws` overloads (both conform to `ExpressibleByIntegerLiteral`, so Swift's overload resolution can't pick one). Fixed with an explicit `Int64(0)`; grepped the whole file to confirm it was the only ambiguous call site. This is a general Swift trap (any bare integer literal passed to an overloaded function differing only in `Int64` vs. `Double`), not specific to money — noted in the handover so later domain ports (ledger's `aggregateNetWorth` in particular) apply it proactively rather than rediscovering it.
- 2026-07-31 (latest of all) — **Mobile P1.1 built: money ported to Kotlin AND Swift — the first real domain logic on either native app.** Same session as P0.4 (built right after, on "move ahead" rather than waiting for a build report). Ported from `packages/core/money/src/index.ts`, checked against the actual TS source and `tools/golden-vectors/export.ts`'s exact field names (not guessed): `Money.kt` / `Money.swift`, 13 of 14 functions (`money`, `fromMajor`, `toMajor`, `minorUnits`, `add`, `subtract`, `negate`, `scale`, `sum`, `convert`, `split`, `itemsReconcile`, `isZero`/`isNegative`), registered via `MoneyVectors.kt` / `MoneyVectors.swift` into each platform's `FunctionRegistry`. `format()` deliberately deferred on both — it needs a 150+-entry currency-to-locale table and its exact string output depends on ICU/CLDR data that can differ between V8 (which exported the vectors) and each native platform's own ICU, a real cross-platform risk worth its own careful, build-verified pass rather than a same-turn guess; `money.json`'s 3 `format` vectors stay skipped. Money rounding (round-half-away-from-zero) was verified rather than assumed per plan §1's "never guess on money": Kotlin uses `BigDecimal.valueOf(Double).setScale(0, RoundingMode.HALF_UP)` specifically because `kotlin.math.round`'s tie-breaking rule isn't documented as the one wanted; Swift uses the standard library's explicit `.toNearestOrAwayFromZero` rounding rule, which needed no such workaround. Currency decimal-place lookup: Kotlin uses `java.util.Currency` (JDK-authoritative); Swift has no equivalently authoritative single API — search surfaced a report of `NumberFormatter`'s automatic per-currency fraction-digit behavior being inconsistent on older iOS, so Swift uses an explicit, search-verified ISO 4217 exception table instead of trusting that. While reviewing the P0.4 runner infra written earlier the same session, caught a real gap in both `VectorRunnerTest.kt` and `VectorRunnerTests.swift`: neither actually checked a thrown error's TYPE against a vector's `throws.name`, only its message — meaning `CurrencyMismatchError`'s entire reason for existing (being identifiably distinct from a generic `Error`) wasn't being verified. Fixed both runners to check identity when `throws.name` isn't the generic JS `"Error"`. Also discovered and documented a genuine platform difference: Kotlin's `JsonElement` equality is textual (a Double `500.0` won't match an exported JSON `500`), needing `Vectors.kt`'s new `jsonNumber()` helper; Swift's `NSNumber`-based comparison is value-based and needed no equivalent. None of this is build-verified yet — the next real test run on either platform checks all of it at once, including the money rounding edge cases.
- 2026-07-31 (even later) — **Mobile P0.3 DONE + P0.4a/P0.4b built (golden-vector runners, both platforms).** iOS caught up to Android: `xcodebuild test` went green on the human's machine after two real, previously-undiscoverable-in-a-sandbox build errors (`SWIFT_VERSION: "6.0"` should have been `"6"` — Xcode's Swift Language Version setting takes major-version-only values, not toolchain patch versions; `PocketCareTests` had no `Info.plist` and XcodeGen doesn't imply `GENERATE_INFOPLIST_FILE` for `bundle.unit-test` targets, unlike the app target's explicit `info:` block), and the human confirmed the app running on a simulator. `docs/mobile/TODO.md` P0.3 row moved DOING → DONE. Then built P0.4 (plan §4): `kotlin.test` runner in `apps/android/domain/src/test/.../vectors/` and `XCTest` runner in `apps/ios/Domain/Tests/DomainTests/`, both loading every file under `tools/golden-vectors/vectors/` and reporting per-domain pass/skip/fail counts, one test per domain file (16), all currently skipped via an empty `FunctionRegistry` that Phase 1 porting tasks populate to un-skip their vectors — keyed by `(domain, fn)` on both platforms since the vector JSON's bare `fn` field isn't unique across domains. Android needed `kotlinx.serialization` (1.11.0, confirmed compatible with Kotlin 2.4.10 via search) since `:domain` has no Android SDK and thus no built-in JSON parser — a real new-dependency decision, approved in-session per plan §0/§1 and logged in TODO.md's "Decisions needing a human" list. iOS needed no new dependency (Foundation's `JSONSerialization` is already available) but hit a real SwiftPM constraint: `resources:` can't reference paths outside the package, so the runner reads vector files directly off disk via a path computed from `#filePath` at runtime instead of bundling them — verified via search before writing the code, not guessed. Also caught, mid-write, a second instance of the exact nested-block-comment bug from the P0.2 round (Kotlin *and* Swift both nest `/* */` comments) — a doc comment in the new `VectorRunnerTest.kt` said `vectors/*.json`, fixed before it ever reached Gradle; the iOS files sidestep the whole bug class by using only `//`/`///` line comments. Neither runner is build-verified yet — that's the immediate next step, tracked in the handover block.
- 2026-07-31 (latest) — **Mobile P0.2 DONE.** `./gradlew build test` came back BUILD SUCCESSFUL on the human's machine (AGP 9.2.0, Gradle 9.4.1, built-in Kotlin, compileSdk/targetSdk 36) — first green native-mobile build of rev 3, after 3 real fix rounds in one session (see entries below). `docs/mobile/TODO.md` P0.2 row moved DOING → DONE; P0.4a (Android golden-vector runner, kotlin.test) is now unblocked (needed P0.1 + P0.2, both DONE). Added a "Run on a device" section to `apps/android/README.md` (USB debugging + `adb`/`installDebug`, Android Studio emulator path, wireless debugging) — the app is still just P0.2's placeholder Compose screen, real UI starts in Phase 3 after Phase 1/2 land. P0.3 (iOS) remains untouched and still blocked on ever attempting a first build (sandbox has no Xcode/Swift toolchain) — its own version audit (swift-tools-version, deploymentTarget) should happen before that first attempt, not after, per this session's Android lesson.
- 2026-07-31 (even later still) — **Mobile P0.2: migrated to AGP 9.2.0 + Gradle 9.4.1, built-in Kotlin.** Third real-build fix round in one session. The Kotlin Gradle plugin warned that Gradle 8.13 would soon be unsupported (wants >=8.14.4); rather than a minimal within-8.x bump, the owner asked for true-latest Gradle. Per Google's own versioning policy ("one major AGP version per Gradle major version"), real-latest Gradle (9.6.1) is only officially paired with AGP 9.x, not 8.13.0 — so this became a full AGP 9.2.0 migration (9.3 exists but developer.android.com's compatibility table hadn't published its minimum Gradle version yet, so the last fully-documented version was used instead of guessing). Applied the official built-in-Kotlin migration (developer.android.com/build/migrate-to-built-in-kotlin): removed the `kotlin-android` plugin from `libs.versions.toml`, root `build.gradle.kts`, and `app/build.gradle.kts` (AGP 9.0+ makes it a hard error to keep applying it); removed the explicit `kotlin.compilerOptions.jvmTarget` block since it now defaults from `compileOptions.targetCompatibility`. `:domain` (plain `kotlin.jvm` module, not Android) untouched — built-in Kotlin only replaces `kotlin-android`. Still not build-green; this is genuinely new territory for the repo, more real errors are plausible on the next run.
- 2026-07-31 (even later) — **Mobile P0.2 fixes: real Kotlin compile error + verified dependency version bump.** A human ran the actual build for the first time and hit two things the sandbox couldn't have caught. (1) `domain/.../Placeholder.kt` failed with "Unclosed comment" — **Kotlin block comments nest** (unlike Java/C), and the KDoc contained literal `/*` substrings inside glob-style paths (`packages/core/*;`, `vectors/*.json`), each silently opening an unterminated nested comment; fixed by rephrasing the comment to avoid embedded `/*` sequences (and dropped the em dash/section-sign chars while at it, cheap insurance against any encoding surprise). (2) Per the owner's standing instruction to verify dependency compatibility via search before trusting anything scaffolded from training-knowledge assumptions: audited `apps/android/gradle/libs.versions.toml` against developer.android.com's live AGP/Gradle compatibility table — confirmed AGP 8.6.0/Kotlin 2.0.21/Compose BOM 2024.10.01/compileSdk 35 were stale (current: AGP 9.2 line, Kotlin 2.4.10, Compose BOM 2026.06.00; Play Store requires new apps to target API 36 by 2026-08-31). Bumped to **AGP 8.13.0 + Gradle 8.13 (wrapper + CI) + Kotlin 2.4.10 + Compose BOM 2026.06.00 + compileSdk/targetSdk 36**, deliberately staying on the AGP 8.x line rather than jumping to 9.x's "built-in Kotlin" (a real DSL migration — drops the separate `kotlin-android` plugin — that can't be verified without a real build; noted as a future, separately-verified task). Also fixed `apps/android/.gitignore`: `/build` was anchored to the module root so it only ignored `apps/android/build`, missing `apps/android/domain/build` entirely (surfaced as an untracked dir after the human's local build) — changed to unanchored `build/` so every module's output is ignored.
- 2026-07-31 (later still) — **Mobile P0.2 + P0.3 scaffolded (Android + iOS skeletons) — written, NOT build-verified.** `apps/android/{app,domain}` (Gradle Kotlin DSL, Compose, `:domain` pure-Kotlin) and `apps/ios/{App,AppTests,Domain}` + `project.yml` (XcodeGen, SwiftPM `Domain` package, App Group `group.care.pocket`) both exist now, plus new `android`/`ios` CI jobs in `.github/workflows/ci.yml`. The agent sandbox has no JDK compiler, Gradle, Xcode, or Swift toolchain, so none of this has actually been compiled — every file was hand-checked for XML/YAML/TOML well-formedness and balanced braces, which is not the same as a passing build. Both tasks stay `DOING` in `docs/mobile/TODO.md` until a human runs the real build/test commands (listed in the Handover block and in each app's `README.md`) and reports the result. Android's Gradle wrapper jar isn't committed — can't fabricate a trustworthy binary in a sandbox — one bootstrap command (`gradle wrapper --gradle-version 8.6`) is needed once, documented in `apps/android/README.md`.
- 2026-07-31 (later) — **Mobile P0.1 done: golden-vector exporter.** `tools/golden-vectors/export.ts` runs a fixed corpus against the real `packages/core/*` + `apps/web/src/splits/math.ts` implementations and writes `vectors/<domain>.json` — 16 files, 250 vectors, at least one per public function across all 88 exported functions (money, finance, ledger, budget, upi, sync-policy, reconcile, guardrail, splits-insights, entitlements, diagnostics, receipts allocate/reconcile/parse/money-text incl. every fixture in `fixtures.ts`, and `pairwiseEdges`). Two runs verified byte-identical. These are now the law the native Android/iOS ports must reproduce (plan §5, §9). Next: P0.2/P0.3 (native skeletons — sandbox can't verify Gradle/Xcode output, needs CI or a human machine).
- 2026-07-31 — **Mobile rev 3 planning recreated and committed.** The rev-3 pure-native direction (two independent apps, no RN/KMP) had been decided in an earlier session but its docs sat uncommitted and were lost to a `git reset --hard origin/main` (done deliberately, at the owner's request, to discard the never-pushed rev-2 RN scaffold). Recreated `docs/plans/native-mobile-apps.md` (rev 3), `docs/mobile/TODO.md`, and this "Native mobile" section — this time committed immediately rather than left pending. No native code yet; next task is P0.1 (golden-vector exporter).
- 2026-07-29 — **EMIs charged to a credit card (0047).** A loan now names the account its EMI hits, asked when the loan is created, with "Not linked" a first-class option.
  - **CHARGED and PAID are separate events** — conflating them is what made the first attempt wrong. A due EMI is *charged*: `autoPost.ts` posts it onto the linked account on its due date, exactly as a bank adds the instalment to your statement, so it shows in the card's total due. It becomes *paid* only when the bill containing it is settled. `auto_mark_paid` no longer gates the charge (an instalment is owed whether or not you've said you paid it).
  - **Settling the card ASKS before marking EMIs paid.** `findCoveredEmis` walks due, unmarked EMIs on that card oldest-first and stops when the settled amount runs out; the user confirms. This is money state inferred from a payment amount — a partial payment must not silently clear an instalment.
  - **The link moved from localStorage to the DB.** It was per-device while it only pre-filled a dropdown; now it drives a figure on the Cards page, and the same card can't owe different amounts on your phone and your laptop. No FK, same reason as 0046: a loan row can outrun the account row it references and 23503 → quarantine.
  - `markEmisPaid` re-reads `emi_payments` before writing — it's a whole-column JSON blob, so a stale copy would drop another device's change.
  - **Deploy:** `supabase db push` + deploy the Sync Streams config.
- 2026-07-30 (rev 2) — **Mobile plan re-architected: KMP → React Native (Expo). PLANS ONLY.** Same-day supersession of the entry below, on review: every spec-bearing line of this repo is TypeScript, and RN runs `packages/core/*` (~300 tests), `packages/db` (AppSchema/connector/quarantine) and `packages/data` **unchanged** — so the KMP plan's riskiest workstream (LLM-executed logic porting against golden vectors) is deleted rather than mitigated. Golden vectors replaced by an **L1 Hermes spec run** (the same core suite executed under RN's engine; failures fixed by polyfill, never by forking core). Native features are preserved: widgets (Glance/WidgetKit) + iOS Live Activities + App Intents/QS tile are separate Swift/Kotlin extensions even in a fully-native app, so they remain identical small native modules behind Expo config plugins; notifications via FCM/APNs with the same single planned migration (`push_subscriptions.platform/token/live_activity_token`) + `notify-dispatch` fan-out. E2E split: **Maestro** on CI emulators for scripted journeys; **Firebase Test Lab** for Robo crawls + thin Espresso/XCUITest hardware smoke on the real-device matrix. Known frictions owned in the plan: single-React `pnpm.overrides` reconciliation (validated by running `next build` in Phase 0 CI), Hermes `Intl`, crypto polyfill (SEC-1 cross-platform round-trip mandatory, human-reviewed). Both docs updated in place: `docs/plans/native-mobile-apps.md` (rev 2), `docs/plans/full-test-plan.md`.
- 2026-07-30 — **PLANS ONLY (no code): native mobile apps + full test suite.** Two new plan docs, both written as checklist tasks for smaller LLM executors with per-task Done-when gates. (1) `docs/plans/native-mobile-apps.md` — **fully-native** Android (Kotlin/Compose) + iOS (SwiftUI) apps with widgets (Glance/WidgetKit), iOS Live Activities, and native FCM/APNs notifications. Architecture: **KMP shared core** (`packages/mobile-core`, PowerSync Kotlin MP SDK + supabase-kt) + native UI per platform; the TS `packages/core/*` stay as the executable spec via **golden test vectors** (`tools/golden-vectors/`) that every ported domain must reproduce exactly. Backend unchanged except one planned migration (`push_subscriptions.platform/token/live_activity_token`) + `notify-dispatch` fan-out. Widgets read a pre-masked snapshot, never the PowerSync DB. (2) `docs/plans/full-test-plan.md` — 7-layer test strategy (L0 TS spec → L1 golden vectors → L3 sync integration w/ fault-injection presets replaying the real incidents → L5 **Firebase Test Lab** device matrix, India-weighted, Robo + instrumented + XCUITest → L6 Playwright) and a ~100-case catalog by feature ID (ACC/TXN/SPL/UPI/RCP/SYN/PRIV/SEC/WID…) incl. regression suites for the three hide-amounts leaks, the 0040 partial-upload incident, and cross-platform crypto round-trip. Neither doc has been executed; Phase 0 of the mobile plan will add the "Native mobile" section + parity table here.
- 2026-07-29 — **Recurring groups (§3) — the UI redesign is complete.** `/recurring` is now three sections of expandable group cards (Subscriptions, Salary, SIPs…) instead of one flat list. Plan: `docs/plans/ui-redesign-2026-07.md` §3.
  - **Migration `0046_recurring_groups.sql`** + the four sync steps: `recurring_groups` table, `transaction_templates.group_id`, both mirrored in `AppSchema`, and a new `user_data` stream query. `transaction_templates` is already `SELECT *`, so the column syncs automatically — the table is the part that needed adding.
  - **`group_id` is NULLABLE with NO foreign key, deliberately.** Creating an item in a brand-new group writes two rows that PowerSync uploads in two SEPARATE transactions; a `NOT NULL` + `REFERENCES` row landing before its parent raises 23503 → 3 retries → quarantine, the exact head-of-line block 0040 caused and 0042 removed. The invariant is enforced client-side and made observable by `pocketcare.audit_ungrouped_recurring()`.
  - **Nothing may be ungrouped.** The modal can't save without a group; when a direction has none, the select is replaced by an inline create form. Items created outside the modal (the statement analyzer) go through `resolveGroupForImport`. Deleting a non-empty group *requires* a destination and moves the items in the same action — `deleteGroup()` throws without one so a future caller can't bypass it.
  - **Legacy items get one-time triage, not an "Ungrouped" bucket** — a strip above the sections with a suggested group pre-selected, gone permanently once cleared.
  - **Defaults reuse the existing `BUCKETS` taxonomy** from `src/cashflow/model.ts` (Household / Subscription / Loan-EMI / SIP / FD …) rather than inventing a second set of names for the same idea, which would have left Recurring and Planned Cashflow quietly disagreeing.
  - **Seeding is safe on two devices at once**: default ids are a deterministic UUID v5 of `(user_id, direction:slug)`, so both produce identical rows and the connector's array upsert no-ops instead of hitting a PK violation.
  - **Follow-ups, not done:** `planned_cashflow.bucket` still isn't unified with this table, and the `BUCKETS` glyph icons on Cashflow/dashboard are still Unicode.
  - **Deploy:** `supabase db push` **and deploy the Sync Streams config**.
- 2026-07-29 — **First-run walkthrough — written for the user who bounced.** A real 60+ user signed up, landed on the dashboard, didn't know what to do, and believed "add an account" meant **linking their real bank**. They stopped. Plan + full copy: `docs/plans/first-run-walkthrough.md`.
  - **`src/onboarding/Walkthrough.tsx` replaces `GettingStarted`** (deleted). The checklist already explained each step's *why*, but it **linked away** — so the explanation stayed on the dashboard while the user landed on `/accounts/new`, a form headed "New account" offering Savings/Current/Credit card/Demat. It reads as bank onboarding, and the fear returned exactly when the reassurance was gone. It was also a card among cards, and permanently dismissible.
  - **Three claims, early, in plain words:** not connected to your bank (no login, card number or OTP); nothing is tracked automatically; that's *on purpose*, to build the habit of noticing. **Manual entry is framed as the product, not an apology** — that reframing is the whole design.
  - **Two parts with a real exit.** Part A (1–4) sets up and ends in *Finish*; Part B (5–7 — Insights, Ask PocketCare, trial/plans) is opt-in. Making onboarding *longer* would be the wrong answer to "I found this overwhelming".
  - **Step 2 asks two questions** (name, rough balance) and defaults type/currency/colour/net-worth, writing through the existing `repos.accounts.create` + `setOpeningBalance`. The full form is the thing that scares people, so a first-timer never sees it. `/accounts/new` now carries a permanent `noBankLink` line for everyone else.
  - **Mounted in BOTH dashboard branches.** `app/page.tsx` returns early at `balances.length === 0` — precisely the new user's state — so mounting it only in the tiles branch would have made it unreachable for the person it was written for. Nearly shipped that.
  - **Absorbs the trial welcome modal.** `TrialNotice` has its own one-time "14-day trial is live" dialog; the walkthrough sets the same `trialWelcomeSeenKey` so they can't stack. Countdown banner stays. Step 7 leads with the trial rather than a price — everything is already unlocked, so a payment ask at minute one would also have been factually wrong. Guests get a create-an-account variant (no trial for guests). Plans render from `src/billing/plans.ts`, never hard-coded.
  - **Shared `Modal` gained real dialog semantics**: `role="dialog"`, `aria-modal`, Escape-to-close, focus trap, focus restoration. Keyboard users could previously tab out of *any* dialog into the dimmed page behind it. Benefits every dialog in the app.
  - i18n: 60 new `onboarding` keys + `accounts.noBankLink`, en/hi/nl, verified key-identical. **hi/nl need native review more than usual** — this copy exists to defuse a specific cultural fear, and "we are not connected to your bank" is the one sentence that can't land slightly wrong.
- 2026-07-29 — **Splits + Groups & trips merged into one screen; friend insights.**
  - **Two routes became one.** `/friends` already listed the group tiles, so the two pages were one screen's worth of information split in half. `/friends` is now the whole feature — Groups & trips → who owes whom → **Friends** → **Patterns** — and `/groups` is a redirect (kept so existing links, bookmarks and notification deep links don't break). `/groups/[id]` is unchanged. One nav entry, "Splits & groups". Group creation moved to `src/splits/NewGroupModal.tsx`, still on the `groups` i18n namespace so no copy was duplicated.
  - **Group cards show both directions.** A single net figure hid being owed by two people while owing a third; tiles now show owed-to-you and you-owe within that group.
  - **New "Friends" section** lists everyone you share a group with, *including* people you're settled with — the owes/owed lists drop them by construction, which made it a debt list rather than a directory.
  - **New core package `@pocketcare/splits-insights`** (added to `test:core`, **18 tests**): who covers the most, who owes the most, who *always* ends up owing or owed, and who settles fastest/slowest. Pure records in, rankings out; the app hook builds pairwise edges with the existing `pairwiseEdges` so the balance maths keeps exactly one implementation.
  - **Settle speed is FIFO age-of-debt**: payments clear the oldest debts first and each cleared chunk contributes `days(debt → payment)` **weighted by size** — clearing ₹5 instantly and ₹5,000 after six months is not "three months average". Never settling yields `null`, not `0` (it must not read as settling instantly), and a payment predating any debt is ignored rather than counted as negative age.
  - **Rankings refuse to assert what the data doesn't support**: 2 groups for an "always" claim, 2 cleared debts for a settle-speed claim, and two *different* people before fastest/slowest is stated at all. A thin ledger renders **no** Patterns section — a box reading "biggest lender: someone, ₹0" teaches people to distrust the whole page.
  - **⚠️ `pnpm-lock.yaml` could not be regenerated** (no pnpm in the agent sandbox). CI runs `--frozen-lockfile`, so **run `pnpm install` locally and amend** before pushing, or CI will fail on the new workspace package.
- 2026-07-29 — **UI redesign: one transaction tile, dashboard tiles that clip, insight cards for market data.**
  - **`src/ui/TransactionTile.tsx` is now the only transaction row in the app.** There were three competing implementations (`TransactionRow`, the Statements `TxnTile`, and a hand-rolled row in the dashboard's Recent tile); the Statements one was the best of them, so it was promoted and the other two deleted. Coloured merchant-initial avatar, merchant name extracted from the UPI/bank narration, and the **full narration wraps** underneath instead of truncating — a UPI string ellipsised at 40 chars is unreadable. Adopted on Statements, Transactions, Search, Recent activity (a `dense` variant) and the group detail page. Supports `avatarIcon` (people get a `person` glyph coloured by user id), `card`, `amountText`/`amountColor` and a `trailing` slot.
  - **Privacy bug: `/statements` leaked through hide-amounts.** It called `format()` directly rather than `useMoneyFmt()`, so the net figure, all three summaries and every row rendered real amounts with the toggle on. Same class of bug as the dashboard bar charts (below). Both fixed.
  - **Statements summary is one row per figure.** Income/Expenses/Transactions were three columns of a `1fr` grid — far too narrow for Indian-format amounts, so anything from a lakh up ellipsised to `₹1,2…`, cutting exactly the digits that matter.
  - **Dashboard tiles CLIP, never scroll, at every width.** Root cause was `.dash-tile-body > section { overflow-y: auto; overscroll-behavior: contain }` — `contain` explicitly disables scroll chaining, so a tile ate the wheel/swipe and refused to hand it back to the page. The mobile override that reverted this is gone: two behaviours meant the bug could return on one form factor, and the 860px breakpoint is not "mobile" (a landscape phone or any tablet took the desktop path). Content now fits instead: `src/dashboard/useFitRows.ts` (ResizeObserver → rows that fit) drives every list tile with a "+N more →" link, and charts flex via `.tile-flex` instead of forcing pixel heights. `HBarTile`'s `height={Math.max(180, data.length * 34)}` was the worst offender — 272px inside a `clamp(80px,10.5vh,118px)` row unit.
  - **Bar charts brought up to the line charts' standard**, incl. a second hide-amounts leak: `HBarTile` and `MonthCompareTile` rendered real figures straight through the privacy toggle. Shared `chartMoney()` + `chartTooltip()` now formats and masks every chart, so the two can't drift apart again. HBar gained a tooltip (it had none), currency-formatted labels and ellipsised category ticks; MonthCompare gained a grid, real axis, legend and value labels.
  - **Dividend + projection panels became insight cards** (`dividend_income`, `portfolio_projection`), both returning `[]` when the user holds nothing so non-investors don't get empty cards. The interactive panels moved to `/investments`, where the holdings are — a card is static, so converting them verbatim would have lost the growth/contribution/years controls.
  - **`/insights` is the card feed and nothing else** (full-bleed via `body[data-fullbleed]`), templates lost manual ordering (alphabetical now; the inert `sort` column stays), hairline list dividers became `.row-stack`/`.row-tile` on Groups and Help, and the nav uses a **self-hosted 8 KB Material Symbols subset** (`public/fonts/`, 54 glyphs, rendered by codepoint not ligature — see `apps/web/public/fonts/README.md` for why, and for how to regenerate).
  - **Still open:** the recurring-groups redesign (plan in `docs/plans/ui-redesign-2026-07.md` §3), the hairline-divider sweep across the other ~16 files, and unifying `planned_cashflow.bucket` with the new grouping taxonomy.
- 2026-07-28 — **Sync fault tolerance, layers 0 + 2 + 5** (after a data-loss incident; plan in `docs/plans/sync-fault-tolerance.md`).
  - **Root cause is architectural.** PowerSync's queue is strictly ordered, retried forever, with **no dead-letter path**. Right for transient failures, catastrophic for permanent ones: the op can never succeed, everything behind it is head-of-line blocked, and the only escape is deletion. Underneath that, one logical action (create group, create itemized expense) writes N ops that upload in N separate transactions with **no atomicity across the boundary** — any subset can land, and the remainder becomes permanently unsyncable.
  - **Layer 0 — recovery** (`src/sync/repair.ts` + Settings → "Check for unsynced data"). Key fact I originally got wrong: `insertRow` writes the ROW to local SQLite and PowerSync records the upload INSTRUCTION separately in `ps_crud`; discarding a queued op removes the instruction, **not the row**. Users who cleared a stuck queue still have their data. The panel diffs local ids against the server and re-uploads what's missing, parents first. Rows are **described, not counted** ("Fresh Mart · ₹1,122.50 · 12 Jun"), export is offered before any repair, upsert makes it re-runnable, and it uploads one row at a time so a failure identifies WHICH row. The old Discard now exports first.
  - **Layer 2 — new core package `@pocketcare/sync-policy`** (added to `test:core`, **21 tests**): `classifyFailure` (transient vs permanent), `shouldQuarantine`, `backoffMs`, `explainForUser`. **Defaults to transient** — retrying forever is the recoverable mistake; quarantining good data is not. 401 is transient (a token refresh fixes it); 408/429 are transient despite being 4xx; a known SQLSTATE beats the HTTP status because PostgREST reports several conditions as 400. Permanent failures still get 3 attempts, since a parent may be just behind in the same queue. `explainForUser` is tested to never leak a SQLSTATE to a user.
  - **Layer 5 — fault injection** (`setFaultInjection` in the connector + a dev-only Settings panel, gated on `NEXT_PUBLIC_ENABLE_FAULT_INJECTION` or localhost). Every bug in this area — the 0040 deferred constraint, the `useSyncExternalStore` render loop, a stuck-op detector that could never fire — was one reasoning missed and only execution would have caught, because these paths are unreachable in normal use. Now they're one tap away, with presets for each real incident.
  - **Still to do:** Layer 1 (aggregate writes via server RPCs — the root fix).
- 2026-07-28 — **Sync fault tolerance, layers 3 + 4: the dead-letter queue.** Shipped together on purpose — quarantining data where nobody can see it is the same loss with a better excuse.
  - **Layer 3 — quarantine instead of retry-forever** (`packages/db/src/quarantine.ts`, wired into `SupabaseConnector.uploadData`). A failure classified permanent by `@pocketcare/sync-policy` gets `MAX_PERMANENT_ATTEMPTS` tries, then its **full payload is copied to `failed_writes` and the op is deleted from `ps_crud`** — so the queue drains and every unrelated write stuck behind it goes through. Write-then-delete ordering: if the process dies between them the op is simply quarantined again, whereas the reverse order loses the write.
  - **Both new tables are `localOnly: true`.** A synced `failed_writes` would put its own rows into the queue it exists to unblock, and a failure to upload a quarantine record would be quarantined again. `sync_attempts` persists the retry counter because an in-memory one resets on reload, and a permanent failure that reloads before hitting the threshold retries forever — the exact block being fixed.
  - **No `ON CONFLICT` anywhere.** PowerSync exposes every table as a VIEW backed by INSTEAD OF triggers and SQLite cannot UPSERT a view; both writes read-then-branch or delete-then-insert instead.
  - **Layer 4 — "Problems syncing"** (`src/sync/deadletter.ts` + `ProblemsPanel.tsx`, plus a `SyncProblemsBanner` in `AppShell`). Written for a person: the row is named ("Fresh Mart · ₹1,122.50 · 12 Jun"), the cause is plain language via `explainForUser`, and the SQLSTATE is behind a "Technical details" disclosure. Retry goes direct via Supabase rather than re-queueing (re-queueing risks re-blocking the queue with the op we just freed it from) and runs **sequentially**, since a parent succeeding is often what makes a child's retry work. **Discard always downloads an export first** — no opt-out; the previous Discard deleted ops with no copy and no record, and that cost a user data. Resolved rows are marked, not deleted. The panel renders **nothing** when there's nothing wrong, because a permanent "problems" box teaches people to ignore it; the banner exists because this failure is otherwise silent — the app looks perfectly healthy while a few expenses sit in limbo.
  - **Verification:** web typecheck clean, 259 core tests passing. The end-to-end path is exercised via the Layer 5 fault injector, which now also shows whether the injected failure will quarantine.
- 2026-07-28 — **Automatic error reporting → `/admin/errors` (0044).** The diagnostics log only helped if a user filed a bug report, and most never do. Errors now report themselves: deduped by fingerprint (scope + message with ids/digits normalised out, so a retry loop is one row with `count: 412`), **sent directly via the `report_client_error` RPC rather than through PowerSync** — the failure most worth seeing is the sync queue being stuck, so routing the report through that queue would guarantee it never arrived. Rate limited per-fingerprint-per-session + 20/session client-side and 50/hour/user server-side. New `/admin/errors` page groups by fingerprint with an affected-users count (many users = regression; one user, huge count = retry loop) and a Resolve action that re-opens automatically on recurrence. Also **renders the attached device log on `/admin/feedback`** — 0043 added the column but nothing displayed it.
- 2026-07-28 — **On-device diagnostics: making mobile bugs reportable.** On a laptop you can ask someone to open the console; on a phone you can't, so a sync failure that prints a perfectly clear PostgREST error was invisible to everyone — the user says "syncing isn't working" and that's all anyone knows.
  - **New core package `@pocketcare/diagnostics`** (added to `test:core`, **20 tests**): log types + the redaction that makes a support log safe to send. Three passes, deliberately different: `redactSecrets` (credentials/contacts only, **never numbers** — a PostgREST code like `23514` is indistinguishable from an amount by shape and is the most diagnostic field there is), `redactText` (secrets **plus** money-shaped anything, for free-text messages where no key tells us what a number means), and `redactDetail` (key-based: sensitive keys replaced wholesale, free-text keys get the full treatment). UUIDs are stashed before every pass and restored after, so the number scrubber can't mangle the row id.
  - **Capture** (`apps/web/src/diagnostics/log.ts`): 150-entry ring buffer mirrored to localStorage (survives reload/crash), fed by wrapped `console.error`/`warn` (originals still called), `window.onerror`, `unhandledrejection`, online/offline, and **structured sync failures**. Entries carry the route.
  - **`packages/db` stays decoupled** — `setSyncDiagnosticSink` is a module-level hook the app opts into; the connector neither knows nor cares whether anything is listening. Structured emission matters because the console line is free text, where the code gets scrubbed.
  - **Two routes out**: Settings → **Diagnostics** panel (status, last-synced, upload-queue depth, error count, the raw sync error, expandable log, Share via the native share sheet) and **automatic attachment to bug reports** (`bug_reports.diagnostics`, migration `0043`, checkbox default-on) so the diagnosis lands in the admin console with zero user effort.
  - **Worked example** — the real error: `expense_items for <uuid> sum to 20000 but the expense total is 1258784` → both amounts become `[amount]`; table, op, row count, **code 23514** and the UUID all survive. Everything needed to diagnose, nothing about what was bought.
  - **Deliberate non-goals**: no automatic telemetry (nothing uploads unless shared or reported), no `console.log` capture (buries the signal), no unredacted mode. See `docs/features/diagnostics.md`.
- 2026-07-28 — **Fix: drop the `expense_items` sum trigger (0042) — it wedged the sync queue.** 0040 shipped a deferred constraint trigger enforcing `Σ expense_items.amount = expenses.amount`. That is not expressible against an incremental sync engine: PowerSync uploads the queue as separate HTTP requests, each its own Postgres transaction, so items arrive a few at a time and every transaction holds a PARTIAL set. `DEFERRABLE INITIALLY DEFERRED` defers only to the end of its own transaction, so the check fired on 1-of-N items (`sum to 20000 but the expense total is 1258784`) and retried forever. Integrity stays enforced **client-side** (reconcile gate + `allocateReceipt` assertion + a re-check in `createSplitExpenseItemized`, all tested); server-side it's now *observable* via `pocketcare.audit_expense_item_sums()`. Also reordered `createSplitExpenseItemized` to write all items then all shares — the connector coalesces only *consecutive* same-table ops, so interleaving turned a 40-line bill into ~40 round-trips. Rule added to CLAUDE.md.
- 2026-07-28 — **Pay friends via UPI (settle-up).** Settle a split balance by paying over UPI from inside the app. **No money touches PocketCare's accounts** — we build a UPI Intent deep link and the payer's own app moves it bank-to-bank.
  - **Not Razorpay, deliberately.** A Payment Aggregator settles to a *merchant* via escrow; routing a P2P settle-up through one makes PocketCare the merchant of record for money that isn't ours. UPI Intent needs no PSP, escrow, MDR or licence. ⚠️ UPI **Collect** was sunset 28 Feb 2026 (Intent is mandated) — we only push via Intent, but confirm the non-merchant P2P position with a lawyer/PSP before launch.
  - **New core package `@pocketcare/upi`** (added to `test:core`): `buildIntentUrl` (with our `tr=` reference), `isValidVpa`, `maskVpa`, `formatAmount` from minor units, `newPaymentRef`. **29 tests**, including that an injected note can't forge `am=`/`pa=`, that amounts never get thousands-grouped (UPI apps mis-read them), and that spaces encode as `%20` not `+`.
  - **Handles are server-only and NOT zero-trust — stated openly.** The existing scheme wraps a DEK with a passphrase-derived KEK owned by ONE user, so a co-member could never decrypt it; a shared payment handle must be readable by the payer, so it can't use that scheme. `payment_handles` therefore sits in no sync stream (like `push_subscriptions`), encrypted at rest via `pgp_sym_encrypt` under `PAYMENT_HANDLE_KEY`, released just-in-time by the new **`payment-handle`** edge function behind three gates — shared live group + existing financial activity + 20/hour rate limit — writing an audit row per disclosure. Without that gate the endpoint is a UPI-ID directory. `payment_handle_disclosures` *is* synced (no secret; it's the owner's audit trail).
  - **Two-sided confirmation, because there is no callback.** UPI Intent returns nothing to a web page — structural, not a gap. Payer taps paid → `status='pending'` and **their ledger leg posts immediately** (their cash really moved); the payee confirms → `confirmed` + their leg; "didn't arrive" → `disputed`, with **no reversal** (append-only ledger; the payer's transfer stands, it just stops settling the debt). Pending is counted optimistically in netting — excluding it shows the payer as still owing money they've sent, which drives duplicate payments. Nudge at 3 days, **auto-confirm at 14** via `sweep_pending_settlements()` called from `notify-dispatch`.
  - **Migration `0041_payment_handles.sql`**: `payment_handles`, `payment_handle_disclosures`, `settlements.{status,confirmed_at,confirmed_by,upi_ref}` (status defaults to `confirmed`, so every existing row and the manual mark-settled flow are unchanged), `notification_prefs.settlement_confirm`, a pending→payee notification trigger, and three service_role-only RPCs. **Guests blocked by a trigger on `auth.users.is_anonymous`** — not on `guest_sessions`, because guest→registered is an in-place UID upgrade that leaves that row behind.
  - **⚠️ `settlements` is queried in FIVE places** (four in `splits/hooks.ts`, one in `assistant/summary.ts`) — all now filter `status <> 'disputed'`. Missing one silently corrupts a balance.
  - **Deep links are never a dead end.** `upi://` handling varies by OS/browser and iOS/Safari can no-op **silently**, so a 1.5s visibility check detects a failed hand-off (a `visibilitychange` listener cancels it when the app really did open) and auto-reveals a manual fallback; a "Didn't open?" chip makes it reachable anyway. On mobile the fallback is **copy-first** (UPI ID + amount) since a QR on the screen you'd scan with is useless — the QR is still offered for *scan-from-gallery* or a second device. Desktop shows the QR immediately. QR lib is CDN-loaded only when the fallback appears.
  - **UI**: Pay-via-UPI on the settle sheet (INR + payer-side only; intent hand-off on mobile, CDN-loaded QR of the identical payload on desktop), `PendingSettlements` confirm/dispute card at the top of `/friends` surfacing the `tr=` reference for statement matching, and a Settings panel showing only the masked hint plus a who's-seen-this log. New `payments` i18n namespace (en/hi/nl, 35 keys, key-identical).
  - **We do NOT verify VPA ownership** — that needs a penny-drop through a PSP, i.e. the regulated path this design avoids. The payer's own UPI app resolves and shows the real account name before they confirm; that is the verification.
  - **Deploy:** `supabase db push` → redeploy `sync-streams.yaml` → `supabase secrets set PAYMENT_HANDLE_KEY=…` (**losing this key makes every stored handle unreadable — back it up**) → `supabase functions deploy payment-handle notify-dispatch`. Web typecheck clean; core tests 218 passing. **hi/nl need native review.** See `docs/features/upi-settle-up.md`.
- 2026-07-27 — **Receipt & bill scanning + itemized splitting.** Photograph a restaurant bill or grocery receipt → structured line items (qty, unit price, tax, service charge, tip, discount) → a transaction, or a **per-item split** across a group.
  - **New core package `@pocketcare/receipts`** (`packages/core/receipts`, added to `test:core`): `types.ts` (money in minor units, quantities in MILLI-units so 0.5 kg is exact), `allocate.ts` (`splitByWeights` largest-remainder + `allocateItem`/`allocateProportional`/`allocateReceipt`), `reconcile.ts`, `money-text.ts` (locale-agnostic money/date/unit parsing), `parse.ts` (OCR → `ReceiptDraft`), `fixtures.ts`. **88 tests.** `apps/web/src/splits/math.ts` now re-exports `splitByWeights` from here, so there is exactly ONE allocation implementation.
  - **Reconciliation is the quality gate.** A draft only passes when `Σ lines === printed total`, exactly. OCR confidence alone is not trusted (engines are confidently wrong). Failing loudly beats a silently wrong ledger entry; review won't save until it balances, fixable in one tap either way (add the difference as a line / adopt the computed sum).
  - **Hybrid OCR.** On-device tesseract.js (lazy-loaded from CDN like pdf.js in `parsePdf.ts`) with **word bounding boxes** — lines are rebuilt from box geometry so multi-column grocery layouts survive. PDFs with a text layer skip OCR entirely (most accurate path). Opt-in **`receipt-scan` edge function** (clone of `assistant`'s auth/quota/CORS shape) calls a vision model with `tool_choice`-forced `emit_receipt`; shares the assistant's quota pool. **No receipt image is ever stored** — held in memory, dropped after the scan; `receipt_scans.image_path` is a forward-compatible seam, always NULL.
  - **Migration `0040_receipts_and_expense_items.sql`**: `receipt_scans` (owner RLS), `expense_items` + `expense_item_shares` (group-membership RLS via `is_group_member`), `expenses.has_items`, and a **deferred constraint trigger** enforcing `Σ expense_items.amount = expenses.amount`. `expense_item_shares.user_id` **cascades** — unlike the 0011 tables that 0031 had to retrofit a deletion cleanup for.
  - **Itemized splitting is additive, not a second balance model.** Per-item shares roll up into `expense_participants`, so `hooks.ts`, `math.ts`, `collapse.ts`, settle-up and the friends graph needed **zero changes**. Per-line modes: equal / quantity / percent / exact, plus **proportional** for charges (default: tax & service allocated by each person's item subtotal; overridable, because "the service charge was for the table" is just as common). DB constraint blocks `proportional` on item lines.
  - **UI**: dashboard FAB became a **speed dial** (`src/ui/AddSpeedDial.tsx` — + rotates to ×, "Add transaction" and "Scan bill / receipt" stack above it; scrim, focus trap, arrow-key menu nav, `prefers-reduced-motion`, keeps the `data-dash-edit`/`data-dash-empty` hide rules). New `/receipts/{new,review,split}`; draft persisted to `receipt_scans` so a refresh doesn't lose work. `ItemBreakdown` expands in place on the group detail page; "Scanned" chip on the transactions list. New `receipts` i18n namespace (en/hi/nl, 96 keys each, key-identical) + `fab.*` in `translation`.
  - **Deploy:** `supabase db push` → **redeploy `sync-streams.yaml` in the PowerSync dashboard** (`receipt_scans` in `user_data`; `expense_items`/`expense_item_shares` in **`split_shared`** — every member must see every line) → `supabase functions deploy receipt-scan` (`ANTHROPIC_API_KEY`, optional `RECEIPT_MODEL`). Web typecheck clean; core tests 137 passing. **hi/nl need native review.** See `docs/features/receipt-scanning.md`.
- 2026-07-23 — **Offline-first auth + persistent offline banner.** Root cause of "logged out in a tunnel": Supabase access tokens are short-lived, and when one expired while offline the client couldn't refresh, so `getSession()`/`onAuthStateChange` reported *no session* and the auth gate bounced the user to onboarding. Fixed in `useAuthStatus` (`account.ts`): a tiny persisted `pc_auth` marker records who's logged in on every real session; the hook now seeds its initial state from that marker synchronously (no logged-out flash on reload) and, **while `navigator.onLine === false`, refuses to downgrade to "none"** — trusting the local session. Only a genuine online `SIGNED_OUT` clears the marker (and explicit `signOut()` clears it up front). The app already runs on local SQLite offline; the JWT only gates network sync, which refreshes on reconnect. Added a sticky `OfflineBanner` in `AppShell` ("You're offline — changes are saved on this device and will sync when you're back online.") driven by the `online`/`offline` events, shown on both the app shell and bare routes. Typecheck clean; no migration.
- 2026-07-23 — **Resume group invite after sign-up/login.** A logged-out user opening `/join?token=…` previously lost the token when they went to sign in — they landed on the dashboard, unjoined. Now the join page stashes the token in `localStorage.pendingInvite` (survives the Google OAuth full-page redirect), and a new AppShell effect, the moment a session exists (email/Google/guest), redirects to `/join?token=…` to finish joining and land on the group. The token is cleared on the first consume attempt so a stale/expired one can't cause a redirect loop. `join/page.tsx` + `AppShell.tsx`; typecheck clean; no migration.
- 2026-07-23 — **Split covered-portion books as an expense (not a transfer); settlement → "record it" notification.** (1) In `projectPersonal` (`splits/write.ts`) the `overpay`/`lend` leg — the amount you covered for others — is now an **expense on your account** instead of a transfer to the hidden "Owed to me" account. Net worth over the settle-up lifecycle is unchanged (repayment lands as an inflow) and who-owes-whom still derives from `expense_participants`; the win is that a split now reads as one clean expense end-to-end (no stray transfer leg). (2) New migration `0039_settlement_notifications.sql`: an `AFTER INSERT ON settlements` trigger notifies the *counterparty* (the party who didn't record it) with a deep link `/transactions/new?type=income|expense&amount=…&desc=…` — income if they received, expense if they paid. The new-transaction page now prefills `type`/`amount`/`description` from those query params (gated on the `group_expense` pref). Web typecheck clean.
- 2026-07-23 — **Collapse split expenses to a single tile + in-detail breakdown; notification test button.** A split expense writes up to 3 private ledger rows (`own_share`, `lend`, `borrow`) which are kept for balance correctness but previously rendered as 2–3 separate list rows. New `src/splits/collapse.ts` (`useSplitInfo` + `collapseSplitRows`) collapses every posting of one expense into a single tile across **all** lists (Transactions, Search, dashboard Recent), badged "Split" via the new `SplitChip`, showing the **total you paid** (own_share+lend; falls back to your share if you paid nothing) with "your share" in the subtitle. The transaction detail/edit page gained a `SplitBanner` showing total bill / your share / you paid / owed-or-owe + participants and an "Open group" link — so the breakdown lives in the detail, not the list. Ledger + balances untouched (display-only). Also added a **"Send test notification"** button in `NotificationPanel` (fires a local `showNotification` via the SW) to isolate browser/permission issues from server dispatch — note that real OS notifications only fire once `notify-dispatch` is deployed/scheduled with `VAPID_PRIVATE_KEY` set. Web typecheck clean; no migration.
- 2026-07-23 — **Group/trip notifications + per-feature notification preferences.** Migration `0038_group_notifications.sql` adds two `SECURITY DEFINER` triggers that fan out inbox rows to *other* members when the synced row lands in Postgres: `group_invite` (`AFTER INSERT ON split_group_members` — tells existing members "X joined" and the joiner "You joined"; silent for solo group creation) and `group_expense` (`AFTER INSERT ON expenses` — tells every member except the payer). Both respect per-recipient prefs and dedupe on `(user_id, dedupe_key)`. New prefs `group_invite` + `group_expense` (default on) added to `notification_prefs` (+ AppSchema, hooks, and a "Groups & trips" section in `NotificationPanel`). Delivery unified via a new `notifications.pushed_at` flag: trigger- and cron-created rows both start unpushed, and each `notify-dispatch` run pushes every recent unread/un-pushed row and stamps `pushed_at` — so event-driven group rows get Web Push on the next tick without the dispatcher knowing how they were created. Web + db typecheck clean; needs `supabase db push` + PowerSync sync-rule redeploy (prefs columns) + `notify-dispatch` redeploy.
- 2026-07-23 — **Notifications (in-app + Web Push), bulk statement import, smarter categorization, auto-categorize job, split-invite fix.** Five threads shipped together:
  - **Sync upload batching** — `packages/db/src/connector.ts` `uploadData` now coalesces *consecutive* CRUD ops of the same table+op into one PostgREST request (array `upsert`, `id IN (...)` delete); PATCH stays per-row. A 200-row statement import went from 200+ HTTP round-trips to a handful, order-preserving (no cross-table reordering, so FK order like account-before-txn holds). Speeds up *all* syncs.
  - **Bulk statement import** — new `importTransactionsBulk` (`apps/web/src/data/importCsv.ts`): preloads account/category maps once, inserts every row inside ONE `writeTransaction`, skips the per-row overdraft guard (statement rows are historical). Replaces the old per-row `importTransactions` (which opened a writeTransaction + re-read the whole ledger per row, O(n²)). Wired into `statements/analyze`.
  - **Categorization overhaul** — `categorize/normalize.ts` now strips UPI/IMPS/NEFT/POS/bank/VPA noise + reference numbers and extracts a merchant blob; `engine.ts` adds substring merchant matching (so `swiggylimited`/`amazonin` resolve) and a preloaded `buildClassifier` for bulk work (loads rules+seeds once, classifies N rows in memory); `seeds.ts` gained ~90 Indian merchants + `buildSeedList`. Analyze page uses the bulk classifier.
  - **Auto-categorize job** — `categorize/autoJob.ts` scans `category_id IS NULL` expenses, classifies with the preloaded engine, previews the count, and applies all matches in one batched `writeTransaction`. UI card on `settings/categories`.
  - **Notifications** — migration `0037_notifications.sql`: `notifications` + `notification_prefs` (synced, added to AppSchema + `sync-streams.yaml` `user_data`) and `push_subscriptions` (server-only, not synced). Client: `notifications/push.ts` (VAPID subscribe → save endpoint), `hooks.ts` (inbox/unread/prefs), `NotificationPanel.tsx` (settings), `/notifications` inbox page, bell+badge in `AppShell`, and `push`/`notificationclick` handlers in `public/sw.js`. Server: `notify-dispatch` edge function computes four triggers (EMI/bill due, budget 80/100%, low balance, spend outliers), dedupe-upserts inbox rows, and sends Web Push via `npm:web-push`. Needs `NEXT_PUBLIC_VAPID_PUBLIC_KEY` (client) + `VAPID_*` secrets (function) and an hourly cron. See `docs/features/notifications.md`.
  - **Split-invite fix** — reopening an already-accepted invite returned 410 for the accepter too; `split-invite-accept` is now idempotent (already-a-member/`accepted_by` → return `group_id`). Client `acceptInvite` unwraps the real edge-function error from `FunctionsHttpError.context` instead of the opaque "non-2xx" string. Redeploy: `supabase functions deploy split-invite-accept --no-verify-jwt`.
- 2026-07-22 — **Dashboard/statement overflow + empty-state FAB fixes.** (1) **No horizontal spill on tiles:** `TileCard` header now ellipsises its title (`min-width:0` + nowrap) with a `flex-shrink:0` action, and the section is `min-width:0; max-width:100%; overflow-x:hidden`; `.dash-tile-body > section` switched from `overflow:auto`/`visible` to `overflow-y` + `overflow-x:hidden` (desktop and mobile) — fixes the Upcoming-payments tile getting cut / scrolling sideways. (2) **Statement outliers overflow:** the outlier row (a grid item, so `min-width:auto` by default) got `min-width:0` so long `BIL/INFT/…` descriptions ellipsise instead of spilling past the card. (3) **Empty-dashboard FAB:** the floating "Add transaction" button is now hidden when the dashboard has no accounts — new `body[data-dash-empty="true"] .add-fab { display:none }` rule + a `dashEmpty` body-dataset effect in `app/page.tsx` (mirrors the existing edit-mode hide). Web typecheck clean; no migration.
- 2026-07-22 — **Fix getting-started checklist flash on the dashboard.** The `GettingStarted` card mounted, computed step-completion from three `useQuery` COUNT(*) results that start *empty* (doneCount 0) on every navigation, rendered as "incomplete", then vanished once the real counts arrived — a one-frame flash for users who'd finished onboarding. Now it only renders once it has a definitive answer: it gates on each query's `isLoading` (never judges on the momentary empty result) and persists a `gettingStartedComplete` localStorage flag the instant all steps are done, so returning users hit `return null` synchronously before any query runs. Net effect: the card is either shown or not — no render-then-hide. `src/onboarding/GettingStarted.tsx` only; web typecheck clean; no migration.
- 2026-07-22 — **Custom radios & checkboxes (global).** Restyled all native `input[type="radio"]` and `input[type="checkbox"]` in `globals.css` via `appearance: none`: 18px controls with `--surface` bg + `--border-strong` outline unchecked, and **accent (orange) fill with a white indicator when checked** — a white checkmark (clip-path) for checkboxes (rounded-corner square, 6px radius) and a white centre dot for radios (circle). Added `:checked` scale-in animation, `:focus-visible` ring, and `:disabled` dimming. Applies everywhere automatically (settings, accounts, assistant, splits, loans, cashflow, etc.); `input[type="color"]` is unaffected. CSS-only; no migration.
- 2026-07-22 — **i18n rollout Batch 4e: onboarding + assistant (en/hi/nl) — user-facing i18n rollout COMPLETE.** New `onboarding` namespace (all 7 walkthrough slides' title+body fully translated, plus Next/Skip/Create-account/Sign-in/Try-as-guest, footer, install) — slide visual identity (glyph/gradient) stays in code. New `assistant` namespace covering the whole Ask-PocketCare UI: the localised **greeting** bubble and **suggestion chips** (`t("suggestions", {returnObjects:true})`), premium gate, landing (quota chip, intro, quota-reset, start/continue, delete-chat dialog), privacy modal, chat header, view-data toggle, thinking, out-of-quota (paid credit packs + free upgrade), confirm-action, composer placeholder/send, and `friendly()` error mapper (now takes `TFunction<"assistant">`). **Intentionally English (model-facing / flagged):** the assistant `PERSONA` system prompt (LLM instructions), `describeToolCall`/tool text in `src/assistant/tools.ts`, and the help FAQ Q&A bodies. Web typecheck clean; no migration. **Rollout status: 21 feature namespaces across en/hi/nl; all route pages except admin are localised (admin kept English per request). hi/nl still need a native-speaker review before release; consider a TMS + `pnpm i18n:extract` for ongoing maintenance.**
- 2026-07-22 — **i18n rollout Batch 4d: statements/analyze (en/hi/nl).** New `statementsAnalyze` namespace covering the on-device statement analyzer end-to-end: the upload screen (intro + on-device note, bank/card type, account picker, choose-file, PDF-password prompt, tip), busy/parse/categorise states, and the full Results view — summary stats (money in/out/net/closing), credit-card due block, chart eyebrows (where it went / daily spend / no-spends), outliers, recurring detection (with cadence map + "seen N×"), reconciliation counts + import-missing button, and the collapsible transaction list (show all/less). Web typecheck clean; no migration. **hi/nl need native review.** Batch 4 remaining: onboarding, assistant (both long-form — chrome translated, persona/marketing prose flagged).
- 2026-07-22 — **i18n rollout Batch 4c: settings (main) + statements (en/hi/nl).** New `settings` namespace for the main Settings page (account/guest banner with plural days-left, sync status, display name, theme, privacy/hide-amounts, base currency, language, categories/labels + import/export links, help & support, sign-out + delete-account modals with all warnings). New `statements` namespace (premium gate, header actions, date labels, summary tile — net/income/expenses/transactions, empty state, and Today/Yesterday day-group labels via `groupByDay(rows, t)`). **Deferred (English):** the `Billing`, `SecurityPanel`, and `ProfileTraits` sub-components embedded in Settings are shared components with their own copy — left English for a later pass. Web typecheck clean; no migration. **hi/nl need native review.** Batch 4 remaining: statements/analyze, onboarding, assistant.
- 2026-07-22 — **i18n rollout Batch 4b (part 2): login (en/hi/nl).** New `login` namespace covering the full auth screen — register / sign-in / reset modes, the OTP-verify and set-new-password steps, all validation + status messages, the Google buttons, mode-switch links, and the encryption-passphrase note. The module-level `friendly()` Supabase-error mapper now takes the `t` function (typed `TFunction<"login">`) so its friendly error copy is localised too. Web typecheck clean; no migration. **Admin pages intentionally kept English (Batch 5 dropped per request).** **hi/nl need native review.** Batch 4 remaining: settings (main), statements, statements/analyze, onboarding, assistant.
- 2026-07-22 — **i18n rollout Batch 4b (part 1): groups + join + help-chrome (en/hi/nl).** New `groups` namespace (groups list — title/friends/new, empty state, tiles, new-group modal with kind chips/dates/auto-split; + group detail — header stats, auto-split banner, members with owe/owed/settled, expenses list, edit + invite modals, copy-link), `join` namespace (invite landing states), and `help` namespace for the **FAQ page chrome** (title, subtitle, search, no-match, footer). **Deferred (English, flagged):** the help FAQ's ~40 long-form Q&A entries in `SECTIONS` stay English — they're knowledge-base prose best localized by a translator/TMS, not machine-translated in bulk. Web typecheck clean; no migration. **hi/nl need native review.** Batch 4 remaining: **login**, settings (main), statements, statements/analyze, onboarding, assistant.
- 2026-07-22 — **i18n rollout Batch 4a: categories + labels + search + data (en/hi/nl).** Four small namespaces: `categories` (manage-categories page — tree, add/edit/delete, kind map), `labels` (manage-labels — search/add/edit/delete), `search` (query box, filters, type chips, plural result count, empty state), `data` (import/export — export button + messages, import format/file/skip-dup, preview table headers + plural counts, result line, footer note). Registered all four; renamed the search `TYPES.map`/`results.map` params off `t`. Adapter labels + previewed row `type` stay English (data). Web typecheck clean; no migration. **hi/nl need native review.** Batch 4 remaining: settings (main), statements, statements/analyze, login, onboarding, join, help, assistant, groups (+ [id]).
- 2026-07-22 — **i18n rollout Batch 3e: investments + insights (en/hi/nl) — Batch 3 complete.** New `investments` namespace covering the page (grand-total stats, EOD/sync note, empty state, group tiles with plural holdings, drill-in, holding tiles incl. LTP/value/matures/p.a. subtitle pieces, edit-holding form) and the **AddInvestmentDialog** (type picker, in-list/not-listed, name/amount/qty, FD rate+maturity, SIP amount/frequency/date/debit-account + note, existing-vs-new funding, over-funds warning). New small `insights` namespace (premium gate copy + statements link). Registered both. **Deferred (English, data layer):** `src/investments/model.ts` labels (asset-class names, `classMeta`/`holdingLabel` unit words, `fyLabel`), matching the cashflow model deferral. Web typecheck clean; no migration. **hi/nl need native review.** **Batch 3 (planning) DONE** — budgets, goals, templates, recurring, cashflow, loans, investments, insights all namespaced (subscriptions is a redirect). Rollout: 1 accounts ✓ · 2 transactions/cards ✓ · 3 planning ✓ → 4 settings/data → 5 admin.
- 2026-07-22 — **i18n rollout Batch 3d: loans list + detail (en/hi/nl).** New `loans` namespace covering the loans list (total-EMI header, plural loan count, Active/Closed pills, per-loan range/rate/EMI-paid + Loan/EMI amount footers, empty state) and the **Add-loan modal** (fixed/variable, principal/tenure/rate/EMI with auto-calc hint + variable note, start/due-day, auto-mark toggle). The **detail page** is fully wired: summary cards, next-due/remaining/paid/total-interest stats, the auto-mark policy strip, the variable-rate month-by-month EMI list, the fixed-rate amortization schedule, EMI status pills (Auto/Paid/Mark paid), the mark-paid dialog (paid-on + optional expense posting) and the edit form. Ordinals: each EMI/ due-day string is passed both `ord` (English "1st") and `n` (number); en uses `{{ord}}`, hi/nl use `{{n}}`. Persisted ledger descriptions (`EMI #n — lender`) stay English. Web typecheck clean; no migration. **hi/nl need native review.** Batch 3 remaining: investments, insights.
- 2026-07-22 — **i18n rollout Batch 3c: cashflow + RecurringModal (en/hi/nl).** New `cashflow` namespace covering the Planned Cashflow hub (header/intro, timeframe hero stats, donut/ratio chart labels, quick-add note, the three sections + empties, financial-summary/projection copy, and every row type — planned/recurring/subscription/loan with their subtitles, per-month, next/auto-post, and kebab remove dialogs, portfolio summary with plural holdings) plus the shared **RecurringModal** (`src/cashflow/RecurringModal.tsx`: direction titles, deposit/fund/pay labels, investment/category selects, frequency, first-due, auto-post toggle). Registered. Renamed `TIMEFRAMES`/`TEMPLATES` map params off `t`. **Deferred (English for now):** model-provided labels in `src/cashflow/model.ts` (timeframe segment labels, quick-add template labels, `bucketLabel`) — they live in the model, not the page. Web typecheck clean; no migration. **hi/nl need native review.** Batch 3 remaining: loans (list + detail), investments, insights.
- 2026-07-22 — **i18n rollout Batch 3b: templates + recurring (en/hi/nl).** New `templates` namespace (list, free-limit notice, intro, reorder, use/edit/delete, create/edit modal, upgrade modal) and `recurring` namespace (header, due-now confirm strip, Incomes/Payments/Savings sections + empties, per-row freq/next/mode + post/edit/remove). Registered both; fixed `t`-shadow in templates list (`templates.map` param → `tpl`). `subscriptions` page is a redirect (no strings). The shared **RecurringModal** (in `src/cashflow/`) is deferred to the cashflow sub-group. Web typecheck clean; no migration. **hi/nl need native review.** Batch 3 remaining: cashflow (+ RecurringModal), loans (list + detail), investments, insights.
- 2026-07-22 — **i18n rollout Batch 3a: budgets + goals (en/hi/nl).** First sub-group of the planning batch. New `budgets` namespace (list, empty state, new-budget modal — limit/threshold/categories/labels/timeframe + period map, per-row edit + delete + spent/over/left) and `goals` namespace (list, EF banner, new-goal form, GoalCard edit, funded/locked states, allocate/block modal). Both registered. Web typecheck clean; no migration. **hi/nl need native review.** Batch 3 remaining: cashflow, recurring, subscriptions, loans (list + detail), investments, insights, templates.
- 2026-07-22 — **i18n rollout Batch 2: transactions + cards domains (en/hi/nl).** New `transactions` namespace (covers the **transactions list, new-transaction, and edit-transaction** pages — including the full split editor: modes, participants, exact/percent/paid validation lines, multi-payer, your-share summary, template save/limit, and the edit-history audit field labels) and a `cards` namespace (**cards wallet** page + per-card panel: cycle/limit/due, edit-details form, settle-bill). Both registered in `resources`/`NAMESPACES`. Fixed `t`-shadowing in the list (`TYPES.map`/`rows.map` params renamed) and moved template-name guessing to translated fallbacks. Persisted data (item "Item N" fallback descriptions, ledger notes) stays English by design. Web typecheck clean; no migration. **hi/nl need native review.** (Rollout: 1 accounts ✓ → 2 transactions/cards ✓ → 3 planning → 4 settings/data → 5 admin.)
- 2026-07-22 — **i18n rollout Batch 1: accounts domain (en/hi/nl).** New `accounts` namespace (`locales/accounts/{en,hi,nl}.json`, registered in `resources`/`NAMESPACES`) covering the full **accounts list, new-account, and edit-account** pages. All three now use `useTranslation("accounts")` — titles, archived toggles, the multi-currency card, account-type labels (`type.*` map for savings/current/credit_card/cash/mutual_funds/stocks/demat), the new-account form (type/currency/colour/net-worth/overdraft, credit-card cycle preview, demat/opening-balance), and the edit page (delete dialog, balance-adjustment section) switch language live. Persisted ledger notes (e.g. "Balance adjustment") intentionally stay English (data, not UI). Web typecheck clean; no migration. **hi/nl need a native-speaker review before release.** (Rollout order: 1 accounts ✓ → 2 transactions/cards → 3 planning → 4 settings/data → 5 admin.)
- 2026-07-22 — **i18next-parser tooling for key extraction.** Added automated string extraction so locale catalogs stay in sync with source instead of being hand-maintained. New root `i18next-parser.config.mjs` scans `apps/web/{app,src}` for `t()`/`useTranslation()` and writes **namespace-first** catalogs at `packages/core/i18n/src/locales/$NAMESPACE/$LOCALE.json` (`keepRemoved: true`, blank `defaultValue`, TS/JSX lexers, sorted). To make the layout uniform, the default-namespace files were **moved into the folder layout** `locales/translation/{en,hi,nl}.json` (index.ts imports repointed); the old flat `locales/{en,hi,nl}.json` are now unused orphans — safe to `git rm`. Root `package.json`: added `i18next-parser` devDep + `pnpm i18n:extract` script. Workflow: run extract → review diff → author `en` → translate `hi`/`nl`. Web typecheck clean; no migration. **Note:** couldn't `pnpm install` in this environment (mount blocks unlink) — run `pnpm install` locally to fetch the parser, then delete the orphan flat locale files.
- 2026-07-22 — **Dark-mode text fix (buttons) + i18n namespaces (splits pilot).** (1) **Theme bug:** native `<button>`s don't inherit `color`, so any `.card`/row/icon button that relied on inheritance (splits group/person names, the hamburger's `currentColor` `MenuIcon`) rendered UA-default **black in dark mode**. Fixed globally in `globals.css`: `button { color: inherit; }` (`.btn`/`.chip[data-active]` set their own colour and out-specify it). This corrects the theme across every unclassed button app-wide. (2) **i18n moves to per-feature namespaces** (the scalable pattern, not one monolithic file): `@pocketcare/i18n` now registers a `splits` namespace (`src/locales/splits/{en,hi,nl}.json`) alongside the default `translation` ns (`ns`/`defaultNS` set; new `NAMESPACES` export). **Pilot:** `app/friends/page.tsx` fully routed through `useTranslation("splits")` — every visible string (header, sections, EMI tile member-line with ICU plural + `kind` interpolation, who-owes-whom labels, person sheet, settle modal, remind messages) now switches with the language picker in Settings. en/hi/nl provided. Remaining ~78 files still hold inline English (rollout pending approval). Web typecheck clean; no migration. **Convention for future features:** add `src/locales/<feature>/<lng>.json`, register in `resources`/`NAMESPACES`, consume via `useTranslation("<feature>")`; keep truly-global words in `translation.common`. Automated extraction (`i18next-parser`) recommended before the full rollout.
- 2026-07-22 — **Splits screen redesign: EMI-style group tiles + "who owes whom" + per-person transaction sheet.** (1) **Group tiles** (`app/friends/page.tsx`) rebuilt in the loans/EMI card language to fix the fixed-width title that wrapped vertically on mobile: overlapping **avatars top-left** (smaller — 30px, −10 overlap — showing up to 4 + "+N"), **expand chevron top-right**, **name + "N people · trip/group" bottom-left** (nowrap/ellipsis), **your net bottom-right**. Expanded state still spans the full row and lists the per-member breakdown (now with mini avatars + `.tap-row`). (2) **Compact header:** "Your balance" hero shrunk (padding 24→18, net figure 46→26 on a single label-left/amount-right row, thinner bar). (3) **Who owes whom:** replaced the flat DIRECT list with an aggregate — every 1:1 balance (group pairwise edges + direct) summed per person into **OWES YOU** / **YOU OWE** sections (`BalanceRow`). (4) **Person sheet** now shows the **total at top**, the **itemised transactions** behind the balance (new **`usePersonLedger`** hook in `src/splits/hooks.ts` — per shared expense the pairwise edge via `pairwiseEdges`, plus settlements, newest-first, signed), then **Settle up / Remind**. Docs: `docs/features/splits.md` updated. Web typecheck clean; no migration (read-only new hook).
- 2026-07-22 — **Google sign-in / sign-up (OAuth) with profile import.** (1) **Login page** gained a **"Continue with Google"** button (four-colour `GoogleIcon`, shown in both register and sign-in modes, hidden in reset) → `supabase.auth.signInWithOAuth({ provider:"google", options:{ redirectTo: <origin>/auth/callback, queryParams:{ access_type:"offline", prompt:"select_account" } } })`. (2) New route **`app/auth/callback/page.tsx`** handles the return: the client already has `detectSessionInUrl:true`, so it waits for the session (fast-path `getSession`, else `onAuthStateChange`, 15s timeout), then **imports the Google profile** — copies `full_name`/`name` into the app's canonical `user_metadata.username` (via `updateUser`) + localStorage, stores `avatar_url`/`picture` as `avatarUrl`, sets `onboardingSeen`, and routes home. Errors (`error_description`) render with a "Back to sign in" action. The existing `onAuthStateChange` rekey in `powersync.ts` connects PowerSync under the Google user automatically. (3) **Guest data is preserved:** `signInWithGoogle` checks the current session — an **anonymous guest links Google via `linkIdentity`** (same UID → no re-key, no clear, all guest data kept locally and on the server); everyone else does a normal `signInWithOAuth`. The callback now **prefers an existing `user_metadata.username`** over Google's profile name (only adopts Google's when none is set), so a guest's chosen display name survives linking. Web typecheck clean; no migration. **Deploy notes (Supabase dashboard):** enable **Authentication → Providers → Google** (Google Cloud OAuth client ID + secret); add the app origin's **`/auth/callback`** to **Authentication → URL Configuration → Redirect URLs**; enable **manual linking** (Authentication settings, required for `linkIdentity`); in Google Cloud console add Supabase's `…/auth/v1/callback` as an authorized redirect URI.
- 2026-07-22 — **Forgot-password on login + clearer encryption-passphrase loss warning.** (1) **Login page** (`app/login/page.tsx`) gained a **password-reset flow**: a "Forgot password?" link in Sign-in mode → new `reset` mode (email only) → `supabase.auth.resetPasswordForEmail` sends a 6-digit **recovery** code → reuse the OTP step (`otpType:"recovery"`, `verifyOtp type:"recovery"`) → new `setpw` step sets the new password via `updateUser({ password })`. The set-password step notes that this resets **account sign-in only** — the **encryption passphrase is separate and unchanged**. (2) **SecurityPanel** (`src/crypto/SecurityPanel.tsx`) now warns **before** setup (passphrase never leaves the device, can't be reset) and adds a red **"There is no other way back in"** block on the recovery-code screen: forgetting the passphrase AND losing the code = encrypted fields **permanently unrecoverable, support cannot help**. Confirm button reworded to "I understand — I've saved it". **Note on data model:** encryption is field-level/opt-in (only notes + names/descriptions sealed by the DEK); losing both keys loses only those fields — amounts/dates/accounts/categories still export (encrypted cells export as ciphertext). (3) New **`supabase/email-templates/reset-password-otp.html`** — a branded Reset Password email matching the existing OTP template, renders the 6-digit `{{ .Token }}` and repeats the account-vs-encryption-passphrase note. Web typecheck clean; no migration. **Deploy note:** paste `reset-password-otp.html` into Supabase → Authentication → Email Templates → **Reset Password** (it must contain `{{ .Token }}` for the OTP reset to work).
- 2026-07-22 — **Tap feedback now lands on the tapped item, not the whole card.** On Android, tapping a Recent-Activity row (or any item inside a card that carries pointer handlers) flashed the browser's default blue highlight across the *entire* card. Fixed globally in `globals.css`: `* { -webkit-tap-highlight-color: transparent }` kills the whole-container flash, and interactive elements now supply their **own** pressed state — `.lift:active`, `a.card:active`, `.tx-tile:active` get a subtle `scale(0.985)` press, and a new **`.tap-row`** utility (`:active { background: var(--surface-2) }`, rounded) highlights just a tapped list row. Applied `.tap-row` to the Recent-Activity row `<Link>`s in `src/dashboard/tiles.tsx` (padded + negative-margin so the highlight is inset within the card). Result: cards/rows/tiles each respond only where you press. Web typecheck clean; no migration.
- 2026-07-17 — **Statements page redesigned mobile-first (day-grouped tiles, no more table).** Replaced the overflowing `<table>` with a bank-app-style layout: a **summary header tile** (statement name + range, big non-wrapping Net-for-period, then a 3-column Income/Expenses/Transactions stat row with ellipsised nowrap values) and **transaction tiles grouped by day** (Today / Yesterday / date header with the day's net). Each tile has a coloured **avatar** (first letter of a merchant name extracted from the UPI/BIL narration via `merchantTitle`), the merchant title, the full narration wrapping below, and the amount + time on the right. `groupByDay` helper. Everything fits mobile width; prints fine. Web typecheck clean; no migration.
- 2026-07-17 — **Assistant composer restyle + real mic icon; statement text overflow; checklist flash.** (1) **Composer** is now a Gemini-style rounded pill (borderless textarea inside one rounded surface) with an icon-only mic and a **circular send** (`ArrowUpIcon`, accent when there's text). (2) New **`MicIcon`/`ArrowUpIcon`** SVGs; MicButton uses the proper mic glyph (a filled square = "stop" while active) instead of the 🎙 emoji. (3) **Statement overflow:** the printable `/statements` description column and the analyze-page **outliers** row now wrap/ellipsize (`overflow-wrap:anywhere`, `flex:1 min-width:0`); analyze-page **transaction tiles show the FULL title** (wrap instead of truncate). (4) **Getting-started checklist flash:** now gated on `useInitialSyncPending()` so it no longer appears-then-vanishes for existing users while their counts sync (it only renders after the first sync, when the true done/undone state is known). Web typecheck clean; no migration.
- 2026-07-17 — **Fix misleading empty-state flash on first login (initial-sync skeletons) + checklist for everyone.** Root cause: the local `useQuery` resolves *empty* before the first Supabase sync lands, so pages briefly showed "create your first account" / "No transactions" before the data appeared. New **`useInitialSyncPending()`** in `src/sync.ts` (online && !hasSynced) — pages now show **widget-shaped skeletons** while the first sync is in flight and only show the empty state once synced. Applied to the **dashboard** (new tile-shaped skeleton), **accounts**, and **transactions** (same one-line gate `loading || syncPending`; the pattern applies to any other page). Also: the **GettingStarted** checklist is no longer hidden for paid users — "pro" meant power-users; it now shows for everyone (still dismissible + auto-hides when complete). Web typecheck clean; no migration.
- 2026-07-17 — **Onboarding walkthrough + getting-started checklist + voice input to the assistant.** (1) **Onboarding** (`app/onboarding/page.tsx`) is now an **Inshorts-style swipeable deck** — 7 feature cards, each a gradient graphic glyph + plain-language summary, drag-to-swipe (framer-motion) + dots, skippable; sequenced what-to-do-first (add accounts → log spends → …) then what-you-can-achieve, ending in the auth actions. (2) New **`src/onboarding/GettingStarted.tsx`** — a hand-holding checklist on the dashboard (below the net-worth hero): add first account (with *why*), record a transaction, set a budget/goal; **auto-ticks** from live counts, shows a progress bar, is **dismissible**, and **hidden for Pro** (`isPaid`) or once complete. (3) **Voice narration** to the assistant: `src/assistant/speech.ts` + `MicButton.tsx` add a mic to the composer — **on-device Whisper** (transformers.js lazy-loaded from CDN: record via MediaRecorder → decode/resample to 16 kHz mono → transcribe locally) with a **Web Speech API** fallback when recording/Whisper isn't available; inserts text into the composer (assistant still replies in text). Web typecheck clean; no migration. **Deploy note:** Whisper needs the transformers CDN + HuggingFace model host allow-listed in CSP (already allow-listed for the semantic categoriser) and mic permission (HTTPS).
- 2026-07-17 — **PDF statement parsing is now column-aware (fixes ICICI debit/credit sign).** The old parser flattened each row to a string and *guessed* the sign (Dr/Cr marker or keyword, else debit) — wrong for banks with separate Withdrawal/Deposit columns (ICICI/HDFC/SBI) where credits without a keyword were mis-signed. `parsePdf.ts` now keeps each fragment's **x-position** (`extractPdfRows`), detects the header row + each column's x (`detectHeader`), and assigns every money cell on a row to the **nearest numeric column** (Withdrawal/Deposit/Amount/Balance) so debits/credits keep the correct sign (`parseStatementRows`, pure + unit-tested incl. an ICICI-style layout). Falls back to the old line heuristic when no header is found (`parsePdfStatement`). Analyze page uses `parsePdfStatement` (with password prompt). 10 statement tests pass; typecheck clean.
- 2026-07-17 — **Mobile overflow fix (recharts-in-grid).** Root cause: a recharts `ResponsiveContainer` inside a CSS grid forces an intrinsic min-width, pushing the page wider than the viewport (with `overflow-x: clip` this looked like everything spilling off-screen). Global fixes in `globals.css`: `.card { min-width: 0 }`, `.recharts-responsive-container/.recharts-wrapper { min-width: 0 !important; max-width: 100% }`, and `.pc-hero` now uses `minmax(min(180px,100%),1fr)` + `min-width:0` on items — so charts/cards shrink to fit everywhere (statements analyzer, investments, cashflow, assistant, dashboard). Statement analyze page also hardened (grid `minWidth:0`, wrapping import button). No behaviour change.
- 2026-07-17 — **Statement parser & analyzer (Phase 1, fully on-device).** New `/statements/analyze` + `src/statements/*`. Upload a **bank or credit-card** statement (CSV/Excel-export or digital **PDF**) — parsed **entirely on device**, nothing uploaded. Pieces: `parseCsv.ts` (self-contained CSV reader + **generic column auto-mapper** — date/narration/debit/credit/amount/balance — covering the common HDFC/ICICI/SBI/Axis/Kotak shape, tolerant day-first date parsing, returns the mapping for review); `parsePdf.ts` (lazy pdf.js from CDN → text lines grouped by y-position, password prompt, best-effort line parser); `analysis.ts` (summary in/out/net, by-category, day/month series, **IQR outliers**, **recurring detection** by normalized merchant + amount cluster + cadence); `reconcile.ts` (match parsed vs recorded by amount+date-window+description Jaccard → matched / missing / only-on-platform). On-device **categorisation** via the existing categoriser; **gradient earthy charts** (recharts) for where-it-went + daily spend; **outliers** + **recurring → add as recurring**; **reconcile → import missing** (reuses `importTransactions`); collapsible transaction list; credit-card due/min/pay-by surfaced when present. Pure core unit-tested (9 tests). Web typecheck clean; no migration. **Deferred (per `docs/design/ocr-statements.md`):** scanned-image OCR, server fallback, LLM plain-language layer, per-bank PDF tuning, .xlsx (needs SheetJS — export CSV for now).
- 2026-07-17 — **Loans UI restyled to a bank-statement card language.** New shared `src/loans/ui.tsx` (`Pill` status chip, `Field` labelled footer value, `EmiIcon` green-✓/amber-! status circle, `loanRange`). **Loans list** tiles now show a title + **Active/Closed pill**, a date-range + `X/Y EMI Paid` line, a slim progress bar, and a divider'd **two-column footer** (Loan Amount / EMI Amount). **Loan detail** EMI schedule (both fixed amortization and variable) renders each EMI as its own card: status icon + `Nth EMI` + amount on top with a **Paid/Auto/Due pill** (Due = tap to mark paid, Paid = tap to undo) and the on/due date, then a divider'd footer (Principal Amount / Interest Amount for fixed; the editable EMI input for variable). Replaced the old `PaidControl`/`paidState` + chip rows. Presentational only; web typecheck clean; no migration.
- 2026-07-17 — **Assistant nav fix: no more placeholder `record_transaction`; links render.** Root cause of "I'll take you to your food transactions" → a **`Record expense of INR 0 — placeholder`** confirm card: the model fired `record_transaction` (amount 0) for a *navigation* request, and often promised "a direct link" without emitting one. Fixes: (1) `isValidToolInput` (`src/assistant/tools.ts`) rejects placeholder/zero/empty tool calls (amount ≤ 0, missing names, etc.); the assistant page **auto-rejects** any invalid confirm-tool (no confirm card) and feeds back a tool_result telling the model to reply with a link instead of a tool — the loop then continues so it produces the link. (2) PERSONA: explicit **TOOLS vs NAVIGATION** rule — tools only CREATE/LOG data with real values (never placeholders, never for navigation/search); and a NAVIGATION rule requiring the link (a `<ui>` action href or markdown link) **in the same message**, with an exact food-search example. Both render paths already accept `/search?…` query strings. Also renamed nav `nav.loans` i18n "Loans & Recurring" → "Loans" (en/hi/nl). Web typecheck clean; no migration.
- 2026-07-17 — **Assistant can deep-link to any page, a specific account's edit page, and prefilled search.** Snapshot `accounts[]` now include their **`id`** so the model can build **`/accounts/<id>/edit`** ("open my ICICI account" → matching id). PERSONA's href guidance broadened from a fixed list to **any internal page** (added `/accounts/new /transactions/new /templates /data /help`), plus **deep links** (`/accounts/<id>/edit`, `/loans/<id>`, `/groups/<id>`) and a **filtered search** link spec `/search?q=&type=&account=&from=&to=&min=&max=` — and a NAVIGATION rule to always give a tappable button/markdown link to any screen it mentions. **Search page** (`app/search/page.tsx`) now reads those query params on mount (`useSearchParams`, once) and prefills q/type/account/from/to/min/max (opening the filter panel), so assistant links land on pre-filtered results. Action + markdown hrefs already allow any internal `/…` route (incl. query strings). Web typecheck clean; no migration.
- 2026-07-17 — **Assistant snapshot gains spending history so it stops deflecting to Insights.** `buildFinancialSummary`/`summaryForPrompt` (`src/assistant/summary.ts`) now include **`monthly`** (last 6 calendar months of income `in` vs expense `exp`, zero-filled) and **`topSpendCategories`** (top 8 expense categories over ~3 months, "Uncategorized" for null). PERSONA updated: when asked about trends or where money goes, answer from this data with a **chart card** (monthly → line/bar) or **breakdown card** (categories) and only mention Insights as optional "explore more" — no more "I don't have your transaction history." Still aggregated-only (no raw transactions leave the device); amounts sum per stored currency like the existing cashflow figures. Web typecheck clean; no migration.
- 2026-07-17 — **Assistant: full markdown prose + interactive `<ui>` widgets (both).** Replaced the inline-only formatter with a **safe, dependency-free markdown renderer** (`Markdown` in `src/assistant/richMessage.tsx`): headings, GFM **tables**, **task lists** (`- [ ]`/`- [x]` → real checkboxes), bullet/ordered lists, blockquotes, rules, fenced code, and inline **bold**/*italic*/`code`/`[links](/route)` (internal links via next/link, external → new tab; unsafe schemes dropped — no raw HTML, no XSS). The assistant bubble now renders prose via `<Markdown>`. Removed the short-lived JSON `table` UI card (markdown tables supersede it); the **`<ui>` block stays for interactive/visual widgets** markdown can't do — tappable action chips, progress bars, headline stat cards, distribution breakdown bars, and a new **`chart` card** (line/bar **trend over time**, rendered with **recharts** using the same gradient-fill / rounded-bar / tooltip styling as the dashboard graphs; `points:[{x,y}]`, `chart:"line"|"bar"`). PERSONA rewritten: markdown for static structure (tables for comparisons, task lists for todos/steps, headings, links), `<ui>` for interactive/visual; stay concise. Client-side rendering ⇒ ~zero extra tokens. Web typecheck clean; no migration.
- 2026-07-17 — **Assistant: no more raw asterisks + comparison-table widget + widget-first persona.** (1) Prose now renders through **`renderInline`** (`src/assistant/richMessage.tsx`) — lightweight `**bold**`/`*italic*`/`` `code` `` and strips stray markdown/list/heading markers, so raw `**` never shows. (2) New **`table`** UI card (headers + rows, first column = row label, max 4 cols, horizontal-scroll wrapper) for side-by-side comparisons, alongside stat/progress/breakdown. (3) **PERSONA** rewritten to be widget-first ("glance, not read"): prose is plain text only (no markdown), capped at 1–2 sentences; any 2+ numbers / comparison / distribution / plan / choice must go in the block; added the table spec + guidance on which card to pick; hrefs allowlist extended (`/cashflow /recurring /cards`). Web typecheck clean. (Note: nav already lists **Loans** and **Recurring** as separate items — the "Loans & Recurring" label was only the feedback-area picker, now "Loans"; changes need a rebuild to show.)
- 2026-07-17 — **SIP debit date + recorded as a recurring transfer; fixed "New — fund from account".** (1) The Investments add dialog's **SIP** flow now captures a **next/first SIP date** and a **"debits from" account**, and creates a **recurring transfer rule** (debit account → the investment account, `transaction_templates`+`recurring_rules`, `auto_post`, first-due = the SIP date) instead of a standalone `planned_cashflow` saving — so the scheduled bank debit is a real transfer that posts on the date and shows under Recurring / Planned Cashflow. The existing-vs-new funding section is hidden for SIPs (their money movement is the transfer). (2) **Bug fix:** "New — fund from account" was un-selectable because `fundingAccounts` filtered on `kind === undefined`, but `useAccountBalances` already returns only real accounts (with `kind` = 'real'/null) → the list was always empty and the button `disabled`. Now filters only out investment-type accounts. (3) Renamed the feedback area **"Loans & Recurring" → "Loans"** (+ separate "Recurring"). Web typecheck clean; no migration.
- 2026-07-17 — **Dedicated `/recurring` page; recurring moved out of Templates.** New **`app/recurring/page.tsx`** — a home for recurring **incomes / payments / savings** (salary, rent, bills) grouped in sections with add/edit/remove/post-now and the "due — confirm to record" tray, all via the shared `RecurringModal`. The **Templates page no longer manages recurring rules** (removed the Recurring section + add-rule modal + due tray; it's now just one-tap transaction templates, with a link to `/recurring`). **Planned Cashflow** no longer opens the recurring modal inline: its Add buttons, quick-add chips, per-row Edit, and "Make it recurring" convert all **navigate to `/recurring`** via deep-link query params (`?add=income|payment|saving[&name=&amount=<minor>&freq=&convertFrom=<plannedId>]`, `?edit=<ruleId>`); the page reads them once and opens the modal (and soft-deletes the converted standalone item on save). New **Recurring** nav item under Planning (`t(n.tkey, n.label)` falls back, no i18n change). Web typecheck clean. No migration.
- 2026-07-17 — **Loan EMI schedule is mobile-friendly (no horizontal scroll).** Replaced the wide amortization + variable-EMI `<table>`s (minWidth 420–460, `overflow-x:auto`) on `/loans/[id]` with **stacked card rows** that reflow: each EMI shows `#n · due` and the EMI amount + paid control on one line, with principal/interest/balance as a wrapping muted sub-line (fixed) or the inline amount input (variable). Shared `PaidControl` + `paidState` helpers. No data/logic change; web typecheck clean.
- 2026-07-17 — **Planned Cashflow is now recurring-rule-backed (nothing standalone) + recurring add/convert.** New `src/cashflow/recurring.ts` (`useRecurringItems`, `createRecurring`/`updateRecurring`/`removeRecurring`) creates a `transaction_templates` + `recurring_rules` pair in one step (direction→template type: income→income, payment→expense, saving→transfer into an investment account) — real rules that post transactions via the recurring engine. New `src/cashflow/RecurringModal.tsx` (name, amount, account, optional category for payments, destination investment account for savings, frequency, first-due, auto-post vs confirm). The cashflow hub reads recurring items into incomes/payments/savings, renders them as `RecurringRow`s (next-due + auto/confirm), and includes them in all totals/projections; **Add** buttons and quick-add templates now open the RecurringModal instead of writing standalone `planned_cashflow` rows. Legacy standalone entries still show (labelled "one-off entry") with a **"Make it recurring"** convert action that opens the modal prefilled and soft-deletes the old row on save. Dashboard **Upcoming payments** tile now also reads active recurring expense/transfer rules. No new migration (reuses `transaction_templates`/`recurring_rules`). Web typecheck clean. **NOTE — sync error `PGRST204 … 'sort' column of transaction_templates`: the Supabase DB / PostgREST is behind. Apply pending migrations (0016 adds `sort`; 0029–0036 add the newer columns) and reload the PostgREST schema cache (`notify pgrst, 'reload schema';` or Dashboard → API → Reload). Same fix clears equivalent errors for the new loans/holdings columns.**
- 2026-07-17 — **Auto-calculated EMI + fixed/variable-rate loans.** New pure `emiFromPrincipal(principal, annualRatePct, tenureMonths)` in `@pocketcare/finance` (reducing-balance EMI; 0% → flat P/n; +4 tests → 37). Loan add/edit forms now **auto-fill the monthly EMI** from principal/rate/tenure (editable override, with an "Auto-calculated · Use it" hint). Migration **`0036`** (+AppSchema) adds `loans.rate_type` ('fixed'|'variable', checked) + `loans.emi_amounts` (JSON `{emiNo: amountMinor}`). A **Fixed/Variable** toggle on both forms: **fixed** loans keep the computed EMI + amortization schedule; **variable** loans (untrackable — rate re-prices) hide the single EMI and instead render a **month-by-month list where the user enters each month's actual EMI** (inline `AmountInput`, saved on blur → `emi_amounts`), showing "Varies" for the EMI and a "Paid so far" total; per-EMI due dates + auto-mark still apply, and the mark-paid ledger post uses that month's entered amount. List shows "Varies /mo" for variable loans. `AmountInput` gained an optional `onBlur`. Web typecheck clean; 37 finance tests pass. **Deploy: apply 0036 (columns only — streams unchanged, redeploy safe).**
- 2026-07-17 — **Dashboard "Upcoming payments" tile.** New half-span tile (`upcoming` in `DASHBOARD_TILE_IDS`/`TILE_CATALOG`/`TileView`, default-on for fresh installs, deep-links to `/cashflow#payments`) aggregating every scheduled outflow: **subscriptions** (`next_renewal`), **loan EMIs** (next unpaid EMI via `emiDueDate`, rolling past-due forward), **SIPs + planned payments** (household/electricity/piped-gas/etc. from `planned_cashflow.next_due`, rolled forward by `frequency`), and **credit-card bills** (`credit_card_details.pending_due`/`due_on`, joined to the account for name/currency). Each amount converts to base (`useConvertAmount`); the tile shows the **30-day total** + the soonest six items with relative "Today/Tomorrow/in N days/overdue" labels. Pure date helpers `nextOccurrence`/`nextEmiDate` in `src/dashboard/tiles.tsx`; bucket labels/icons reused from `src/cashflow/model`. No migration. Web typecheck clean.
- 2026-07-17 — **Investments & savings rework: multi-asset (stocks/MF/SIP/crypto/FD/other), grouped tiles + drill-in, dividend-FY, insight charts, dialog add, Planned Cashflow sync.** Migration **`0035`** (+AppSchema) extends `holdings`: `asset_class` (stock|mf|crypto|fd|sip|other, backfilled from `instrument_type`), `current_value` (manual value for unpriced assets), `annual_rate`+`maturity_date` (FDs), `source_account_id` (savings acct that funded a NEW investment), `planned_id` (linked SIP saving). New page structure (`app/investments/page.tsx` rewrite): **grand total** (current/invested/gain), **group tiles** — listed stocks **by exchange** (NSE/BSE/…), everything else **by category** (Mutual Funds/SIPs/Crypto/Fixed Deposits/Other) each with invested/current/gain subtotals — **tap to drill in** to a list of **Zerodha-style holding tiles** (name, qty×avg, LTP, value, gain abs+%) with a **contextual Add** (adds into that exchange/category). **Insights** row: **dividends earned this financial year** (Apr–Mar, via `computeDividendEvents` filtered to FY), **allocation donut**, **gain-by-group** bars — recharts with gradient fills (`src/investments/Charts.tsx`). The inline form is replaced by a **dialog** (`src/investments/AddDialog.tsx`): type chips, catalog picker for listed / free-text for the rest, FD rate+maturity, SIP amount+frequency, and an **existing-vs-new** choice — *existing* raises the invested pool via an `adjustment` (nothing leaves savings), *new* **transfers** the amount from a chosen savings account into the investment account (`src/investments/write.ts`). Pure model in `src/investments/model.ts` (asset classes, grouping, valuation, FY helpers). **SIPs** also create a linked `planned_cashflow` saving (bucket `sip`, new in `BUCKETS`) so they appear under Planned Cashflow → Savings, which also gains a read-only **invested-portfolio summary** card linking to `/investments`. Cap (large/mid/small) chart deliberately deferred (no market-cap feed). Web typecheck clean. **Deploy: apply 0035 (columns only — sync streams unchanged, redeploy safe).**
- 2026-07-17 — **Loan EMI due-dates + auto-mark-paid, EMI→ledger option, grouped-input rollout finished.** (1) **EMI due dates:** migration **`0034`** adds `loans.emi_due_day` (1–31, checked) + `loans.auto_mark_paid` (0/1) (+AppSchema). Add/edit loan forms capture a **loan start date** and **EMI due day**; the schedule now derives each EMI's due date from them via new pure helpers in `@pocketcare/finance` — `emiDueDate(start, dueDay, n)` (first EMI = first due-day on/after start; +1 month each, day clamped to month length), `isDuePassed`, `effectivePaidEmis` (+9 finance tests → 33). (2) **Auto-mark:** a per-loan toggle; when on, past-due EMIs count as paid **derived at read time (never written)**, so toggling off instantly reverts — manual marks in `emi_payments` always win. List + detail both use `effectivePaidEmis` for paid count / next EMI / remaining / progress. (3) **EMI mark-paid → ledger:** the Mark-paid dialog now takes a paid date **and optionally a funding account**, posting an EMI expense via `transactions.create` (checklist follow-up). (4) **Grouped amount inputs** rolled out to the remaining forms — cashflow inline edit, card settle-up + cycle-edit (`AmountInput`), split-expense exact-share + multi-payer inputs, and the Friends settle dialog (budgets/goals already done). Web typecheck clean; **96/96 core tests pass. Deploy: apply 0034 (columns only — sync streams unchanged, redeploy safe).**
- 2026-07-16 — **EMI paid-tracking, Loans page, unified investments, card due clarity, grouped inputs, admin job triggers.** (1) **EMI paid-on:** migration **`0033`** adds `loans.emi_payments` (JSON `{emiNo: paidOnISO}`); loan detail page lets you **mark each EMI paid with a date** (Paid column), deriving paid/next/remaining from it. (2) **Loans page:** `/loans` is now a real list page **in the nav** (Planning) with add + links to `/loans/[id]`; the Planned Cashflow loan bucket redirects here instead of an inline form; the savings section's "Add investment" now opens **`/investments`** (single source of truth for savings/investments). (3) **Cards:** panel shows a clear **"Due this cycle"** amount + **"Pay by"** date (from `pending_due`/`due_on`), notes amount rolled to next cycle, and the edit panel now edits the due amount; empty state already links to account creation. (4) **Grouped amount inputs:** enabled `FloatingInput`'s existing `group`/`currency` (lakh/crore-aware `amountFormat`) on the money fields across accounts/new, loans, loan detail, cashflow, investments (transactions already used `AmountInput`). (5) **Investments empty state** links to add a demat account (done in the demat rework). (6) **Admin:** new **`/admin/jobs`** page to manually trigger `fx-sync` + `market-sync` and display success/error + timing. Typecheck clean. **Deploy: apply 0033.**
- 2026-07-16 — **Real multi-currency conversion + FX feed + currency insights.** Root cause of "changing base currency only flips the sign": `exchange_rates` was empty, so every pair fell back to par (net worth already converts via `aggregateNetWorth`, but had no rates), and subscriptions/planned-cashflow amounts (stored in base-at-creation) were never re-converted. Fixes: new **`fx-sync` edge function** (`supabase/functions/fx-sync`) populates `exchange_rates` daily with all cross pairs among supported currencies from a free FX API (`open.er-api.com`, override via `FX_PROVIDER_URL`; `exchange_rates` already syncs globally via `reference`/`exchange_rates` stream). New hooks in `src/hooks.ts`: **`useConvert`** (Money→base), **`useConvertAmount`** (minor units→base), **`useCurrencyBreakdown`** (net worth by held currency). Cashflow hub now **converts every item to base** for totals/projections/charts and shows subscription/loan/planned amounts in base. **Accounts stay in their native currency** (unchanged); a new **"Across currencies"** card on `/accounts` shows holdings per currency (native + base-converted + share). **Deploy: `supabase functions deploy fx-sync` + schedule it daily** (set `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`). Also: **OCR statement-upload feature spec'd** in `docs/design/ocr-statements.md` (hybrid on-device+edge, zero-retention, plain-language insights) — design only, not built pending sign-off. Typecheck clean.
- 2026-07-16 — **Loans EMI schedule + demat investments + credit-card cycle.** Migration **`0032`** (+ AppSchema): `loans.emis_paid`; `holdings.instrument_type`(stock|mf)/`off_list`/`name`; `credit_card_details.pending_due`/`due_on`; new **`demat`** account type. **Loans:** cashflow "Add payment → loan" now captures principal, EMI, tenure, interest %, start date, EMIs paid; loan rows link to a new **`/loans/[id]`** detail page showing paid/remaining, next EMI, and a reducing-balance **amortization table** (principal vs interest per month) via `amortizationSchedule()` in `@pocketcare/finance` (+4 tests). **Investments:** reworked around a **demat account** (holds an invested amount = balance); holdings deploy from it with an enforced **invested ≤ account balance** check (per-account available shown); MFs tracked in **units** (NAV); **off-list** stocks/MFs allowed with an "untracked gains/losses — held at cost" disclaimer. **Cards:** `/accounts/new` collects statement day, due day, and amount due; if created after the statement day, shows **0 due this cycle** and rolls the amount to the next due date (`pending_due`/`due_on`), with a live preview. Files: `app/loans/[id]/page.tsx`, `app/investments/page.tsx`, `app/accounts/new/page.tsx`, `app/cashflow/page.tsx`, `packages/core/finance`. **Deploy: apply 0032 + redeploy sync rules (columns only; streams unchanged).** Typecheck clean; 24 finance tests pass.
- 2026-07-16 — **Fix: "About you" gender/country never persisted.** `ProfileTraits` read from the local synced `profiles` row (`useQuery`) but `save()` wrote **straight to Supabase Postgres** (`getSupabase().schema('pocketcare').from('profiles').update()`), bypassing PowerSync — so the local row never changed and the panel kept showing empty + re-prompting. Now writes to the **local** DB (`updateRow('profiles', uid, …)`, or a local `INSERT` if no row yet); PowerSync uploads it. Also relaxed the one-shot `ready` guard → a `dirty` flag so synced values re-seed the selects without clobbering an in-progress edit, and added a saving state. General rule reinforced: **write local, let PowerSync upload** (never write synced tables directly to Supabase). File: `apps/web/src/ui/ProfileTraits.tsx`. Docs: `docs/features/settings.md`.
- 2026-07-16 — **Fix: account deletion was a no-op (404).** `delete_user_account` lives in the `pocketcare` schema, but Settings called `supabase.rpc(...)` which resolves in `public` → PostgREST 404, and the ignored result meant it signed the user out having deleted nothing (email stayed registered, data intact on next login). Fixes: (1) client now calls `supabase.schema("pocketcare").rpc("delete_user_account", …)` and **checks `error`** — surfaces failures in the modal, and only signs out + `disconnectAndClear()`s the local mirror on success; (2) migration **`0030_delete_account_fix.sql`** rewrites the function to reliably delete the identity (explicit owner-table cleanup incl. `planned_cashflow` + `delete from auth.users` which cascades the rest), drops the broken/confusing "orphan records" branch (always full-delete), and `grant execute … to authenticated`. Settings modal simplified to a single "Delete everything" action. **Follow-up `0031_delete_account_splits.sql`:** the multi-user splits tables from 0011 (`split_groups.created_by`, `expenses.created_by`, `expense_participants.user_id`, `settlements.from_user/to_user/created_by`, `split_invitations.inviter`) FK `auth.users` **without** cascade, so `delete from auth.users` threw `expense_participants_user_id_fkey`. 0031 rewrites the function to clear the leaving user's splits rows in FK-safe order first (verified via full-schema scan: these 7 are the *only* non-cascade FKs to auth.users; everything else cascades). 0031 is self-contained and supersedes 0030's function body. **Deploy: apply 0030 + 0031 (or just 0031).**
- 2026-07-16 — **Dashboard tiles tap-to-navigate + Cashflow section deep-links.** Tapping any dashboard tile now routes to its detail page (drag-reorder/long-press-edit preserved; taps on inner controls still win); subscriptions tile → `/cashflow#payments`. Planned Cashflow hero stat cards are buttons that smooth-scroll to their section (`#incomes/#payments/#savings/#summary`), and the page honors incoming `#hash` with a retry while synced data loads. Files: `app/page.tsx`, `src/dashboard/tiles.tsx` (new `TILE_HREF`), `app/cashflow/page.tsx`.
- 2026-07-15 — **Space-efficient list layouts (shared `.list-grid`).** Full-width stacked list rows → responsive tile grid `repeat(auto-fill, minmax(min(320px,100%),1fr))` (1 col mobile → 2 laptop → 3+ wide), same info retained. Applied to Cashflow sections, Search, Transactions, Goals, Budgets, Investments, Templates (Accounts already gridded at 260px). `TransactionRow` gains a `tile` prop → `.tx-tile` bordered card w/ hover lift (vs. divider row); Search/Transactions now render tiles + full-width empty/skeleton states. Friends/Groups left single-column (expandable nested balances). Typecheck clean.
- 2026-07-15 — **Planned Cashflow hub (BETA) — unifies incomes, payments, savings + AI projections.** New consolidated route **`/cashflow`** (replaces standalone `/subscriptions` + `/loans` in nav; both old routes now `redirect('/cashflow')` so dashboard/insights CTAs still land). Migration **`0029_planned_cashflow.sql`** + AppSchema add one owner-scoped synced table **`planned_cashflow`** (`direction` income|payment|saving, `bucket` sub-category, `amount` minor units, `frequency` Period, `timeframe` monthly|quarterly|yearly, `next_due`, `expected_return` annual %×100 for savings, `is_active`); **sync-streams.yaml** gains it in `user_data`. The hub merges three tables: incomes/household/savings → `planned_cashflow`, subscriptions → `subscriptions`, loan EMIs → `loans` (add/edit/delete route to the right table from one UI). Sections: timeframe segmented control (Monthly/Quarterly/Yearly scales the aggregate hero), recurring incomes, planned payments (household + subs + loans), savings tracker (per-item expected return), quick-start **templates** (Salary/Freelance/Rent/Electricity/House Help/Groceries/FD/Emergency/MF/Stocks/Crypto), and a **deterministic AI projection panel** (finance `projectCashflow`: monthly-compounded, inflation-aware, income-growth; 1/2/3-yr structure cards + nominal-vs-real growth area + net-surplus bars; tunable return/inflation/growth/starting-savings sliders). Charts in `src/cashflow/Charts.tsx` (recharts, CSS-token colors). New finance exports: `projectCashflow`, `yearlyEquivalent`, `timeframeTotal` (+9 tests, all green). Module code under `apps/web/src/cashflow/` (`model.ts` pure logic/templates, `Charts.tsx`, `Projections.tsx`) + `app/cashflow/page.tsx`. New CSS (tokens only): `.beta-badge`, `.pc-segment/.pc-seg-btn`, `.pc-hero`, `.pc-template`, `.pc-row-icon`, `.pc-glass` (glassmorphism), `.pc-range`. **BETA** badges on the page, AI panel, add-modal, and sidebar nav item. **Deploy: apply 0029 + redeploy sync streams.**
- 2026-07-15 — **Assistant: visual/actionable responses + native-messenger chat layout.** (1) **Structured responses:** PERSONA now enforces 1–3 sentences of prose and one `<ui>{cards,actions}</ui>` JSON block; new `src/assistant/richMessage.tsx` parses/validates it (defensive; malformed → plain text) and renders **stat cards** (headline numbers, toned), **progress cards** (ProgressBar), **breakdown bars** (parts-of-whole / step plans), and **action chips** — `{label,send}` sends a chat message on tap (suggest, don't decide), `{label,href}` links to allow-listed internal routes. Blocks persist in message content, so reopened threads re-render visuals. `MAX_TOKENS` 700→900. (2) **Layout:** chat view is now a fixed frame sized to `window.visualViewport` (JS sets `height` on `.assist-page`; CSS dvh calc = first-paint fallback): pinned header (‹ Chats / quota / New chat), `.assist-thread` is the ONLY scroller (`overflow-y:auto` + `overscroll-behavior:contain`), `.assist-composer` pinned at the bottom (auto-expanding textarea unchanged). `body[data-assist-chat="true"]{overflow:hidden}` locks the page while chatting — safe vs the old mobile touch-scroll bug because there's no outer scroller to chain to; keyboard opening shrinks the visual viewport → frame resizes → composer stays visible (incl. iOS Safari, where dvh ignores the keyboard).
- 2026-07-13 — **Assistant rework: mobile scroll fix, landing + chat screens, greeting-first chat, "why can't you" persona.** Root cause of the mobile no-scroll bug: `.assist-scroll` was a nested `overflow-y:auto` + `overscroll-behavior:contain` container — touch gestures starting on it (e.g. the suggestions card) were swallowed and never chained to the page. Fix: **no nested scroller** — the body scrolls; new `.assist-page` (flex column, dvh min-height) + `.assist-thread`; `.assist-composer` is now `position: sticky; bottom: 0` (works under the framer-motion template since sticky ignores transformed ancestors, unlike fixed). Page split into two views: **landing** (Start a new chat + Continue a conversation list w/ dates + delete; quota/reset/help) and **chat** (‹ Chats back, quota chip, New chat; first message is a local-only assistant greeting bubble with suggestion chips that send on tap and disappear after the first user turn; auto-scroll to newest via `endRef.scrollIntoView`; multiline sticky composer, Enter=newline, ⌘/Ctrl+Enter=send). PERSONA gained a "when asked WHY you can't fetch prices / search the internet" clause: financial decisions are the user's; researching a price yourself is a healthy pause that can slow an unwise purchase; invite them to bring the number back — warm, 2–3 sentences, never scolding.
- 2026-07-13 — **Admin feedback console: filters, sort, expandable descriptions, LLM export.** `app/admin/feedback/page.tsx`: filter by kind + severity + area (dynamic) + status (dynamic) + free-text search (title/description/reporter/route); sort newest/oldest/severity(fatal→low)/area. Description no longer truncates — 2-line clamp, click a row to expand full text (pre-wrap) + auto-captured context (route/version/platform/viewport/offline/status). New **"Export for LLM"** button: builds a markdown prompt from the *current filtered+sorted view* (project-context header, triage/fix-plan instructions, bugs + suggestions with per-record context), copies to clipboard AND downloads `.md`. Also: `support-key/*.share` files untracked (`git rm --cached`) + `.gitignore` (`support-key/`, `*.share`), commit `7b05b85` — key rotation + history purge still pending (manual, offline).
- 2026-07-10 — **Shared promo codes (BETA_TESTER) + audience-segment foundation.** Migration `0024`: `promo_codes` (shared code → tier/months, active window, max redemptions), `promo_redemptions` (once per user), `profiles.gender`/`country` (optional self-declared traits), `segments` (named trait-rule audiences). Seeded **`BETA_TESTER`** = 1 month **Pro** from the apply date. `redeem-coupon` now falls back to promo codes (validates active/window/limit/not-already-used → grants comp from apply date, records redemption). Settings gains an optional **"About you"** panel (gender/country, private). New `scripts/admin-coupons.ts`: `promo` (create/activate a code), `segment` (save an audience rule), `issue` (bulk-issue per-user coupons to users matching gender/country). AppSchema `profiles` +traits, `promo_redemptions`; streams add own `promo_redemptions`. **Apply 0024 + redeploy streams + `redeem-coupon`.** *Not yet:* an admin console UI + targeted **notification delivery** (needs the deferred notification center) — segmentation currently drives coupon issuance via the admin script.
- 2026-07-10 — **Critical fix: subscription purchase now activates the plan.** Root cause: every `entitlements` write in `razorpay-webhook` + `razorpay-subscription` used `.update().eq(user_id)`, which **silently no-ops when the user has no entitlements row** — payments recorded (billing history) but `tier` never changed. Switched all four writes (subscription activate/charge, downgrade, credit apply, subscription pre-record) to **`upsert({...}, {onConflict:'user_id'})`** so the row is created/updated. Client: `startSubscription`/`buyCredits`/`redeemCoupon` now **fail fast when offline** ("reconnect to change your plan"); Billing `planCard` **blocks re-buying** — same tier+cycle shows "Current plan", a lower tier while a sub is active shows "Included in your plan", same tier other cycle = "Switch to {cycle}". **Redeploy `razorpay-webhook` + `razorpay-subscription`.**
- 2026-07-10 — **Proper on-device semantic auto-categoriser (replaces keyword matching).** `src/categorize/semantic.ts`: lazy-loads a small sentence-embedding model (transformers.js `all-MiniLM-L6-v2`, quantized) from a CDN at runtime (`webpackIgnore` dynamic import → not bundled; browser-cached → offline after first use). Each category is embedded from rich example **anchors** (`src/categorize/anchors.ts`) into a centroid; input text is embedded and matched to the nearest category by **cosine similarity** (threshold 0.30) — so any word maps to the closest category by meaning, not keyword equality. **Learns from corrections**: `semanticLearn` stores confirmed (phrase→category) examples in IndexedDB and folds them into centroids (rebuilt on change). `useAutoCategorize` tries semantic first and **falls back to the keyword engine** on any model failure (offline first load / blocked CDN), so it never breaks the form; `useLearnCategory` feeds both. Still premium-gated. **Deploy note:** allowlist `cdn.jsdelivr.net` (library) + `huggingface.co`/`cdn-lfs.huggingface.co` (weights) in CSP if set; first use downloads ~20MB once. Old keyword `engine.ts`/`seeds.ts` remain as the fallback.
- 2026-07-10 — **Feedback: segment bug vs suggestion.** Migration `0023`: `bug_reports.kind` ('bug'|'suggestion', default bug), `severity` made nullable (suggestions omit it), and the award trigger now counts only `kind='bug'` (suggestions don't earn coupons). Modal gained a 🐞 Bug / 💡 Suggestion segmented toggle (severity shown for bugs only, copy adapts); sidebar entry renamed "💬 Feedback". AppSchema `bug_reports` +`kind`. **Apply 0023.**
- 2026-07-10 — **Beta bug reports + reward coupons; auto-cat re-gated to premium.** Migration `0022`: `bug_reports` (severity fatal/high/medium/low, area, description + auto-captured version/route/platform/UA/viewport/online), `coupons` (per-user, `tier` lite/pro, `months`, `expires_at` redeem-by, `redeemed_at`, unique per milestone `reason`), and `entitlements.comp_tier`/`comp_until` (time-bound complimentary plan). A **security-definer trigger** auto-issues a 1-month Lite coupon at 5 reports and Pro at 25. `useEntitlement` now folds in an active comp tier (higher of base/comp wins). Edge fn `redeem-coupon` validates (own + unexpired + unredeemed) and applies/extends the comp. UI: `src/ui/BugReport.tsx` low-effort modal (severity chips + area dropdown + free text; context auto-captured) opened from a sidebar "🐞 Report a bug" button; Billing gains a **coupon redeem** section (earned coupons + manual code). Reports insert via PowerSync (offline-safe; trigger fires on sync). **Apply 0022 + redeploy streams + deploy redeem-coupon.** Also: **reverted the auto-categorisation ungate** — it's premium-only again (seed name-mismatch fix + generic words kept); a proper on-device *semantic* classifier (embed category set, nearest-match any word) is the planned rework, replacing keyword seeds.
- 2026-07-10 — **PWA install guide, AI-credits gating, goals cap + orbitable cake.** (1) **Install:** shared `src/pwa.ts` (`useInstallPrompt`, platform detect, standalone check) + `src/ui/InstallGuide.tsx` — one-tap install where supported (Android/desktop `beforeinstallprompt`), else concise iOS-Safari / Android / desktop steps with inline glyphs; opened from a sidebar "Install app" button and an onboarding button. (2) **AI credits now require Lite/Pro:** server gate in `razorpay-credits` (checks `entitlements.tier`, rejects free) + upgrade prompts replace credit packs in the assistant out-of-quota card and the Settings billing panel. (3) **Goals:** allocations are **capped at the target** (never over-fund; modal shows "X left" + cap note); a **funded goal card** gets a subtle celebratory gradient/glow + "🎉 Funded"; the celebration cake becomes a **drag-to-orbit 3D model** after its entrance (auto-close paused while you spin), and still fires only once per goal (localStorage guard).
- 2026-07-10 — **Offline session cache, INR grouping, past-only budget chart, auto-cat "working" indicator.** (1) `useSession` now hydrates from a `pc_session` localStorage cache and reads `auth.getSession()` (local, no network) instead of `getUser()`, so display name / "signed in as" survive offline. (2) `amountFormat.groupAmount(raw, currency)` now does **Indian lakh/crore grouping** (1,00,000) for INR/PKR/LKR/BDT/NPR, Western otherwise; `currency` threaded through `AmountInput`/`FloatingInput group` and passed at all call sites (transactions use the account currency, goals/budgets their own). (3) `BudgetSpendChart` clamps its series to **today** — no future days of the period. (4) `useAutoCategorize` exposes `working`; New Transaction shows a spinning "✦ Finding category…" while it runs (per keystroke) and "✦ Auto-categorised · <name>" once applied.
- 2026-07-10 — **Dashboard tiles are draggable in place.** New `DraggableGrid` in `app/page.tsx`: each tile has a grip (⠿, top-centre); pointer-drag reorders live by hit-testing tile rects (works with the 2-col grid + full-width tiles), lifts the dragged tile (shadow/opacity), and persists via `reorderTiles` on release (hidden premium tiles kept at the tail of the order). The Customize-menu drag list still works too.
- 2026-07-10 — **UX batch: charts, trends, friends graph, budgets, forms.** Dashboard: pie now uses `minAngle` + % labels so small categories show beside a big one; Cashflow got a compact Y-axis; new **Expense Trends** tile (`3d/1w/1m/1y` dropdown → period-relevant buckets: days/weeks/months, gradient area). New **`FriendsGraph`** (radial SVG who-owes-whom, arrows to the creditor, green/red) on `/friends`. Budgets: rows show the **active window dates** (`periodWindow`), a **cumulative spend-vs-limit** area chart per budget, **delete confirmation**, and the create form gained a **currency selector + alert-threshold + missing-field errors**. Forms: reusable `src/ui/amountFormat.ts` + `AmountInput` + `FloatingInput group` add **live thousand separators** (caret-preserving) — wired into new-transaction amounts, goals, budgets; **goals** got a currency selector + validation errors; the account-card net-worth toggle now uses a **ScaleIcon** (was a misleading eye).
- 2026-07-10 — **Security part 2b: encryption + sealed support access (COMPLETE).** Migration `0021`: `user_keys` (salt + `wrapped_dek_passphrase`/`wrapped_dek_recovery` + `signing_public_jwk` + `wrapped_signing_private`), `support_grants` (scope, `wrapped_dek_for_support`, signature, expires_at, revoked_at), `security_audit` (append-only, **hash-chained via a before-insert trigger** using pgcrypto digest) — owner RLS, audit insert+select only; new `user_data` streams + AppSchema. `@pocketcare/crypto` gained `auditRowHash`/`verifyAuditChain` (mirror of the trigger) — core suite now **76**. Client: `src/crypto/session.ts` (key lifecycle — setup returns a one-time recovery code, unlock/unlockWithRecovery, lock, in-memory DEK + signing key; writes wrapped keys straight to Supabase, reads from the synced copy), `src/crypto/fields.ts` (backward-compatible `encryptForWrite` + `useDecrypted` w/ ciphertext→plaintext cache; plaintext passes through, locked shows •••••), `src/crypto/support.ts` (issue/revoke user-signed, 2h, scoped grants + audit), `src/crypto/SecurityPanel.tsx` (Settings → set up/unlock/lock encryption + Grant Temporary Support Access, structural vs content). **Demonstrated column encryption** on `transactions.note` (encrypt on new/edit save, decrypt on edit prefill; other fields extend mechanically via the same helper). Headless `scripts/support-admin.ts`: `verify` (grant sig+expiry), `drift` (reconcile local vs remote, no plaintext), `decrypt` (Shamir-reassemble support key → unwrap DEK under a live grant → decrypt) — all actions hash-chained into `security_audit`. Config: `NEXT_PUBLIC_SUPPORT_PUBLIC_JWK` (client), support private key Shamir-split offline. **Apply 0021 + redeploy streams.**
- 2026-07-10 — **UX: dashboard tile reordering, portal'd dialogs, global back + scroll restore.** (1) **Reorder:** `dashboard.ts` now persists an explicit tile ORDER (was catalog-order); new `reorderTiles()`; the Customize dialog has a pointer-drag sortable list (grip handle, touch+mouse) for enabled tiles plus an Add section — the dashboard renders in that order. (2) **Mobile dialogs:** `Modal` now renders via `createPortal(document.body)` so the fixed overlay anchors to the viewport, not a transformed framer-motion ancestor (that ancestor `transform` was pushing dialogs below the fold — fixes it everywhere at once); also locks body scroll while open. (3) **Back + scroll:** `AppShell` shows a global "‹ Back" on sub-pages (path depth ≥ 2) and restores per-route window scroll from sessionStorage (retrying briefly while async data grows the page), so returning to a list continues where you left off.
- 2026-07-10 — **Security part 2a: crypto core (Hybrid zero-trust foundation).** See `SECURITY_ENCRYPTION_PLAN.md`. New `@pocketcare/crypto` (WebCrypto, pure): PBKDF2 KEK derivation, random DEK, AES-GCM DEK wrap/unwrap + field encrypt/decrypt (`v1.<iv>.<ct>` envelopes), recovery-code path, RSA-OAEP support-grant wrap/unwrap, ECDSA consent-token sign/verify, and `./shamir` GF(256) M-of-N secret sharing (for splitting the support private key). 10 tests (round-trip, confidentiality, GCM tamper detection, wrong-key rejection, recovery, support grant, consent signatures, Shamir 2-of-3 & 3-of-5) — core suite now **74**. Model: encrypt sensitive *content* client-side (admins see ciphertext), keep amounts/tickers readable; support is drift-fix-by-default (no plaintext) with user-consented, 2h, Shamir-split, hash-chain-audited content grants. **Next:** schema (`user_keys`/`support_grants`/`security_audit`), key lifecycle UI, field encryption through repos, support switch, headless Support Admin.
- 2026-07-10 — **CI/CD + exhaustive tests (part 1 of the security epic).** New `.github/workflows/ci.yml`: `unit` job (pnpm install → `pnpm typecheck` → `pnpm lint` → `pnpm test:core` → `pnpm build`), `e2e` job (Playwright Chromium smoke), and a **gated `deploy-production` job** (`needs: [unit, e2e]`, main-push only) that promotes via Vercel CLI (`vercel pull/build/deploy --prebuilt --prod`) — so a red test blocks prod (requires Vercel Git prod auto-deploy disabled + `VERCEL_*` secrets). **AI guardrail:** new `@pocketcare/guardrail` — pure deterministic `screenPrompt` blocking injection / exfiltration / secrets / malware / harmful classes, with **54 adversarial fixtures + 15 benign** (7 tests, all green); wired into the `assistant` edge function (refuses before hitting the model, mirror kept in sync). **Offline-sync fidelity:** new `@pocketcare/reconcile` — order-independent BigInt-FNV checksum + `reconcile()` drift report (missingRemote/missingLocal/mismatched); 8 tests incl. the 10-offline-txn → 100%-fidelity case (also the backbone for the future Support Admin drift-fixer). Core suite now **64 tests** (was 49). Playwright: `playwright.config.ts` + `e2e/{smoke,offline-sync,premium-trial}.spec.ts` (full backend-dependent flows gated behind `E2E_FULL=1`). Aligned all core-package tsconfigs (`allowImportingTsExtensions`+`noEmit`) so `pnpm typecheck` is genuinely green; excluded `e2e/` from the web tsconfig. **Next in this epic:** Vault/pg_crypto column encryption, then sealed Support Access tokens.

- 2026-07-09 — **Market data — Phase 3: projected future wealth on insights.** `src/market/ProjectionPanel.tsx`: compounds current portfolio value (market quotes → base via FX, cost fallback) at a user-set growth rate, plus monthly contributions, plus optionally reinvested dividends (effective yield derived from `dividendSummary`). Interactive sliders (growth %, monthly contribution, horizon 1–40y, reinvest toggle) drive a gradient area chart (projected value over contributed baseline, recharts) + stat tiles (projected value / put in / growth+dividends). Guidance-not-advice disclaimer. Rendered on `/insights` below the dividend panel; both panels no-op when the user has no holdings. Completes the Alpha Vantage arc (valuation → dividends → projection).
- 2026-07-09 — **Market data — Phase 2: dividend income by period on insights.** `src/market/dividends.ts` estimates dividend income in base currency (per dividend row: amount-per-share × current shares held, converted via `useRates` FX), buckets it by **week / month / quarter / year / all** (`bucketize`, recent windows capped, "all" = by-year over full history), and computes a summary (last-12-months, next-12-months est., all-time). `src/market/DividendPanel.tsx`: interactive period toggle + gradient rounded bar chart (recharts, matching the dashboard style; amber bars = upcoming scheduled ex-dates) + the three stat tiles; rendered on `/insights` above the feed (Premium-gated with the page). Reads `holdings` × `market_dividends`. Estimate caveat: uses current share counts (historical lots aren't tracked). **Phase 3 (next):** projected future wealth.
- 2026-07-09 — **Market data (Alpha Vantage) — Phase 1: collective sync + investments valuation.** See `MARKET_DATA_PLAN.md`. Free tier is EOD-only + **25 calls/day**, so sync is **server-side & collective**: edge function `market-sync` reads the deduplicated union of held symbols across all users, spends a 25-call budget (≤20 `TIME_SERIES_DAILY` prices, ≤3 `DIVIDENDS`, ≤2 `OVERVIEW`) round-robin by staleness, and writes global tables. Migration `0020`: `market_symbols` (service-role only), `market_quotes`, `market_dividends`, `market_overview` (read-only RLS) + reuses `price_snapshots` for history. New `market_data` sync streams (synthesized text `id`), AppSchema tables, and `src/market/hooks.useMarketData()` (symbol+exchange lookups w/ symbol fallback). Investments page now shows **current value / invested / total gain-loss %**, per-holding live price + day change + gain%, and a dividend hint (yield · next ex-date); the obsolete per-holding "daily fetch" toggle is removed (sync is collective now). Key is a **server secret** `ALPHAVANTAGE_API_KEY` (never shipped to browser). **Deploy:** `supabase secrets set ALPHAVANTAGE_API_KEY=…` → apply `0020` + add tables to publication → redeploy streams → `supabase functions deploy market-sync` → schedule daily (pg_cron+pg_net, see plan). **Phases 2–3 (next):** dividend earnings-by-period graphs + projected wealth on insights.
- 2026-07-09 — **Per-account overdraft policy (allow/disallow negative balances).** Migration `0019` adds `accounts.allow_negative boolean default false` (credit cards set true). AppSchema + `Account` type + `accounts.create/update` carry it (create defaults `allow_negative` to `type === 'credit_card'`). Enforcement lives in the transactions repo: new `OverdraftError` (exported) + private `assertNoOverdraft(accountId, deltaMinor, excludeTxnId)` derives the account's ledger balance (excluding the edited row) and throws if an expense / transfer-source would push a no-overdraft account below zero — called from `create()` and `update()`. `runRecurring` now wraps each materialize in try/catch so an overdraft-blocked auto-post leaves the rule due and doesn't stall others. UI: an "Allow negative balance (overdraft)" toggle on new + edit account (new-account default follows type), and a friendly inline error on the new/edit transaction screens. **Apply 0019 + redeploy sync-streams.**
- 2026-07-09 — **Goal-achieved easter egg: the tile morphs into a cake.** Reworked from the three.js cylinder to a **CSS-3D morph** (`src/goals/GoalCelebration.tsx`, no three.js): the cake is built from six faces sized to the goal *tile* (top face = the tile itself, showing the title + a full 100% progress bar; four frosted sides; a plate). It animates `pc-cake-rise` — starts as a flat top-down view (`rotateX(-90)`), **rises** (translateY) then **turntable-rotates** to a front view (`rotateX(-22) rotateY(360)`), revealing the body. Candles stand out of the top face (`rotateX(-90)` off the face plane) with billboarded flickering flames. A center-out **confetti** burst (2D canvas) fires ~0.5s in, timed to the rise/rotate. Detection unchanged (`GoalCard` prevFunded transition + `pc_goals_celebrated` localStorage guard). Reduced-motion → static 🎂 card.
- 2026-07-09 — **Catalog download is now lazy + investor-gated with a progress bar.** Removed the app-open `refreshCatalog()` — no one downloads the 6MB ticker DB unless they track investments. New `refreshCatalogWithProgress(onProgress)` **streams** the response (reads `res.body` reader, tracks bytes vs `content-length`, falls back to a 7MB denominator / indeterminate bar) and returns `ok|unchanged|offline|error`; `isCatalogReady()` checks whether a full fetch has ever landed. `src/instruments/hooks.useCatalog(active, autoStart)` runs only when the user has stock/MF accounts: first visit → one-time download with progress; later visits → silent daily refresh. `src/instruments/CatalogProgress.tsx` renders the bar + an offline/error note with **Retry** (seed keeps the picker usable throughout, so entry is never blocked). Wired into `/investments` Add-holding card.
- 2026-07-09 — **Instruments catalog now sources the free global ticker DB daily.** `refreshCatalog()` fetches [`adanos-software/free-ticker-database`](https://github.com/adanos-software/free-ticker-database) `data/tickers.csv` (MIT; 63k+ tickers / 80+ exchanges; columns `ticker,name,exchange,asset_type,stock_sector,etf_category,country,country_code,isin,aliases`) from **GitHub raw** (permissive CORS) once a day, client-side into IndexedDB. Parsed with the existing `src/data/csv.parseRecords`. Trading currency isn't in the dataset so it's derived: `CCY_BY_EXCHANGE` (exchange code) → `CCY_BY_COUNTRY` (ISO country_code) → USD. **ETag conditional GET** (`if-none-match`) means unchanged days return 304 with no multi-MB re-download. `NEXT_PUBLIC_INSTRUMENTS_URL` still overrides (a `.csv` in that schema, or a `.json` array). Seed exchange codes aligned to the dataset (`NSE_IN`/`BSE_IN`). No new deps.
- 2026-07-09 — **Investments: searchable stock picker + daily-refreshed offline catalog; transaction delete; CRUD sweep.** (1) **Instruments catalog** (`src/instruments/catalog.ts`): a *local-only* (non-PowerSync) universe of stocks/ETFs cached in **IndexedDB** and merged with a bundled `SEED` (~60 top tickers across NASDAQ/NYSE/NSE/BSE/LSE/XETRA/EPA/AMS/SWX/TSE/HKEX/ASX/TSX) so the picker works offline on first run. `refreshCatalog()` fetches at most **once a day** from a configurable CORS JSON source (`NEXT_PUBLIC_INSTRUMENTS_URL`, empty by default → fully offline no-op), normalizing common field aliases; `searchInstruments(query, exchange, limit)` ranks exact-symbol → prefix → contains. `AppShell` fires `refreshCatalog()` ~4s after mount (beside `runRecurring`). (2) **Picker UI** (`src/instruments/InstrumentPicker.tsx`): async type-to-search combobox + `ExchangeSelect` scope; wired into `/investments` Add-holding and the edit row — replaces the old free-text Symbol input and stores the chosen `symbol`+`exchange`+`currency`. Migration `0018` adds `holdings.exchange text` (AppSchema updated; `SELECT *` stream picks it up). (3) **Transaction delete**: new `transactions.remove(id)` repo method (soft-deletes txn, soft-deletes items, clears label links, writes a `delete` audit record) + a **Delete** button on `/transactions/[id]/edit` — closes the last CRUD gap. Note: **auto-categorization is already built** (`src/categorize/`, migration `0017_category_rules`) and shows a `✦ Auto-categorised` badge on New Transaction, learning from corrections on save.
- 2026-07-09 — **Templates + recurring engine.** Migration `0015`: `transaction_templates` (saved transactions incl. optional split config) + `recurring_rules` (a template on a schedule) — owner RLS, per-user streams, AppSchema. `src/templates/`: write (`createTemplate`, `createRule`, `materializeTemplate` — plain or equal-split, `postRuleOnce`/`skipRuleOnce`, `runRecurring` generate-on-open with catch-up + cap) and hooks (`useTemplates`/`useRules`/`useDueRules`). Recurring is materialised **client-side on app open** (auto-post rules post automatically; confirm rules surface in a due-tray) — `AppShell` calls `runRecurring()` once ~2.5s after mount. New `/templates` page (manage templates + recurring rules + due-to-confirm tray). Add-transaction gains **?template=ID prefill** and a **Save as template** button. Nav: "Templates" under Money. Email notifications deferred to a later phase.
- 2026-07-09 — **Assistant refresh + Help/FAQ page.** Updated Ask PocketCare for the grown app: `PERSONA` scope now covers splits/friends/groups-trips, insight feed, loans/investments, CSV, and includes split how-to know-how + updated action list. `summary.ts` snapshot adds `splits{owed,owe,groups}` (derived like the Friends page, aggregated only). New assistant tools (all confirm-gated): `record_transaction` (income/expense on a named/default account + optional category), `create_subscription`, `create_group` — with execute/describe wiring. New graphical **`/help`** page: searchable, accordion FAQ across 11 sections (getting started, transactions, budgets, goals, subscriptions, splits & friends, cards/accounts, insights/statements, Ask PocketCare, premium/billing, privacy/sync); nav link under Settings + a Help chip on the assistant.
- 2026-07-09 — **Splits: multi-user shared ledger (Splitwise-style).** Migration `0011` **replaces** the Phase-1/2 contact schema (drops `contacts` + old split tables) with a shared ledger keyed by real `user_id`: `split_groups`, `split_group_members(user_id)`, `expenses`, `expense_participants(paid+share per user, denormalized group_id)`, `settlements(from_user→to_user)`, private `expense_postings`, `split_invitations`, `connections`; `profiles.display_name/email` for identity; `is_group_member`/`can_see_user`/`is_group_creator`/`is_connected` SECURITY DEFINER helpers; **membership-based RLS** (shared rows visible to group members, private postings owner-only, membership mutation service-role/creator-only). `sync-streams.yaml`: new `split_shared` stream (membership subqueries) + co-member/connection profiles + private postings/invites/connections. Edge functions `split-invite` (create token+link, member-only) and `split-invite-accept` (add membership + connect, link-is-secret + 14d expiry). Client rewrite: `src/splits/{math(pairwiseEdges by user),hooks(useUserProfiles/useConnections/useGroups/useGroupMemberIds/useFriendBalances/useGroupBalances),write(createGroup/getOrCreateDirectGroup/createSplitExpense/settleUp/createInvite/acceptInvite + personal projection)}`; Friends/Groups pages, group detail (Invite → link), add-transaction editor (pick group → members → modes/multi-payer), dashboard tile, new `/join?token=` page (bare route). Everyone is a registered user; no data copied across users; shared rows carry no private data (account/category live only on private postings). **Deploy order:** apply `0011` → ensure new tables in `powersync` publication → redeploy sync streams → `supabase functions deploy split-invite split-invite-accept` → set `APP_URL` secret (for invite links) → ship client. Needs two-account testing.
- 2026-07-09 — **Splits: shared tables scrubbed of private data (multi-user prep).** Migration `0010` drops `shared_expense_payers.account_id` and `shared_expenses.category_id`. The shared fact now records only who/how-much/what/when — no account, no private category id. The shared↔personal link is the per-user `expense_postings` table (expense_id ↔ your private transaction_id + role); the account you paid from and your category live only on your private `transactions` row, reachable solely via your own `expense_postings`. So a member can never see another member's account or category. `createSplitExpense` still takes `accountId`/`categoryId` as write-time inputs (for the private posting) but no longer stores them on the shared rows. Deploy: apply `0010` + redeploy streams.
- 2026-07-09 — **Expense splitting — auto-split by date range.** Group create dialog + group detail now expose an **auto-split** toggle (enabled only when both dates are set; stored in `split_groups.auto_split`). In the add-transaction flow, when an expense's date falls inside an active auto-split group's `[start_date, end_date]`, the split is **auto-enabled** into that group (equal, members prefilled) with an accent banner; a `splitTouched` flag means once the user manually toggles the split or changes the group, auto-apply stops — turning off "Split this expense" is the per-transaction **exclude**. No migration (reuses `0008`). Applied client-side at save time (offline-first), so it covers new expenses; a retroactive sweep of already-logged transactions isn't included.
- 2026-07-09 — **Expense splitting — groups/trips + dashboard tile.** No migration (reuses `split_groups`/`split_group_members` from `0008`). `src/splits/write.createGroup`; hooks `useGroups`/`useGroup`/`useGroupMemberContactIds`/`useGroupExpenses`/`useGroupBalances` (per-contact edges within a group). New routes `/groups` (list + create dialog: name, trip/group, members, optional dates) and `/groups/[id]` (total spent, you're-owed/you-owe, per-member balances, expense list). Add-transaction split editor gains a **group/trip selector** that prefills participants from members and tags the expense (`shared_expenses.group_id`). New **"Friends & splits" dashboard tile** (owed/owe/net + top contacts; free, default-off) registered in `src/dashboard.ts` + `dashboard/tiles.tsx`. Nav: "Groups & trips" under Money; Friends↔Groups cross-links. **Not yet:** auto-date-range auto-split of all transactions (the `auto_split` flag exists, unused), split insights, and real multi-user (invites/membership RLS/PowerSync buckets) — the remaining Phase 3.
- 2026-07-09 — **Expense splitting — Phase 2 (modes, multi-payer, contact-paid, settlements).** Migration `0009_settlements.sql` (settlements table + RLS/grants; added to AppSchema + sync-streams). `src/splits/math.ts`: `splitByWeights` (largest-remainder for equal/percent) + `contactEdges` (per-contact pairwise edge = `(share_c·self_paid − self_share·paid_c)/total`, rounded to sum exactly to your net — supports multi-payer). `write.createSplitExpense` generalised: **equal/exact/percent** shares, **multi-payer** (you + contacts), and unified private postings — expense for the part of your share you paid (on your account), expense on hidden **Payable** for the part a contact covered (you owe, still budget-counted), transfer of overpayment to **Receivable**; cash-out = exactly what you paid, budget = exactly your share. `settleUp` (received: Receivable→account; paid: account→Payable; `accountId=null` = "None", record only). `useFriendBalances` rewritten to derive per-contact net from shares+payers edges minus settlements (never materialised). UI: add-transaction split editor now has mode chips, include-you toggle, per-participant share inputs (exact/percent with live totals), a "multiple people paid" payer editor with validation, and a your-share/net summary; Friends page shows owes-you / you-owe / settled with a **Settle** dialog (amount + account or None). **Phase 2 scope done;** groups/trips + real multi-user (invites) are Phases 3–4. Deploy: apply `0009` + redeploy streams.
- 2026-07-09 — **Expense splitting — Phase 1 (foundation).** Migration `0008_splits.sql`: adds `accounts.kind` ('real'|'receivable'|'payable') and tables `contacts`, `split_groups`, `split_group_members`, `shared_expenses`, `shared_expense_shares`, `shared_expense_payers`, `expense_postings` (owner RLS + grants; single-user for now, schema shaped for Phase-3 shared visibility). Added to `packages/db` AppSchema + per-user `sync-streams.yaml`. **Double-counting solution:** a split expense books only YOUR share as `expense` on your real account, and the rest as a `transfer` to a hidden **Receivable** virtual account (`kind='receivable'`, lazily created per currency, `include_in_net_worth=0`) — so budgets/insights count only your share while the account reflects the full outflow, with ZERO changes to existing analytics queries. Virtual accounts hidden from pickers/lists via `IFNULL(kind,'real')='real'` (new-tx, edit-tx, `useAccountBalances`). Domain module `src/splits/` (`math.splitEqual` largest-remainder; `accounts.ensureVirtualAccount`; `write.createEqualSplitExpense` + `addContact`; `hooks.useContacts`/`useFriendBalances`). UI: "Split this expense" section in the add-transaction flow (equal split, you as sole payer, pick/add contacts) + a **Friends** page (nav under Money) showing per-contact "owes you" balances. **Phase 1 scope:** you pay, equal split only; contact-paid splits, exact/percent, multi-payer, settlements, groups/trips, and real multi-user come in later phases. Deploy: apply `0008` + redeploy sync streams.
- 2026-07-09 — **Insights page IS the stack feed now.** Replaced the old recharts-panel `/insights` page (cashflow/category/label/comparison charts) with the swipeable `InsightFeed` (premium-gated, blurred teaser for non-paid). Deleted the separate `/insights/feed` route and its entry cards. `/insights` = the stacks.
- 2026-07-09 — **Insight feed: dropped 3D for polished 2D charts + 9 new insight types.** Removed react-three-fiber visuals (`Charts3D.tsx`, `perf.ts` deleted); `VisualSpec` kinds renamed to `bars`/`area`/`donut`/`gauge`/`progress`. `Charts2D.tsx` rebuilt (gradient area, rounded bars w/ value labels, horizontal ranked bars, donut with center label, threshold gauge, full-ring progress; unique gradient ids via `useId`). `InsightCard` is now 2D-only (no lazy/Suspense/WebGL). **New generators** (15 total): biggest_expense, weekday_pattern, label_breakdown, subscriptions_load, month_pace, no_spend_days, goal_progress, category_spike, avg_daily_spend — added queries in `useInsightStack` (70-day expense/day, top expenses, labels, active subscriptions, goals+allocations, 4-month category history) and derived weekday averages, month pace/projection, no-spend days, daily averages, category spike. `composeStack` cap raised to 12.
- 2026-07-08 — **Insight feed: swipeable, premium-gated, 3D visuals.** New route `app/insights/feed/page.tsx` (gated by `useEntitlement().isPaid`; blurred teaser + Upgrade for non-paid). Architecture: `src/insights/types.ts` (InsightCard discriminated-union payload + `VisualSpec` kinds bars3d/ribbon3d/donut3d/gauge3d/orb3d — the portable contract), `src/insights/generators.ts` (pure generators: weekly_summary, budget_warning, savings_achievement, spending_trend, category_breakdown, streak + `composeStack` ranking/dedupe by cadence key), `src/insights/useInsightStack.ts` (PowerSync aggregates → continuous 14-day/8-month series, this-month categories, simplified monthly budget spend, streak → generators; offline-first, reactive), `src/insights/perf.ts` (`usePrefer3D` = WebGL && !reduced-motion; `THEME_HEX`). UI in `src/ui/feed/`: `InsightFeed` (mobile = native CSS scroll-snap + IntersectionObserver active-index; desktop = framer coverflow deck via wheel/keys/pointer-swipe/buttons; measured fill-height), `InsightCard` (mobile stacked / desktop landscape chart-left-text-right; only active±1 mount a WebGL canvas via `near`), `Charts3D` (R3F; active card `frameloop="always"`, neighbours `"demand"`; canvas `touch-action:none` so it never steals paging), `Charts2D` (recharts fallback), `ProgressRail` (story segments) + "N of M · K left" counter. Entry points added on `/insights`. Decisions: client-side generation, 2D coverflow desktop, 3D-everywhere w/ 2D fallback.
- 2026-07-08 — **Trial onboarding: welcome dialog + persistent countdown banner.** New `src/ui/TrialNotice.tsx` (mounted in `AppShell` above page content): shows registered trial users a one-time welcome dialog (seen-flag in localStorage keyed by email) announcing the 14-day full-access trial and what becomes paid-only after, plus a persistent banner with days-left + what they'll lose + an Upgrade CTA to /settings. Only renders when `session && !isGuest && useEntitlement().isTrial` (hides once they upgrade or the trial lapses). Also: **plan cards are tappable to reveal that plan's inclusions**, Ask PocketCare quota now uses the exact `useEntitlement()` formula (Settings ↔ assistant always agree), current plan shows cycle+renewal, and the **dev tier override was removed** (entitlements are now purely server-driven).
- 2026-07-08 — **ROOT CAUSE of credits/plan changes not applying: edge functions queried the wrong schema.** `entitlements` and `payments` live in the **`pocketcare`** schema, but all five edge functions (`assistant`, `razorpay-webhook`, `-subscription`, `-credits`, `-cancel`) built their Supabase client with the default **`public`** schema — so every `.from("payments"/"entitlements")` hit `public.*`, failed, and (in the webhook's try/catch) returned 200 with nothing written. Fixed: all clients now use `createClient(url, key, { db: { schema: "pocketcare" } })`. **Also requires exposing `pocketcare` to PostgREST** (Dashboard → API → Exposed schemas, or `alter role authenticator set pgrst.db_schemas='public, graphql_public, pocketcare'; notify pgrst,'reload config';`) — see DEPLOY.md §8.6. Redeploy all five functions after this.
- 2026-07-08 — **Credit-pack purchases now reliably apply + AI credit balance in Settings.** Root cause: Razorpay does NOT copy an order's `notes` onto the payment entity, so the webhook's notes-based credit logic only worked on `order.paid`, not `payment.captured`. Fixed by recording a **pending `payments` row (status `created`)** when the order is created in `razorpay-credits`, then matching the webhook back by `razorpay_order_id` (works on either event; race-safe via a conditional `status='created'→'captured'` update so credits apply exactly once). Billing history query now filters `status='captured'` (hides abandoned orders). `src/ui/Billing.tsx` shows an **AI credit balance** card (prompts available = plan quota left this cycle + purchased credits that never expire; shows reset date). **Redeploy `razorpay-credits` and `razorpay-webhook`; ensure `order.paid` OR `payment.captured` is enabled in the Razorpay webhook.** Pre-fix test payments won't have a pending row — re-test after redeploy.
- 2026-07-04 — **Billing history + branded invoices.** Webhook now also inserts a `payments` row on `subscription.charged` (not just credits), so history covers subscriptions too. Settings billing shows **Billing history** (from the synced `payments` table) with a per-row **Invoice** button. `src/billing/invoice.ts` renders a self-contained, letterheaded HTML invoice (PocketCare mark + `INVOICE_ISSUER` legal details — placeholders to fill; GSTIN optional → "Tax Invoice") and opens it in a new tab to print / save as PDF (downloads as fallback). Redeploy `razorpay-webhook` for subscription-charge history.
- 2026-07-04 — **Billing: cancel subscription + Modal fix.** New `razorpay-cancel` edge function (cancels at cycle end via Razorpay `cancel_at_cycle_end`; sets `subscription_status='cancelling'`, webhook downgrades to Free when it actually ends). `cancelSubscription()` in `src/billing.ts`; Settings billing shows a "Cancel plan" button when `subscription_status==='active'` and a "cancels at end of cycle" note when cancelling. Fixed the `<Modal>` missing `open` prop on the assistant privacy dialog.
- 2026-07-04 — **Razorpay billing: 3 tiers (Free/Lite/Pro) + recurring subs + AI credit packs.** Migration `0007_billing.sql` adds `lite`/`pro` to tiers, Razorpay/subscription columns on `entitlements`, a `payments` audit table (webhook-written, owner-readable, synced), and re-seeds the 14-day trial WITH 200 AI prompts. **Server-authoritative** entitlements: edge functions `razorpay-subscription` (recurring Subscriptions API, plan IDs from `RZP_PLAN_*` env), `razorpay-credits` (one-time order for a pack), and `razorpay-webhook` (HMAC-verified, idempotent — activates/renews plan, resets monthly quota, adds credits; deploy `--no-verify-jwt`). Client: `src/billing/plans.ts` (prices/quotas/packs — Lite ₹49/499·50, Pro ₹99/999·200, packs ₹29/49/99), `src/billing.ts` (Razorpay Checkout), `src/entitlement.ts` `useEntitlement()` (effective tier + trial + quota; single gating source), `src/ui/Billing.tsx` (Settings plan cards + buy-credits + dev preview). Gating switched from `tier==='premium'` to `isPaid` across insights/statements/subscriptions/dashboard; assistant out-of-quota buys real credit packs. Assistant edge function already enforces/decrements quota. Setup in DEPLOY.md §8. Untested against live Razorpay.
- 2026-07-03 — **Back-dated transactions + list overflow fix.** Add-transaction page now has a **Date** field (defaults today, `max` today); `occurredAtIso()` keeps live time for today, else local noon (no TZ day-shift). **Mobile overflow:** long comma-joined label strings (common after CSV import) were pushing transaction rows past the screen — the label now truncates with ellipsis and the amount is `flex-shrink:0` in the Recent-activity tile, transactions list, and search results; dashboard account cards get `min-width:0` + ellipsis on type/name/balance.
- 2026-07-03 — **Sidebar cleanup:** grouped the long nav into sections (Overview / Money / Planning / Growth / Settings) with muted headers; removed the dark-mode toggle from the sidebar (theme already lives in Settings → Appearance); added a `PocketCare v{APP_VERSION}` footer (version bumped to 0.1.0 in both package.jsons).
- 2026-07-03 — **Assistant is now Premium-gated** (`useTier` in `app/assistant/page.tsx` → lock/"Go Premium" card for free tier, matching Insights/Statements). Settings Plan line lists it. Note: tier is a client-side preview store (`src/tier.ts`), so this is a UX gate; true server enforcement would require syncing entitlements and checking tier in the edge function. Also fixed mobile overflow on Statements (date fields stack at ≤~460px) and Ask PocketCare (long example/history chips wrap; input flex:1/min-width:0 + Send flex-shrink:0).
- 2026-07-03 — **CSV import/export (extensible for other apps).** New `/data` page (linked from Settings → Import & export). **Export:** all transactions → PocketCare-format CSV (`src/data/exportCsv.ts`, `downloadText`), round-trippable. **Import:** pluggable **adapter** architecture (`src/data/adapters.ts`): a canonical `CanonRow` + `ImportAdapter` interface, with a full `pocketcare` adapter and a **`wallet` (Wallet by BudgetBakers)** adapter — validated against a real export (semicolon CSV; signed `amount`; `type` Expenses/Income; `payment_type_local` mapped to our methods; `|`-separated `labels`); validated on 1117- and 1554-row samples. **Transfers:** Wallet splits each into two unlinked one-sided rows (− source / + dest); imported as **signed `adjustment` entries** so both account balances match Wallet exactly and transfers stay out of income/expense analytics. Importer **infers account type from name** (stocks/mutual_funds/credit_card/cash/current, else savings). Add more sources by implementing `parse`. Dependency-free CSV parser/serializer (`src/data/csv.ts`, auto-detects `,`/`;`). `importTransactions()` (`src/data/importCsv.ts`) find-or-creates accounts (default type `savings`)/categories/labels, maps payment-method labels→codes, creates txns via the repo (ledger + labels junction), with optional duplicate-skip (account+amount+type+date) and a preview+confirm step. Balances recompute from the ledger, so import is safe/idempotent-ish.
- 2026-07-03 — **Assistant hardening: guardrails, cost controls, chat history, memory.** (1) **Guardrails/persona:** rewrote the system prompt (`PERSONA` in `app/assistant/page.tsx`) — PocketCare's calm money-companion voice, STRICT scope (only the app + the user's own finances), refuses code generation and off-topic/other-people/medical-legal/stock-picking/jailbreak requests, never invents numbers, and carries an explicit "this is guidance not professional advice — verify important numbers" disclaimer (also shown in the UI). (2) **Token/cost optimization:** stable persona + compacted snapshot sent as cacheable system blocks (`cache_control` prompt caching); `summaryForPrompt()` trims/rounds; conversation history capped to last 16 messages/turn; `max_tokens` 700; cheap default model. (3) **Chat history:** migration `0002_assistant.sql` adds `assistant_threads`/`assistant_messages`/`assistant_memory` (pocketcare schema, RLS, synced); assistant page persists the transcript, lists past chats (History), New chat, open/delete. (4) **Memory:** a non-confirm `remember` tool writes durable facts to `assistant_memory`, injected into the system prompt so it knows the user better over time. Financial writes still require the per-action confirm card. **Apply 0002 + redeploy sync streams.**
- 2026-07-03 — **AI assistant "Ask PocketCare" (Claude, tool-using, action-taking).** New `/assistant` chat page plans purchases/savings in natural language (e.g. "buy an iPhone in the Diwali sale"). Architecture: **Supabase Edge Function** `supabase/functions/assistant` proxies Anthropic's Messages API (key = `ANTHROPIC_API_KEY` secret, `verify_jwt` on; never in the browser). The client computes an **aggregated on-device summary** (`src/assistant/summary.ts` — net worth/accounts, 3-mo avg income/expense, fixed obligations, goals, upcoming renewals; never raw transactions) and sends it as the system prompt. **Tool-use loop** in `app/assistant/page.tsx`: model calls write tools (`src/assistant/tools.ts`: `create_goal`, `reserve_to_goal`, `create_budget`) which run locally against SQLite only **after a per-action confirm card**. Nav entry + i18n `nav.assistant`. Setup/cost notes in DEPLOY.md §7 (needs `ANTHROPIC_API_KEY`; optional `ASSISTANT_MODEL`, default `claude-3-5-haiku-latest`). Non-streaming MVP; untested against the live API from here.
- 2026-07-03 — **Sitewide amount masking + languages (drop Arabic, add Dutch).** (1) **Masking:** `src/ui/Money.tsx` exports `<Money>` and `useMoneyFmt()` (a masking drop-in for `format(m,"en-US")`); applied across accounts, search, budgets, goals, subscriptions, loans, investments, cards (dashboard/tiles/transactions already masked). New Settings → **Privacy** toggle drives the existing reactive `amountsHidden` pref. Statement export + insight charts stay unmasked. (2) **i18n:** removed `ar` (RTL), added `nl` (Dutch); expanded en/hi/nl bundles (nav, page titles, settings sections, common). Wired `useTranslation` into the sidebar nav (every page) + all page H1 titles + settings section headings; language selector persists + calls `changeLanguage` + sets `<html lang>`; boot prefers saved `lang`. (In-form field labels remain English — translatable incrementally via the same `t()` pattern.)
- 2026-07-03 — **Fixed false "Offline" while actually online.** The status used PowerSync's transient `connected` flag, which is often false even while synced → Settings wrongly said "Offline". Now `SyncInfo.online` tracks the real `navigator.onLine` (with online/offline listeners), and `syncMessage()` only says "offline" when actually offline; an online transient websocket blip shows nothing (auto-reconnects); a real non-network error shows the calm "we're on it" note. Settings line now shows Synced/Syncing/Connecting from `lastSyncedAt`/`hasSynced`, not `connected`.
- 2026-07-03 — **Offline navigation + friendly sync messages.** Offline, the SW served the cached `/` shell for every navigation, so any page bounced to the dashboard. Now the SW **precaches the top-level routes** at install and caches each navigation under its own URL, so offline navigation lands on the right page (dynamic routes like `/transactions/[id]` still need a first online visit). Cache bumped to `pocketcare-v3`. **User-facing errors** are no longer raw: `src/sync.ts` adds `syncMessage()` which classifies problems as network/offline ("You're offline… will sync when back online", calm) vs other ("having trouble syncing — we're on it", warning); the raw error stays in the console only. AppShell banner + Settings line use it.
- 2026-07-03 — **Fixed installed-PWA infinite loading.** The old service worker (`public/sw.js`) intercepted EVERY GET and, on any fetch failure, returned the cached HTML shell — so in standalone mode PowerSync's SQLite **WASM/worker** (or a JS chunk) could be served the wrong response and the DB never initialized → stuck on the spinner. Rewrote the SW to only handle same-origin top-level **navigations** (network-first → cached `/` offline) and pass through scripts/wasm/workers/cross-origin untouched; cache bumped to `pocketcare-v2`. Also made `initSystem()` connect PowerSync in the **background** (no longer `await`ed) so a slow/unreachable connect can't block first paint. (Existing installs may need an uninstall/reinstall or a clear-site-data to drop the old SW.)
- 2026-07-03 — **Fixed date-input overflow + subscriptions desktop layout.** Root cause of the statements page overflowing: `<input type=date>` has a large intrinsic min-width, and a page-root `display:grid` auto-column sizes to the widest child's min-content — so the date row forced the whole page wider than the viewport (header/card clipped). Fix: `.input { min-width: 0 }` globally + `min-width:0` on the date field wrappers so inputs shrink and the grid column stays at viewport width. Subscriptions now uses `.subs-cols` — on ≥900px it's a two-column layout (list + a **sticky** right column holding Add + Simulator) so you don't scroll past the list to add; single column on mobile.
- 2026-07-03 — **Subscription cards redesign + reusable KebabMenu.** New `src/ui/KebabMenu.tsx` (⋮ dropdown, `MoreIcon`) for per-row actions. Subscription rows no longer cram amount/monthly/next-due onto one wrapping line next to Edit/Remove buttons — the info is spread into three labelled mini-stats (`Stat`: per-cycle amount, per-month, next due) with a top-right kebab menu holding Edit + Remove. Summary card wraps on narrow screens. (KebabMenu is reusable for goals/budgets/loans rows if we want the same treatment.)
- 2026-07-03 — **Mobile overflow pass + FAB rework + collapsible search filters.** Wrapped the overflowing header rows (dashboard actions, transactions search+type chips, statements card top row / summary) with `flex-wrap` + flex-basis instead of fixed widths. **Add-transaction FAB** now renders only on the dashboard (`pathname === "/"`), pill-shaped with a "Add transaction" label (other pages use their own contextual add buttons). `.shell-main` gets bottom padding on mobile so the FAB doesn't cover content. **Search filters** (type/account/date/min-max) are now collapsed behind a "Filters · N ▾" toggle with a Clear button; only the main search box is always visible.
- 2026-07-03 — **Root-caused cross-device sync: removed client-side seeding.** The upload queue was blocked by `duplicate key value violates unique constraint "labels_user_id_name_key"` — default labels were seeded BOTH server-side (`handle_new_user` trigger) and client-side (`src/defaults.ts`) with different UUIDs but the same `(user_id,name)`; the client's copy collided on upload and, because PowerSync uploads are ordered/all-or-nothing, blocked every later write (incl. accounts) from ever reaching the server. Fix: **server is the sole seeder** — deleted `src/defaults.ts` and its calls; local DB filename bumped to `pocketcare-v2.db` so existing clients discard the poisoned queue and re-download clean server state. (Trade-off: any writes stuck in the old local queue are dropped — they never synced anyway.)
- 2026-07-03 — **Mobile polish + sync diagnostics.** (1) Fixed the huge gap above short pages (e.g. Accounts): mobile `.shell` now `grid-template-rows: auto 1fr; align-content:start` so the topbar isn't stretched. (2) Fixed mid-word text breaks ("Premi um"): removed `overflow-wrap:anywhere`, scoped `break-word` to text elements, and `.btn`/`.chip` are `white-space:nowrap` with their rows set to `flex-wrap:wrap` (budget periods, settings tier toggle). (3) **Sync visibility:** new `src/sync.ts` `useSyncStatus()` (reads `db.currentStatus` + `registerListener`); AppShell shows a warning banner when uploads fail, Settings shows a live "Synced/Offline/Sync error" line; connector now `console.error`s the real upload error (e.g. PostgREST "schema must be exposed" / RLS) before rethrowing. Use this to diagnose the cross-device issue — a non-exposed `pocketcare` schema or missing PowerSync publication makes every upload fail silently.
- 2026-07-03 — **Auth model: no auto-guest.** Previously every visitor was signed in anonymously at boot. Now `initSystem()` (`src/powersync.ts`) only connects PowerSync **if a session already exists** — a brand-new visitor stays unauthenticated. Gate moved from an `onboardingSeen` flag to real auth: new `useAuthStatus()` (`src/account.ts`, "loading|none|guest|user`) drives `AppShell`, which redirects to `/onboarding` when there's no session. Onboarding's final step offers **Create account / Sign in / Try as guest**; only "Try as guest" calls `signInAnonymously()`. Login register now `signUp()`s a fresh account directly when there's no session, and only does the in-place `updateUser` upgrade when the current user is an existing guest (keeps their data). OTP verify type tracked (`signup` vs `email_change`); `/login?mode=signin` opens sign-in. Sign-out (full reload) → onboarding.
- 2026-07-03 — **Fixed cross-device sync + mobile layout.** (1) **Multi-device data bug:** app boots as an anonymous guest and `initSystem()` runs once; signing in used a client-side nav (no reload), so `currentUserId` stayed the guest id and PowerSync kept syncing the empty guest account — the real account never downloaded on a 2nd device. Fix: `src/powersync.ts` subscribes to `supabase.auth.onAuthStateChange` and **re-keys** on identity change (update `currentUserId` → `disconnectAndClear()` → reconnect with the new JWT → reseed). Same-UID guest→register (updateUser) keeps the id so local unsynced writes are preserved; SIGNED_OUT clears. (2) **No horizontal scroll on mobile:** `overflow-x: clip` + `overflow-wrap: anywhere` on body; `min-width:0` on `.shell > *` / `.shell-main` (grid children were blowing out); `.input { max-width:100% }`; auto-fit `minmax(min(Npx,100%),1fr)` grids (dashboard/accounts cards, cards page); statements table wrapped in `overflow-x:auto`; insights/subscriptions two-col grids use `.dash-cols` (collapse ≤860px). (3) **Mobile nav drawer** now full height (`100dvh`), `width: min(82vw,320px)`, `overflow-y:auto` so it covers the left side and scrolls instead of cutting off.
- 2026-07-03 — **Customizable dashboard tiles.** Users add/remove dashboard tiles via a "Customize" modal (gear button in dashboard header). Reactive pref store `src/dashboard.ts` (localStorage, `useSyncExternalStore`; `DASHBOARD_TILE_IDS`, `useDashboardTiles`, `setTileEnabled`; default enabled = recent + spending). Tile catalog + components in `src/dashboard/tiles.tsx` (`TILE_CATALOG`, `TileView`, `tileMeta`): recent, spending pie, budgets (progress via `spentThisPeriod`), goals (progress), and Premium insight tiles cashflow/netTrend/byCategory/byLabel/monthCompare (mirror `/insights` queries, gated on `useTier`). Dashboard (`app/page.tsx`) renders enabled tiles in a `.dash-grid` (2-col; `span:'full'` tiles span both). Net-worth hero + Accounts stay fixed. New `SlidersIcon`. Premium tiles hidden/locked for free tier.
- 2026-07-03 — **Moved all DB objects into a dedicated `pocketcare` schema** (off `public`). `0001_init.sql`: `create schema pocketcare` + `set search_path to pocketcare, public`; every function pins `set search_path = pocketcare, public` (they run at runtime); `auth.users` trigger calls `pocketcare.handle_new_user()`; explicit grants to anon/authenticated/service_role at end. Connector (`packages/db/src/connector.ts`) writes via `client.schema('pocketcare').from(...)` (new `DB_SCHEMA` const, constructor arg). `sync-streams.yaml` queries schema-qualified (`pocketcare.<table>`). **Required external config:** (1) Supabase → Settings → API → **Exposed schemas** add `pocketcare`; (2) PowerSync publication must include the schema — `create publication powersync for tables in schema pocketcare;`; (3) verify PowerSync maps `pocketcare.<table>` → local SQLite table `<table>` (matches AppSchema). Local SQLite/client AppSchema unchanged (no schema concept locally).
- 2026-07-03 — **DB normalization (fresh schema).** All enums converted to **lookup tables** (`account_types, transaction_types, category_kinds, periods, commitment_kinds, tiers, rate_modes, payment_methods`; `id=code`) referenced by FK. All many-to-many relations moved to **junction tables**: `transaction_labels` (replaces `transactions.label` text col), `budget_categories` + `budget_labels` (replace budget `scope/scope_ref/category_ids/label_names`), `account_type_payment_methods` (payment methods per account type). `0001_init.sql` rewritten (71 statements, pglast-valid) and old `0002/0003/0004` migrations folded in & deleted. Client `AppSchema` (`packages/db/src/index.ts`) + `sync-streams.yaml` (new global `reference_data` stream; junctions in `user_data`) updated. `packages/data` repos: `transactions.create/update` take `labels: string[]` (find-or-create → junction); `budgets.spentThisPeriod` reads junctions; `search` matches label names via junction. UI: transaction new/edit (labels→junction, **payment method from lookup mapping, stores code**), read views (transactions list, dashboard, search, statements, insights) compute labels via `GROUP_CONCAT`; **budgets page: junction read/write + multi-select category/label editing in row edit mode**. `Transaction` type dropped `label`. 49 core tests still pass. **Fresh Supabase project required (dev DB reset).**
- 2026-07-02 — **Migrations consolidated** into a single `0001_init.sql` (final defs, incl. subscriptions.purchased_on) for fresh Supabase. **Publishable key** support (`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, falls back to anon). **Categories/labels fix:** client-side fallback seed (`src/defaults.ts`, runs after first sync if none exist) — root cause was download-sync not delivering server-seeded rows. **Reactive prefs** (`src/prefs.ts`): base currency default **INR**, applied app-wide + `hide amounts` masking. Dashboard: top-right mask toggle, per-account eye to include/exclude from net worth, subtle account color accents. Subscriptions: **purchase date → next-due** calc. **Password eye** toggle. **Mobile-friendly**: responsive `.shell` + hamburger drawer + scrim (`globals.css`, AppShell). **Expanded color palette** (`src/colors.ts`, incl. indigo/jewel tones) + colour dots on transactions/dashboard.
- 2026-07-02 — **Proper auth flow:** register (username, email, password, confirm) → converts guest in place via `updateUser({email, data:{username}})` → **email OTP step** (`verifyOtp` type `email_change`) → set password. Sign-in via `signInWithPassword`. Sessions persist (persistSession + detectSessionInUrl true). Friendly error mapping; "already registered" nudges to sign in. **Supabase config needed:** disable "Secure email change"; add `{{ .Token }}` to the *Change Email Address* email template (for the 6-digit OTP); SMTP for delivery.
- 2026-07-02 — **Branding + polish + cards redesign:** PocketCare **logo** (`src/ui/Logo.tsx`, leaf-in-pocket) in sidebar/onboarding/login; regenerated favicon + PWA icons. **Onboarding gate** (first-run redirect via `onboardingSeen`; onboarding/login render full-screen, no sidebar). **Sign-out warning** modal (guests warned of data loss). **Cards redesigned** in CSS + framer-motion (three.js `WalletScene` removed): wallet header, cards emerge into a vertical list, details on card face (holder = user's display name, optional last-4 via migration `0007`), balance/due on the right; statement/due **editable anytime**; "Add card" button. Global polish: `Modal`, `Spinner/Loading/Skeleton`, route-transition `template.tsx`, card hover-lift, focus rings. **Apply 0006 + 0007; redeploy streams for 0006 (transaction_audit).**
- 2026-07-02 — Settings: **account section** (username edit via auth metadata, guest status + 3-day deletion countdown, sign out), **theme control** (shared `src/theme.ts` store, synced with sidebar), **Help & Support**. Sidebar shows guest chip + countdown. **Transaction editing with audit**: migration `0006` `transaction_audit` table (+AppSchema +streams), `transactions.update()` writes changes + appends audit record (clears items if amount changes), `/transactions/[id]/edit` page shows edit history. Account balance editing (direct adjustment vs recorded transaction). **Apply 0006 + redeploy streams** (added transaction_audit). Webpack pnpm-symlink warnings (js-tokens/react/loose-envify) are harmless.
- 2026-07-02 — Tier moved to a **local reactive store** (`src/tier.ts`, localStorage) — entitlements isn't synced client-side, so the DB-based toggle silently failed. Added `Spinner/Loading/Skeleton` (framer-motion); used on app boot + 3D wallet load. Migration `0005`: rich default categories + sub-categories + starter labels via shared `seed_default_categories()` + **backfill loop for existing users** (fixes empty categories/labels for pre-existing guests). Account edit now edits **balance** (direct adjustment vs recorded transaction). **Labels wired** into the transaction form (colored chips + custom text).
- 2026-07-02 — **Complete-product pass:** named/tagged budgets (category/label + recurring OR custom timeframe; migration `0004`), account editing (`accounts.update` + `/accounts/[id]/edit`), full **Insights** page (recharts: cashflow, net trend, category, label, month-vs-month), **Statements** (period picker + printable PDF), **Free/Premium toggle** in Settings + `useTier()` gating on insights/statements/simulator. 49 tests passing; migrations 0001–0004 valid.
- 2026-07-02 — Initial architecture & reference created (planning phase).
- 2026-07-02 — Added web app: monorepo (Turborepo) with `apps/web` (Next.js + PowerSync WASM) sharing `packages/*` core with mobile.
- 2026-07-02 — Added multi-language (i18next + Intl + RTL) and multi-currency (per-account currency, `exchange_rates`, display-time conversion, base currency).
- 2026-07-02 — Guest identity: anonymous Supabase auth UID from first launch; in-place upgrade on register; cascade delete on `user_id`. (ARCHITECTURE.md §9)
- 2026-07-02 — **Phase 0 implemented:** Turborepo monorepo, shared packages (types, money+FX, finance, i18n, entitlements, db, data, ui-tokens), Expo + Next.js skeletons. 31 unit tests passing on money/finance/entitlements.
- 2026-07-02 — **Phase 1 (build):** Supabase migration `0001_init.sql` (all tables, RLS owner policies, deferred items-reconcile trigger, updated_at triggers, cascade deletes, guest-purge fn; validated via libpg_query). New `@pocketcare/ledger` (signed effects, ledger-derived balances, available/blocked, net-worth FX aggregation) — 8 tests. PowerSync `SupabaseConnector` + anonymous-auth helpers. PowerSync-backed account/transaction/balance repositories (atomic txn+items with reconcile guard). Mobile screens: accounts list (reactive `useQuery`), add-transaction with "+" breakdown builder. **39 tests passing.**
- 2026-07-02 — Switched sync config to **Sync Streams** (`sync-streams.yaml`, edition 3): user_data stream (queries array, auto_subscribe) + global exchange_rates (with synthesized id).
- 2026-07-02 — **WEB-ONLY PIVOT + full feature build:** deleted mobile; built dashboard, all-3-type transactions + search, three.js credit-card wallet (scroll-driven), goals (EF-first), subscriptions + impact simulator, loans/recurring (% of income), investments, settings (dark mode, currency, language, sub-categories, colored labels, plan), onboarding + login (guest→register in place), PWA (manifest/SW/icons/install). Account colors + per-account net-worth inclusion. Migration `0003` (categories.parent_id, accounts.include_in_net_worth, labels table). Schema+streams extended (labels, subscriptions, loans, holdings). Animated progress bars (framer-motion), charts (recharts).
- 2026-07-02 — **Phase 2 (build):** `@pocketcare/budget` (period bounds D/W/M/Y, budget progress, threshold-crossing for notifications, credit-card billing cycle with month clamping) — 10 tests. Migration `0002` (credit_card_details surrogate id; new-user trigger seeds profile + free entitlement + default categories + guest session). Budget & CreditCard repos (+ settle-up = transfer). Mobile: budgets screen, account/new, notifications helper. **Web mirrored**: providers + accounts, accounts/new, transactions/new, budgets. credit_card_details added to AppSchema + streams (redeploy streams). **49 tests passing.**
