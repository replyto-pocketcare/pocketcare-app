/// Skeleton marker for the Domain package (P0.3). Real domain logic (Money,
/// ledger, budget, ...) is ported here module-by-module starting at P1.1 —
/// see docs/plans/native-mobile-apps.md §5. Ported from the TS source in
/// packages/core/*; correctness is judged against
/// tools/golden-vectors/vectors/*.json, not against a fresh reading of the TS.
public enum DomainSkeleton {
    public static let ready: Bool = true
}
