package care.pocket.domain

/**
 * Skeleton marker for the :domain module (P0.2). Real domain logic (Money,
 * ledger, budget, ...) is ported here module-by-module starting at P1.1 —
 * see docs/plans/native-mobile-apps.md §5. Ported from the TS source in
 * packages/core/*; correctness is judged against tools/golden-vectors/
 * vectors/*.json, not against a fresh reading of the TS.
 */
public object DomainSkeleton {
    public const val READY: Boolean = true
}
