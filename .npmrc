# Flat, hoisted node_modules (reliable resolution for the bundler).
node-linker=hoisted

# Allow install to reconcile the lockfile (e.g. on Vercel) instead of failing
# with a frozen-lockfile error when dependencies change.
frozen-lockfile=false
