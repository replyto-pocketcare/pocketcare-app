# Dependencies
node_modules/
.pnp.*

# Builds
dist/
.next/
.turbo/
.vercel/
*.tsbuildinfo

# Expo / native
apps/mobile/.expo/
apps/mobile/ios/
apps/mobile/android/
*.orig.*

# Env & secrets — never commit
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Test / coverage
coverage/

# Support key material — NEVER commit
support-key/
*.share
_tmp_*

# iOS local build-configuration overrides (see apps/ios/Config/Sanvya.xcconfig).
apps/ios/Config/Sanvya.local.xcconfig

# Scratch file written by tools/golden-vectors/export-mobile-schema.mjs while it
# imports AppSchema. Normally removed on the same run; ignored so a sandbox that
# cannot delete it never leaves it staged.
packages/db/src/__schema_export_temp__.ts
